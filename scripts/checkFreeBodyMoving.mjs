import assert from 'node:assert/strict';import {chromium} from 'playwright-core';const b=await chromium.launch({executablePath:'C:/Program Files/Google/Chrome/Application/chrome.exe',headless:true});try{const p=await b.newPage({viewport:{width:1536,height:1024}});await p.goto('http://127.0.0.1:5371/mechanics/free-body-diagrams');await p.getByRole('button',{name:'Run motion',exact:true}).click();await p.waitForTimeout(1200);await p.getByRole('button',{name:'Pause',exact:true}).click();const clipped=await p.locator('.fbd-scene-svg text').evaluateAll(nodes=>nodes.map(n=>({text:n.textContent,x:n.getBBox().x,right:n.getBBox().x+n.getBBox().width})).filter(b=>b.x<0||b.right>920));console.log({clipped});assert.equal(clipped.length,0);await p.screenshot({path:'artifacts/studio-rebuild/13-free-body-diagrams/moving-check.png',fullPage:true})}finally{await b.close()}
