import {chromium} from 'playwright-core';import fs from 'node:fs/promises';await fs.mkdir('artifacts/studio-rebuild/16-torque-levers',{recursive:true});const b=await chromium.launch({executablePath:'C:/Program Files/Google/Chrome/Application/chrome.exe',headless:true});try{const p=await b.newPage({viewport:{width:1536,height:1024}});for(const id of ['rotational-dynamics','balancing-act']){await p.goto('http://127.0.0.1:5371/experiments/'+id);await p.locator('main').waitFor({timeout:60000});await p.screenshot({path:`artifacts/studio-rebuild/16-torque-levers/before-${id}.png`,fullPage:true});console.log((await p.locator('body').innerText()).slice(0,600))}}finally{await b.close()}
