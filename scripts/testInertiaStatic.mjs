import{chromium}from'playwright-core';import assert from'node:assert/strict';const b=await chromium.launch({executablePath:'C:/Program Files/Google/Chrome/Application/chrome.exe',headless:true});try{const p=await b.newPage({viewport:{width:1536,height:1024}}),e=[];p.on('pageerror',x=>e.push(x.message));await p.goto('http://127.0.0.1:5371/motion/first-law-inertia');await p.locator('.ils-stage').waitFor();const sl=n=>p.getByRole('slider',{name:n,exact:true}),sp=n=>p.getByRole('spinbutton',{name:n+' value',exact:true}),set=async(n,v)=>{await sp(n).fill(String(v));await sp(n).press('Enter');await p.waitForTimeout(50)};for(const[n,lo,hi]of[['Surface Drag (friction)',0,1],['Cart Speed',0,2],['Object Mass',.05,1]]){await sl(n).focus();await sl(n).press('Home');assert.equal(Number(await sl(n).inputValue()),lo);await sl(n).press('End');assert.equal(Number(await sl(n).inputValue()),hi);await set(n,-1);assert.equal(Number(await sl(n).inputValue()),n==='Object Mass'?.05:lo)}await p.getByRole('button',{name:'Predict',exact:true}).click();await p.getByRole('button',{name:'It keeps moving',exact:true}).click();assert.match(await p.getByRole('status').innerText(),/Correct/);await p.keyboard.press('Escape');for(const[width,height]of[[1536,1024],[1440,900],[1280,720],[1024,768],[768,1024],[390,844]]){await p.setViewportSize({width,height});assert.equal(await p.evaluate(()=>document.documentElement.scrollWidth-innerWidth),0)}console.log({checks:'controls, boundaries, prediction, six sizes',errors:e});assert.deepEqual(e,[])}finally{await b.close()}


