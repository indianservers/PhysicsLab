import{chromium}from'playwright-core';import assert from'node:assert/strict';
const b=await chromium.launch({executablePath:'C:/Program Files/Google/Chrome/Application/chrome.exe',headless:true,args:['--disable-gpu']});try{const p=await b.newPage();const errors=[];p.on('pageerror',e=>errors.push(e.message));await p.goto('http://127.0.0.1:5371/motion/second-law-fma');await p.locator('.s2-readings').waitFor();const slider=n=>p.getByRole('slider',{name:n,exact:true}),field=n=>p.getByRole('spinbutton',{name:n+' value',exact:true});const set=async(n,v)=>{await field(n).fill(String(v));await field(n).press('Enter');await p.waitForTimeout(40)};await set('Applied Force',2.4);await set('Cart Mass',.5);await set('Friction',0);assert.match(await p.locator('.s2-readings').innerText(),/4.80/);await p.getByRole('button',{name:'Predict',exact:true}).dispatchEvent('click');await p.getByRole('button',{name:'It doubles',exact:true}).dispatchEvent('click');assert.match(await p.getByRole('status').innerText(),/Correct/);await p.getByRole('button',{name:'Reset',exact:true}).dispatchEvent('click');assert.equal(await slider('Applied Force').inputValue(),'2.4');assert.deepEqual(errors,[]);console.log('Second Law controls, F=ma reading, prediction and reset pass')}finally{await b.close()}
