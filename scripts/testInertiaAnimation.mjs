import{chromium}from'playwright-core';import assert from'node:assert/strict';const b=await chromium.launch({executablePath:'C:/Program Files/Google/Chrome/Application/chrome.exe',headless:true});try{const p=await b.newPage();await p.goto('http://127.0.0.1:5371/motion/first-law-inertia');await p.locator('.ils-stage').waitFor();const play=p.locator('.ils-actions button').nth(3),pause=p.locator('.ils-actions button').nth(0);await play.dispatchEvent('click');await p.waitForTimeout(300);const before=await p.locator('.ils-graph').innerText();await pause.dispatchEvent('click');await p.waitForTimeout(100);const after=await p.locator('.ils-graph').innerText();console.log({before:before.slice(0,40),after:after.slice(0,40)});assert(before.includes('MOTION GRAPH'));await b.close();console.log('animation smoke pass')}catch(e){console.error(e);process.exitCode=1}
