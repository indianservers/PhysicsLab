import{chromium}from'playwright-core';const b=await chromium.launch({executablePath:'C:/Program Files/Google/Chrome/Application/chrome.exe',headless:true});try{const p=await b.newPage({viewport:{width:1536,height:1024}});await p.goto('http://127.0.0.1:5371/motion/velocity-time');await p.locator('.vtm-modes').waitFor();console.log(await p.locator('.vtm-modes').evaluate(el=>{const rules=[];function walk(rs){for(const r of rs){if(r.selectorText&&el.matches(r.selectorText)&&/width/.test(r.cssText))rules.push(r.cssText);if(r.cssRules)walk(r.cssRules)}}for(const s of document.styleSheets)try{walk(s.cssRules)}catch{};const c=getComputedStyle(el);return {width:c.width,max:c.maxWidth,style:el.getAttribute('style'),rules}}))}finally{await b.close()}
