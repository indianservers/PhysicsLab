import{chromium}from'playwright-core';const b=await chromium.launch({executablePath:'C:/Program Files/Google/Chrome/Application/chrome.exe',headless:true});try{const p=await b.newPage({viewport:{width:1536,height:1024}}),sample=()=>p.evaluate(()=>new Promise(resolve=>{const a=[];let last=0;function f(t){if(last)a.push(t-last);last=t;if(a.length<45)requestAnimationFrame(f);else{a.sort((a,b)=>a-b);resolve({median:a[22],max:a.at(-1)})}}requestAnimationFrame(f)}));console.log('blank',await sample());await p.goto('http://127.0.0.1:5371/motion/relative-motion');await p.locator('.rvm-river').waitFor();console.log('paused',await sample());await p.getByRole('button',{name:'Play',exact:true}).click();console.log('running',await sample())}finally{await b.close()}
