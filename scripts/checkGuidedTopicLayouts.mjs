import {chromium} from 'playwright-core';const b=await chromium.launch({executablePath:'C:/Program Files/Google/Chrome/Application/chrome.exe',headless:true});const p=await b.newPage();await p.goto('http://127.0.0.1:5371/roadmap');for(const width of [1536,1024,390]){await p.setViewportSize({width,height:1024});for(const id of ['measurement','mechanics','waves','thermo','em','quantum','astronomy']){await p.getByRole('combobox',{name:'Interactive Workspace'}).selectOption(id);const bad=await p.locator('.gp-card,.gp-equation').evaluateAll(es=>es.filter(e=>e.scrollWidth>e.clientWidth+3||e.scrollHeight>e.clientHeight+3).map(e=>({c:e.className,w:e.clientWidth,sw:e.scrollWidth,h:e.clientHeight,sh:e.scrollHeight})));if(bad.length)console.log(width,id,bad)}}await b.close();
