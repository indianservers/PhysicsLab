import{chromium}from'playwright-core';import assert from'node:assert/strict';const b=await chromium.launch({executablePath:'C:/Program Files/Google/Chrome/Application/chrome.exe',headless:true});try{const p=await b.newPage({viewport:{width:390,height:844}});await p.goto('http://127.0.0.1:5371/motion/velocity-time');await p.locator('.vtm-graph').waitFor();for(const[name,v0,a,T,t]of[['minimum',-2,-3,8,8],['zero',0,0,.5,0],['reversal',2,-1,4,4]]){for(const[n,v]of[['Initial Velocity',v0],['Acceleration Segment',a],['Duration',T]]){const f=p.getByRole('spinbutton',{name:n+' value',exact:true});await f.fill(String(v));await f.press('Tab')}await p.getByRole('slider',{name:'Graph observation time'}).focus();await p.keyboard.press(t===0?'Home':'End');await p.evaluate(()=>{document.activeElement?.blur();window.scrollTo({top:0,behavior:'instant'})});const collision=await p.locator('.vtm-graph svg').evaluate(svg=>{const axis=svg.querySelector('text[transform]')?.getBoundingClientRect();return [...svg.querySelectorAll('g')].some(g=>{if(!g.querySelector('path')?.getAttribute('d')?.includes('H'))return false;const r=g.querySelector('text')?.getBoundingClientRect();return r&&axis&&r.left<axis.right&&r.right>axis.left&&r.top<axis.bottom&&r.bottom>axis.top})});assert.equal(collision,false);await p.screenshot({path:`artifacts/studio-rebuild/20-velocity-time/final-390-${name}.png`,fullPage:true})}console.log('Negative, zero and reversal graph labels clear; final mobile captures saved')}finally{await b.close()}
