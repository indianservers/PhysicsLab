import {chromium} from 'playwright-core';import fs from 'node:fs/promises';const b=await chromium.launch({executablePath:'C:/Program Files/Google/Chrome/Application/chrome.exe',headless:true});const errors=[];try{const p=await b.newPage({viewport:{width:1536,height:1024}});p.on('pageerror',e=>errors.push(e.message));await p.goto('http://127.0.0.1:5371/comparison');await p.locator('.cp-page').waitFor({timeout:15000});await p.getByRole('button',{name:'Pause',exact:true}).click();await p.waitForTimeout(300);await p.screenshot({path:'artifacts/studio-rebuild/05-compare-phenomena/first-pass.png',fullPage:true});console.log({errors,geometry:await p.evaluate(()=>({overflow:document.documentElement.scrollWidth-innerWidth,cards:[...document.querySelectorAll('.cp-card')].map(e=>({x:e.getBoundingClientRect().x,y:e.getBoundingClientRect().y,w:e.clientWidth,h:e.clientHeight})),clipped:[...document.querySelectorAll('.cp-summary>div,.cp-controls,.cp-intro')].filter(e=>e.scrollWidth>e.clientWidth+2).map(e=>e.className)}))});}finally{await b.close()}
