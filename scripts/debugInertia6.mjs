import{chromium}from'playwright-core';const b=await chromium.launch({executablePath:'C:/Program Files/Google/Chrome/Application/chrome.exe',headless:true});const p=await b.newPage();p.on('console',m=>console.log('console',m.type(),m.text()));p.on('pageerror',e=>console.log('err',e.message));await p.goto('http://127.0.0.1:5371/motion/first-law-inertia');await p.locator('.ils-stage').waitFor();const bs=p.getByRole('button',{name:'▷ Play',exact:true});console.log('count',await bs.count());await bs.click({force:true});console.log('after');await p.waitForTimeout(500);console.log(await p.getByRole('button').allTextContents());await b.close();
