import assert from 'node:assert/strict';import {chromium} from 'playwright-core';const b=await chromium.launch({executablePath:'C:/Program Files/Google/Chrome/Application/chrome.exe',headless:true});try{const p=await b.newPage({viewport:{width:1536,height:1024}});await p.goto('http://127.0.0.1:5371/mechanics/inclined-plane');await p.getByRole('button',{name:'Run motion',exact:true}).click();await p.getByRole('button',{name:'Run motion',exact:true}).waitFor({timeout:15000});const clipped=await p.locator('.ins-force-label').evaluateAll(nodes=>nodes.map(e=>({text:e.textContent,x:e.getBBox().x,right:e.getBBox().x+e.getBBox().width})).filter(t=>t.x<0||t.right>940));console.log({clipped});assert.equal(clipped.length,0);await p.screenshot({path:'artifacts/studio-rebuild/14-inclined-plane/desktop-stop.png',fullPage:true})}finally{await b.close()}
