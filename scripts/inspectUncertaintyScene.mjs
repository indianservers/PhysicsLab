import {chromium} from 'playwright-core';import fs from 'node:fs/promises';const b=await chromium.launch({executablePath:'C:/Program Files/Google/Chrome/Application/chrome.exe',headless:true});try{const p=await b.newPage();const errors=[];p.on('console',m=>{if(m.type()==='error')errors.push(m.text())});await p.goto('http://127.0.0.1:5371/measurement/uncertainty');await p.locator('.unc-page').waitFor();console.log({errors,paths:await p.locator('.unc-cylinder path').evaluateAll(nodes=>nodes.filter(e=>(e.getAttribute('d')||'').includes('Q306')).map(e=>({d:e.getAttribute('d'),stroke:getComputedStyle(e).stroke})))});}finally{await b.close()}
