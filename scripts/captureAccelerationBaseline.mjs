import {chromium}from'playwright-core';import fs from'node:fs/promises';const dir='artifacts/studio-rebuild/21-acceleration-time';await fs.mkdir(dir,{recursive:true});const b=await chromium.launch({executablePath:'C:/Program Files/Google/Chrome/Application/chrome.exe',headless:true});try{const p=await b.newPage({viewport:{width:1536,height:1024}});await p.goto('http://127.0.0.1:5371/experiments/uniform-motion');await p.getByRole('tab',{name:'Simulate',exact:true}).waitFor();await p.locator('.splash-loader').waitFor({state:'detached'});await p.screenshot({path:dir+'/before-default.png',fullPage:true});await p.getByRole('tab',{name:'Simulate',exact:true}).click();await p.getByRole('heading',{name:'Equal times. Equal displacements.',exact:true}).waitFor();await p.evaluate(()=>window.scrollTo({top:0,behavior:'instant'}));await p.screenshot({path:dir+'/before-simulate.png',fullPage:true});console.log((await p.locator('body').innerText()).slice(0,1800))}finally{await b.close()}
