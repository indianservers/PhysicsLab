import{chromium}from'playwright-core';import assert from'node:assert/strict';const b=await chromium.launch({executablePath:'C:/Program Files/Google/Chrome/Application/chrome.exe',headless:true});try{const p=await b.newPage({viewport:{width:1536,height:1024}}),errors=[];p.on('pageerror',e=>errors.push(e.message));await p.goto('http://127.0.0.1:5371/motion/first-law-inertia');await p.locator('.ils-stage').waitFor();await p.getByRole('button',{name:'▷ Play',exact:true}).click();await p.waitForTimeout(150);await p.getByRole('button',{name:'Ⅱ Pause',exact:true}).click();assert((await p.locator('.ils-readings').innerText()).includes('Momentum'));assert.equal(await p.evaluate(()=>document.documentElement.scrollWidth-innerWidth),0);console.log({errors});assert.deepEqual(errors,[])}finally{await b.close()}
