# Lesson 017 — Transformer Lab visual review

- The supplied GLB was deliberately not loaded because the current product direction is 2D-first. A generated transparent apparatus PNG provides the iron core, windings, AC source, leads and load; SVG and DOM overlays carry all changing scientific state.
- The application shell remains intact, so the result follows the mockup's three-column bench hierarchy inside the existing lesson page rather than duplicating its standalone navigation chrome.
- The mockup's oscilloscope, flux direction, meters, controls, power ledger and challenge are represented. The oscilloscope is a compact live SVG rather than a photoreal instrument so both traces remain legible on mobile.
- The physics distinguishes ideal induced secondary emf from loaded terminal voltage. This preserves `Vs/Vp = Ns/Np` exactly while exposing coupling and winding drop instead of silently folding losses into the ratio.
- Core and copper losses are displayed separately and close the power identity exactly. The model is explicitly documented as steady-state and educational; saturation harmonics, leakage inductance, switching transients and thermal runaway are outside scope.
- The generated source asset is `C:\Users\saisa\.codex\generated_images\01a064f9-4270-7ab3-88a1-9c5c3aa9a5a9\exec-1aadcd55-7ee0-4c41-a95a-62328b0046fd.png`; the app copy is `public/assets/experiments/transformer-lab/transformer-apparatus.png` (1672×941, 32-bit alpha).
- Browser checks covered step-up and step-down presets, play/pause/step, 2× speed, reduced-motion mode, the 920:48 mission solution, desktop/tablet/mobile layouts, and a fresh runtime console with no errors. The two React Router future-flag warnings are pre-existing framework notices.
