# Lesson 072 visual and interaction review

- Replaced the generic calculator/workbench with the mockup's three-column field experiment, oscilloscope, time-of-flight table, equation and measurement challenge.
- Used a generated true-alpha speaker/microphones/wall/tape apparatus. The canyon is a lightweight CSS stage; pulse fronts, distance markers and trace are live 2D layers. GLB remains deferred.
- Initial capture showed the app loading overlay, so stable post-load desktop/mobile captures replaced it; a separate mobile apparatus capture proves the core stage remains interactive below the shell.
- Verified short/click/tone-burst sources, 500–5000 Hz, 20–100% amplitude, −10–40 °C, 5–80 m, path visibility, timing markers, play/pause/step, 0.25–2× speed, reduced motion and reset.
- At 5 m the interface correctly warns that the echo overlaps; the temperature boundary check increased the displayed speed from 324.7 to 354.6 m/s.
- Mission passed at 30.0 m and 174.2 ms with 344.4 m/s. The formula, animation clock, trace and table all use the same round-trip state.
- Fresh-tab console: no errors. Mobile `scrollWidth === clientWidth` at the 390×844 test viewport. All project asset requests returned HTTP 200.
