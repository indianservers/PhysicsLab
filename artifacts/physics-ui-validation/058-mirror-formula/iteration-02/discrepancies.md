# Lesson 058 visual comparison — iteration 02

- Matched the reference's warm optical-bench palette, numbered left controls, large measured ray stage, right live readings and challenge, and lower signed u–v graph/equation card.
- Replaced the deferred GLB with a generated true-alpha 2D rail, object, screen and spherical mirror. CSS-clipped copies of that one PNG move with the authoritative object, real-image and mirror coordinates.
- Preserved the application shell rather than copying the mockup's standalone navigation.
- Corrected the reference's internally inconsistent displayed signs. This lab uses the New Cartesian convention throughout: concave f<0, object u<0, real image v<0, and m=-v/u.
- First browser comparison found the generated mirror offset from the calculated surface; the apparatus was split into aligned movable layers.
- Responsive comparison compacted the mobile stage. The u–v curve was rescaled to keep negative real-image distances visible and broken across the focal asymptote instead of drawing a false connecting line.
- Verified desktop 1440×900, tablet 820×1180 and mobile 390×844. The document has no horizontal overflow, the 2172 px alpha asset loads, and no broken images or alert states remain.
