# Iteration 01 discrepancies

- The original reference assumes freely rotated 3D apparatus; the user explicitly deferred 3D, so the implementation uses a generated transparent 2D bench and live SVG optics.
- The application shell remains visible around the lesson, unlike the standalone PhotonLab mockup.
- The first pass needed explicit tablet and mobile overflow measurements plus a critical-angle state capture.

