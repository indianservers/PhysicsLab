export type TempInput={a:number;b:number;particles:number};export const TEMP_DEFAULTS={a:600,b:200,particles:300};export function tempSettings(v:TempInput){return{a:Math.max(100,Math.min(1200,Number(v.a)||100)),b:Math.max(100,Math.min(1200,Number(v.b)||100)),particles:Math.max(50,Math.min(1000,Math.round(Number(v.particles)||50)))} }export function tempSolution(v:TempInput){const k=1.38e-23;return{ka:1.5*k*v.a,kb:1.5*k*v.b,delta:Math.abs(v.a-v.b),eq:Math.max(0,100-Math.abs(v.a-v.b)/12)}}
