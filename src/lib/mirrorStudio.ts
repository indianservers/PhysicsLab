export type MirrorInput={radius:number;object:number;type:'concave'|'convex'};export const MIRROR_DEFAULTS={radius:40,object:30,type:'concave' as const};export function mirrorSettings(v:MirrorInput){return{radius:Math.max(10,Math.min(100,Number(v.radius)||10)),object:Math.max(5,Math.min(80,Number(v.object)||5)),type:v.type}}export function mirrorSolution(v:MirrorInput){const f=(v.type==='concave'?1:-1)*v.radius/2;const di=1/(1/f-1/v.object);return{f,di,m:-di/v.object,real:di>0}}
