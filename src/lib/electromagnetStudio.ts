export type ElectromagnetInput={current:number;turns:number;gap:number};export const EM_DEFAULTS={current:2,turns:800,gap:5};export function emSettings(v:ElectromagnetInput){return{current:Math.max(0,Math.min(5,Number(v.current)||0)),turns:Math.max(100,Math.min(2000,Math.round(Number(v.turns)||100))),gap:Math.max(.5,Math.min(20,Number(v.gap)||.5))}}export function emSolution(v:ElectromagnetInput){const B=4*Math.PI*1e-7*2.08*v.turns*v.current/(v.gap/1000);const force=B*B*1.4e-5/(2*4*Math.PI*1e-7);return{B,force,power:2.025*v.current*v.current,sat:Math.min(100,B/1.235*100)}}
