export type PowerInput={mass:number;speed:number;torque:number;time:number};export const POWER_DEFAULTS={mass:5,speed:.5,torque:2,time:.84};export function powerSettings(v:PowerInput){return{mass:Math.max(1,Math.min(10,Number(v.mass)||1)),speed:Math.max(.1,Math.min(1,Number(v.speed)||.1)),torque:Math.max(.5,Math.min(4,Number(v.torque)||.5)),time:Math.max(.01,Math.min(10,Number(v.time)||.01))}}export function powerSolution(v:PowerInput){const work=v.mass*9.81*v.speed*v.time;return{work,power:work/v.time}}
