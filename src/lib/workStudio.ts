export type WorkInput={force:number;angle:number;distance:number};export const WORK_DEFAULTS={force:50,angle:30,distance:2};export function workSettings(v:WorkInput){return{force:Math.max(0,Math.min(200,Number(v.force)||0)),angle:Math.max(0,Math.min(90,Number(v.angle)||0)),distance:Math.max(0,Math.min(5,Number(v.distance)||0))}}export function workSolution(v:WorkInput){const r=v.angle*Math.PI/180;return{fx:v.force*Math.cos(r),fy:v.force*Math.sin(r),work:v.force*v.distance*Math.cos(r)}}
