export type ExoInput={radius:number;period:number;inclination:number};export const EXO_DEFAULTS={radius:1.6,period:30,inclination:89.2};export function exoSettings(v:ExoInput){return{radius:Math.max(.5,Math.min(4,Number(v.radius)||.5)),period:Math.max(1,Math.min(200,Number(v.period)||1)),inclination:Math.max(80,Math.min(90,Number(v.inclination)||80))}}export function exoSolution(v:ExoInput){const depth=Math.pow(v.radius/17.5,2);const mass=Math.pow(v.radius,3)*1.4;const temp=278/Math.sqrt(1.05);return{depth,mass,temp,habitable:v.period>20&&v.period<80}}
