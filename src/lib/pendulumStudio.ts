export type PendulumInput={length:number;angle:number;gravity:number};export const PENDULUM_DEFAULTS={length:.8,angle:15,gravity:9.81};export function pendulumSettings(v:PendulumInput){return{length:Math.max(.2,Math.min(2,Number(v.length)||.2)),angle:Math.max(1,Math.min(60,Number(v.angle)||1)),gravity:Math.max(1.62,Math.min(24.79,Number(v.gravity)||1.62))}}export function pendulumSolution(v:PendulumInput){const T=2*Math.PI*Math.sqrt(v.length/v.gravity);return{T,freq:1/T,energy:v.gravity*v.length*(1-Math.cos(v.angle*Math.PI/180))}}
