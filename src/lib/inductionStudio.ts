export type InductionInput={speed:number;turns:number;direction:number};export const INDUC_DEFAULTS={speed:.25,turns:200,direction:1};export function inductionSettings(v:InductionInput){return{speed:Math.max(0,Math.min(1,Number(v.speed)||0)),turns:Math.max(50,Math.min(800,Math.round(Number(v.turns)||50))),direction:v.direction<0?-1:1}}export function inductionSolution(v:InductionInput){const B=.306,A=.01,emf=v.turns*B*A*v.speed*v.direction;return{emf,flux:B*A}}
