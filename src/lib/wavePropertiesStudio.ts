export type WaveInput={amplitude:number;frequency:number;speed:number;time:number};export const WAVE_DEFAULTS={amplitude:2,frequency:1.5,speed:.6,time:0};export function waveSettings(v:WaveInput){return{amplitude:Math.max(.5,Math.min(5,Number(v.amplitude)||.5)),frequency:Math.max(.5,Math.min(5,Number(v.frequency)||.5)),speed:Math.max(.2,Math.min(1.5,Number(v.speed)||.2)),time:Math.max(0,Math.min(10,Number(v.time)||0))}}export function waveSolution(v:WaveInput){const lambda=v.speed/v.frequency,T=1/v.frequency,x=v.amplitude*Math.sin(2*Math.PI*(.6/lambda-v.frequency*v.time));return{lambda,T,x}}
