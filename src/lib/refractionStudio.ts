export type RefractionInput={n1:number;n2:number;angle:number};export const REFRACTION_DEFAULTS={n1:1.333,n2:1.49,angle:40};export function refractionSettings(v:RefractionInput){return{n1:Math.max(1,Math.min(2.5,Number(v.n1)||1)),n2:Math.max(1,Math.min(2.5,Number(v.n2)||1)),angle:Math.max(0,Math.min(90,Number(v.angle)||0))}}export function refractionSolution(v:RefractionInput){const r=Math.asin(Math.min(1,v.n1*Math.sin(v.angle*Math.PI/180)/v.n2))*180/Math.PI;return{refracted:r,speed1:3e8/v.n1,speed2:3e8/v.n2}}
