export type ResonanceInput={frequency:number;drive:number;b:number;mass:number;k:number};export const RESONANCE_DEFAULTS={frequency:2.2,drive:1,b:.5,mass:.25,k:40};export function resonanceSettings(v:ResonanceInput){return{frequency:Math.max(.1,Math.min(5,Number(v.frequency)||.1)),drive:Math.max(0,Math.min(5,Number(v.drive)||0)),b:Math.max(0,Math.min(2,Number(v.b)||0)),mass:.25,k:40}}export function resonanceSolution(v:ResonanceInput){const w=2*Math.PI*v.frequency,w0=Math.sqrt(v.k/v.mass),amp=v.drive/Math.sqrt(Math.pow(v.k-v.mass*w*w,2)+Math.pow(v.b*w,2));const phase=-Math.atan2(v.b*w,v.k-v.mass*w*w)*180/Math.PI;const fr=Math.sqrt(Math.max(0,v.k/v.mass-v.b*v.b/(2*v.mass*v.mass)))/(2*Math.PI);return{amp,phase,fr,Q:v.mass*w0/v.b}}
