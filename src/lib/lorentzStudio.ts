export type LorentzInput={sign:number;speed:number;B:number};export const LORENTZ_DEFAULTS={sign:1,speed:5e6,B:.2};export function lorentzSettings(v:LorentzInput){return{sign:v.sign<0?-1:1,speed:Math.max(1e6,Math.min(1e7,Number(v.speed)||1e6)),B:Math.max(.05,Math.min(.5,Number(v.B)||.05))}}export function lorentzSolution(v:LorentzInput){const q=1.602e-19,m=1.673e-27,F=q*v.speed*v.B,r=m*v.speed/(q*v.B),T=2*Math.PI*m/(q*v.B);return{F,r,T,freq:1/T}}
