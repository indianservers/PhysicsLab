export type BernoulliInput={speed:number;throat:number;rho:number};export const BERNOULLI_DEFAULTS={speed:1.24,throat:20,rho:998};export function bernoulliSettings(v:BernoulliInput){return{speed:Math.max(.2,Math.min(3,Number(v.speed)||.2)),throat:Math.max(10,Math.min(50,Number(v.throat)||10)),rho:Math.max(800,Math.min(1200,Number(v.rho)||800))}}export function bernoulliSolution(v:BernoulliInput){const v2=v.speed*Math.pow(40/v.throat,2);const p1=84.2;const p2=p1-.5*v.rho*(v2*v2-v.speed*v.speed)/1000;return{v1:v.speed,v2,p1,p2,p3:p1-.1*(p1-p2),drop:p1-p2,ratio:v2/v.speed,q:v.speed*Math.PI*.04*.04/4,reynolds:v.rho*v.speed*.04/0.001}}
