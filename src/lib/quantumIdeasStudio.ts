export type QuantumIdeasInput={sigma:number;barrier:number;measurement:number};export const QUANTUM_DEFAULTS={sigma:120,barrier:.35,measurement:0};export function quantumSettings(v:QuantumIdeasInput){return{sigma:Math.max(20,Math.min(500,Number(v.sigma)||20)),barrier:Math.max(0,Math.min(2,Number(v.barrier)||0)),measurement:Math.max(0,Math.min(1,Number(v.measurement)||0))}}export function quantumSolution(v:QuantumIdeasInput){const visibility=1-v.measurement*.9;const spread=v.sigma*1.72;return{visibility,spread,clicks:Math.round(752+v.measurement*120),mean:2.3*(1-v.measurement)}}
