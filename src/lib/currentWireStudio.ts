export type WireInput={current:number;radius:number;direction:number};export const WIRE_DEFAULTS={current:3,radius:5,direction:1};export function wireSettings(v:WireInput){return{current:Math.max(0,Math.min(10,Number(v.current)||0)),radius:Math.max(1,Math.min(20,Number(v.radius)||1)),direction:v.direction<0?-1:1}}export function wireSolution(v:WireInput){const B=4*Math.PI*1e-7*v.current/(2*Math.PI*v.radius/100);return{B}}
