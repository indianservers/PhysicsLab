export type EscapeInput={radius:number;mass:number;speed:number};export const ESCAPE_DEFAULTS={radius:6371,mass:5.97,speed:11.2};export function escapeSettings(v:EscapeInput){return{radius:Math.max(1000,Math.min(20000,Number(v.radius)||1000)),mass:Math.max(.1,Math.min(50,Number(v.mass)||.1)),speed:Math.max(0,Math.min(20,Number(v.speed)||0))}}export function escapeSolution(v:EscapeInput){const G=6.674e-11;const R=v.radius*1e3;const M=v.mass*1e24;const ve=Math.sqrt(2*G*M/R)/1e3;const residual=Math.sqrt(Math.max(0,v.speed*v.speed-ve*ve));return{ve,residual,status:v.speed>=ve?'UNBOUND':'BOUND',energy:(v.speed*v.speed-ve*ve)*5e5}}
