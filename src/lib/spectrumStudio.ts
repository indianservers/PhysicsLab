export type SpectrumInput={frequency:number;gain:number;time:number};export const SPECTRUM_DEFAULTS={frequency:440,gain:-6,time:0};export function spectrumSettings(v:SpectrumInput){return{frequency:Math.max(20,Math.min(2000,Number(v.frequency)||20)),gain:Math.max(-60,Math.min(0,Number(v.gain)||-60)),time:Math.max(0,Math.min(5,Number(v.time)||0))}}export function spectrumSolution(v:SpectrumInput){const wavelength=343/v.frequency,period=1/v.frequency,level=74.2+(v.gain+6);return{wavelength,period,level,harmonics:[1,2,3,4,5].map(n=>v.frequency*n)}}
