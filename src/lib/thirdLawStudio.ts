export type ThirdLawInput={massA:number;massB:number;compression:number};
export const THIRD_LAW_DEFAULTS:ThirdLawInput={massA:.5,massB:1,compression:5};
export function thirdLawSettings(v:ThirdLawInput):ThirdLawInput{return{massA:Math.round(Math.min(2,Math.max(.2,Number.isFinite(v.massA)?v.massA:.5))*100)/100,massB:Math.round(Math.min(2,Math.max(.2,Number.isFinite(v.massB)?v.massB:1))*100)/100,compression:Math.round(Math.min(10,Math.max(1,Number.isFinite(v.compression)?v.compression:5))*10)/10}}
export function thirdLawSolution(raw:ThirdLawInput,t=0){const input=thirdLawSettings(raw),peak=2.4*input.compression,duration=.015708,phase=Math.max(0,Math.min(1,t/duration)),force=phase<1?peak*Math.sin(Math.PI*phase):0,impulse=peak*duration*2/Math.PI,va=phase<1?-(peak*duration/(Math.PI*input.massA))*(1-Math.cos(Math.PI*phase)):-impulse/input.massA,vb=-va*input.massA/input.massB;return{input,peak,duration,force,forceA:-force,forceB:force,impulseA:-impulse,impulseB:impulse,velocityA:va,velocityB:vb,positionA:.5*va*t,positionB:.5*vb*t}}
