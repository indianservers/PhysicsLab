export type NuclearInput={z:number;n:number};export const NUCLEAR_DEFAULTS={z:26,n:30};export function nuclearSettings(v:NuclearInput){return{z:Math.max(1,Math.min(118,Math.round(Number(v.z)||1)),),n:Math.max(0,Math.min(200,Math.round(Number(v.n)||0)))}}export function nuclearSolution(v:NuclearInput){const A=v.z+v.n;const av=15.75,as=17.8,ac=.711,aa=23.7,ap=34;const pair=A%2===0?(v.z%2===0?1:-1)*ap/Math.sqrt(A):0;const be=Math.max(0,av*A-as*Math.pow(A,2/3)-ac*v.z*(v.z-1)/Math.pow(A,1/3)-aa*Math.pow(A-2*v.z,2)/A+pair);return{A,be,per:be/A,massDefect:be/931.5,stable:Math.abs(v.n-v.z*1.15)<8}}
