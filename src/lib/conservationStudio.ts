export type ConservationInput={height:number;drag:number;position:number};export const CONSERVATION_DEFAULTS={height:32,drag:.02,position:0};export function conservationSettings(v:ConservationInput){return{height:Math.max(5,Math.min(50,Number(v.height)||5)),drag:Math.max(0,Math.min(.1,Number(v.drag)||0)),position:Math.max(0,Math.min(250,Number(v.position)||0))}}export function conservationSolution(v:ConservationInput){const total=22000;const pe=total*(1-v.position/250)*.7;const loss=total*v.drag*(v.position/250);const ke=Math.max(0,total-pe-loss);return{total,pe,ke,loss,speed:Math.sqrt(2*ke/120)}}
