export type StellarInput={mass:number;metallicity:number;stage:string};export const STELLAR_DEFAULTS={mass:1,metallicity:.02,stage:'Main Sequence'};export function stellarSettings(v:StellarInput){return{mass:Math.max(.1,Math.min(50,Number(v.mass)||.1)),metallicity:Math.max(.0001,Math.min(.03,Number(v.metallicity)||.0001)),stage:v.stage}}export function stellarSolution(v:StellarInput){const lum=Math.pow(v.mass,3.5);const temp=5780*Math.pow(v.mass,.5);const life=1e10/Math.pow(v.mass,2.5);return{lum,temp,life,stage:v.stage}}
