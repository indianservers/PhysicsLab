export type MultiForceInput={tension:number;angle:number;wind:number};export const MULTI_DEFAULTS={tension:1250,angle:25,wind:300};export function multiSettings(v:MultiForceInput){return{tension:Math.max(0,Math.min(2000,Number(v.tension)||0)),angle:Math.max(0,Math.min(40,Number(v.angle)||0)),wind:Math.max(0,Math.min(800,Number(v.wind)||0))}}export function multiSolution(v:MultiForceInput){const m=120,g=9.81,mu=.12,a=v.angle*Math.PI/180,gravity=m*g*Math.sin(a),friction=mu*m*g*Math.cos(a),net=v.tension-gravity-friction-v.wind;return{gravity,friction,net,acceleration:net/m}}
