export type SuperpositionInput={a:number;b:number;fa:number;fb:number;phase:number;x:number};export const SUPER_DEFAULTS={a:.08,b:.08,fa:1.5,fb:1.5,phase:Math.PI/2,x:2.2};export function superSettings(v:SuperpositionInput){return{a:Math.max(0,Math.min(.2,Number(v.a)||0)),b:Math.max(0,Math.min(.2,Number(v.b)||0)),fa:Math.max(.5,Math.min(5,Number(v.fa)||.5)),fb:Math.max(.5,Math.min(5,Number(v.fb)||.5)),phase:Math.max(0,Math.min(2*Math.PI,Number(v.phase)||0)),x:Math.max(0,Math.min(3.14,Number(v.x)||0))}}export function superSolution(v:SuperpositionInput){const lambdaA=.6/v.fa,lambdaB=.6/v.fb;const ya=v.a*Math.sin(2*Math.PI*v.x/lambdaA),yb=v.b*Math.sin(2*Math.PI*v.x/lambdaB+v.phase),returnY=ya+yb;return{lambdaA,lambdaB,ya,yb,y:returnY}}
