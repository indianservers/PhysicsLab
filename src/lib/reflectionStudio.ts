export type ReflectionInput={incident:number;mirror:number;rays:number};export const REFLECTION_DEFAULTS={incident:40,mirror:0,rays:3};export function reflectionSettings(v:ReflectionInput){return{incident:Math.max(0,Math.min(80,Number(v.incident)||0)),mirror:Math.max(-45,Math.min(45,Number(v.mirror)||0)),rays:Math.round(Math.max(1,Math.min(5,Number(v.rays)||1)))}}export function reflectionSolution(v:ReflectionInput){return{reflected:v.incident,normal:v.incident-v.mirror}}
