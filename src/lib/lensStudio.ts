export type LensInput={focal:number;object:number;type:'convex'|'concave'};export const LENS_DEFAULTS={focal:10,object:20,type:'convex' as const};export function lensSettings(v:LensInput){return{focal:Math.max(5,Math.min(20,Number(v.focal)||5)),object:Math.max(5,Math.min(50,Number(v.object)||5)),type:v.type}}export function lensSolution(v:LensInput){const sign=v.type==='convex'?1:-1;const di=1/(1/(sign*v.focal)-1/v.object);return{di,m:-di/v.object,hi:2*(-di/v.object),real:di>0}}
