export type PotentialInput={z:number;layout:number};export const POTENTIAL_DEFAULTS={z:.5,layout:0};export function potentialSettings(v:PotentialInput){return{z:Math.max(0,Math.min(2,Number(v.z)||0)),layout:Math.max(0,Math.min(2,Number(v.layout)||0))}}export function potentialSolution(v:PotentialInput){const k=8.99e9;const charges=[[5,-1,0],[ -5,0,-1],[3,1.2,.8]] as const;let V=0;for(const[q,x,y]of charges){const r=Math.max(.08,Math.hypot(x,y,v.z));V+=k*q*1e-6/r}return{V,energy:V*1e-6,gradient:Math.abs(V)/Math.max(.1,v.z+1)}}
