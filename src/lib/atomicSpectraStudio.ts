export type SpectraInput={element:string;ni:number;nf:number};export const SPECTRA_DEFAULTS={element:'hydrogen',ni:4,nf:2};export function spectraSettings(v:SpectraInput){const ni=Math.max(2,Math.min(6,Math.round(Number(v.ni)||2)));const nf=Math.max(1,Math.min(5,Math.round(Number(v.nf)||1)));return{element:v.element,ni,nf:nf>=ni?Math.max(1,ni-1):nf}}export function spectraSolution(v:SpectraInput){const Z=v.element==='helium'?2:1;const inv=1.097373e7*Z*Z*(1/(v.nf*v.nf)-1/(v.ni*v.ni));const lambda=1/(Math.max(inv,1))*1e9;return{lambda,energy:1239.84198/lambda,series:v.nf===2?'Balmer':v.nf===1?'Lyman':'Paschen'}}
