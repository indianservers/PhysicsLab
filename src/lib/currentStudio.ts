export type CurrentInput={voltage:number;diameter:number;temperature:number};export const CURRENT_DEFAULTS={voltage:5,diameter:.5,temperature:20};export function currentSettings(v:CurrentInput){return{voltage:Math.max(0,Math.min(12,Number(v.voltage)||0)),diameter:Math.max(.1,Math.min(2,Number(v.diameter)||.1)),temperature:Math.max(0,Math.min(200,Number(v.temperature)||0))}}export function currentSolution(v:CurrentInput){const rho=1.68e-8*(1+.00393*(v.temperature-20)),L=186,A=Math.PI*(v.diameter*1e-3/2)**2,R=rho*L/A,I=v.voltage/R,J=I/A,vd=J/(8.5e28*1.6e-19);return{rho,R,I,J,vd}}

