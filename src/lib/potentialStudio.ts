export type PotentialInput={mass:number;height:number;gravity:number};export const POTENTIAL_DEFAULTS={mass:250,height:12,gravity:9.81};export function potentialSettings(v:PotentialInput){return{mass:Math.max(50,Math.min(1000,Number(v.mass)||50)),height:Math.max(0,Math.min(30,Number(v.height)||0)),gravity:Math.max(1.62,Math.min(24.79,Number(v.gravity)||1.62))}}export function potentialSolution(v:PotentialInput){return{energy:v.mass*v.height*v.gravity}}
