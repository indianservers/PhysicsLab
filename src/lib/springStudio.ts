export type SpringInput={k:number;m:number;amplitude:number;time:number};export const SPRING_DEFAULTS={k:50,m:.2,amplitude:.15,time:0};export function springSettings(v:SpringInput){return{k:Math.max(10,Math.min(200,Number(v.k)||10)),m:Math.max(.05,Math.min(1,Number(v.m)||.05)),amplitude:Math.max(.01,Math.min(.3,Number(v.amplitude)||.01)),time:Math.max(0,Math.min(10,Number(v.time)||0))}}export function springSolution(v:SpringInput){const omega=Math.sqrt(v.k/v.m),x=v.amplitude*Math.cos(omega*v.time),vel=-v.amplitude*omega*Math.sin(omega*v.time),acc=-omega*omega*x;return{omega,period:2*Math.PI/omega,x,vel,acc,energy:.5*v.k*v.amplitude*v.amplitude}}
