export type CircuitInput={voltage:number;r1:number;r2:number;closed:boolean};export const CIRCUIT_DEFAULTS={voltage:6,r1:220,r2:470,closed:true};export function circuitSettings(v:CircuitInput){return{voltage:Math.max(0,Math.min(12,Number(v.voltage)||0)),r1:[100,220,470,1000].includes(Number(v.r1))?Number(v.r1):220,r2:[100,220,470,1000].includes(Number(v.r2))?Number(v.r2):470,closed:Boolean(v.closed)}}export function circuitSolution(v:CircuitInput){const i1=v.closed?v.voltage/v.r1:0,i2=v.closed?v.voltage/v.r2:0;return{i1,i2,total:i1+i2,power:v.voltage*(i1+i2)}}
