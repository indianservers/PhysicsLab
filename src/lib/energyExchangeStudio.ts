export type EnergyExchangeInput={amplitude:number;mass:number;k:number;time:number};export const ENERGY_DEFAULTS={amplitude:.2,mass:1,k:50,time:0};export function energySettings(v:EnergyExchangeInput){return{amplitude:Math.max(.05,Math.min(.3,Number(v.amplitude)||.05)),mass:Math.max(.1,Math.min(2,Number(v.mass)||.1)),k:Math.max(10,Math.min(200,Number(v.k)||10)),time:Math.max(0,Math.min(10,Number(v.time)||0))}}export function energySolution(v:EnergyExchangeInput){const w=Math.sqrt(v.k/v.mass),x=v.amplitude*Math.cos(w*v.time),vel=-v.amplitude*w*Math.sin(w*v.time),ke=.5*v.mass*vel*vel,pe=.5*v.k*x*x;return{w,T:2*Math.PI/w,x,vel,ke,pe,total:ke+pe,phase:(w*v.time)%(2*Math.PI)}}
