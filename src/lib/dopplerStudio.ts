export type DopplerInput={source:number;observer:number;frequency:number};export const DOPPLER_DEFAULTS={source:30,observer:0,frequency:800};export function dopplerSettings(v:DopplerInput){return{source:Math.max(-50,Math.min(50,Number(v.source)||0)),observer:Math.max(-50,Math.min(50,Number(v.observer)||0)),frequency:Math.max(100,Math.min(2000,Number(v.frequency)||100))}}export function dopplerSolution(v:DopplerInput){const c=343;const app=v.frequency*(c+v.observer)/(c-v.source);const rec=v.frequency*(c+v.observer)/(c+v.source);return{app,rec,la:c/app,lr:c/rec}}
