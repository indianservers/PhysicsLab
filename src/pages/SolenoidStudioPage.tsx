import{useState}from'react';import{Link}from'react-router-dom';import{MasteryChallengeDialog}from'../components/MasteryChallengeDialog';import{SOL_DEFAULTS,solenoidSettings,solenoidSolution,type SolenoidInput}from'../lib/solenoidStudio';import'../friction-studio.css';function C({label,value,min,max,unit,onChange}:{label:string;value:number;min:number;max:number;unit:string;onChange:(v:number)=>void}){return <label className="frs-control"><span>{label}<input aria-label={label+' value'} type="number" value={value.toFixed(1)} onChange={e=>onChange(Number(e.target.value))}/><em>{unit}</em></span><input aria-label={label} type="range" min={min} max={max} step=".1" value={value} onChange={e=>onChange(Number(e.target.value))}/><small><i>{min}</i><i>{max}</i></small></label>}export default function SolenoidStudioPage(){const[input,setInput]=useState(SOL_DEFAULTS),[panel,setPanel]=useState<string|null>(null);const s=solenoidSolution(input);const update=(p:Partial<SolenoidInput>)=>setInput(solenoidSettings({...input,...p}));return <div className="frs-page tension-page" data-ui-theme="dark"><aside className="frs-side"><Link className="frs-brand" to="/concept-studio">◉ MAGNETISM</Link><nav><Link to="/concept-studio">⌂ Home</Link><Link to="/magnetism/current-wire">Current Wire</Link><Link to="/magnetism/solenoids">Solenoids</Link><Link to="/magnetism/induction">Induction</Link><Link to="/magnetism/electromagnets">Electromagnets</Link></nav></aside><main id="content" className="frs-main"><header className="frs-head"><h1>MAGNETISM　›　<span>SOLENOID</span></h1><p>Explore how current, turns, and core material determine the magnetic field inside a solenoid.</p></header><div className="frs-modes">{['Observe','Predict','Experiment','Explain'].map(m=><button key={m} onClick={()=>m!=='Observe'&&setPanel(m)}>{m}</button>)}</div><div className="frs-layout"><section className="frs-stage"><svg viewBox="0 0 900 380"><rect width="900" height="380" fill="#142b3a"/><rect x="260" y="130" width="380" height="120" rx="50" fill="#172331" stroke="#b46b43" strokeWidth="10"/>{Array.from({length:18},(_,i)=><ellipse key={i} cx={280+i*20} cy="190" rx="6" ry="70" fill="none" stroke="#c47a4e" strokeWidth="4"/>)}{Array.from({length:7},(_,i)=><path key={i} d={`M ${280+i*35} 140 Q450 ${50+i*8} ${620-i*35} 140`} fill="none" stroke="#4db5ff" strokeWidth="3"/>)}<text x="350" y="110" fill="white">B = {(s.B*1000).toFixed(2)} mT</text><text x="315" y="295" fill="#9ed5ff">N={input.turns}　I={input.current.toFixed(2)} A</text></svg></section><aside className="frs-right"><section className="frs-panel"><h2>CONTROLS</h2><C label="Number of Turns N" value={input.turns} min={100} max={2000} unit="turns" onChange={turns=>update({turns})}/><C label="Current I" value={input.current} min={0} max={5} unit="A" onChange={current=>update({current})}/><label>Core Material <select aria-label="Core Material" value={input.mu} onChange={e=>update({mu:Number(e.target.value)})}><option value="1">Air (μᵣ=1)</option><option value="200">Iron (μᵣ=200)</option><option value="800">Ferrite (μᵣ=800)</option><option value="1000">Steel (μᵣ=1000)</option></select></label></section><section className="frs-panel frs-readings"><h2>LIVE READINGS</h2><p>B (at probe)<b>{(s.B*1000).toFixed(2)} mT</b></p><p>Field direction<b>→ (to the right)</b></p><p>Current I<b>{input.current.toFixed(2)} A</b></p><p>Turns N<b>{input.turns}</b></p><p>Core material μᵣ<b>{input.mu}</b></p></section></aside><section className="frs-equation frs-panel"><h2>KEY EQUATION</h2><b>B = μnI = μ(N/L)I</b><p>Inside a long solenoid, field strength is proportional to turns, permeability, and current.</p></section></div><div className="frs-actions"><button onClick={()=>setInput(SOL_DEFAULTS)}>⟳ Reset</button><button className="frs-next" onClick={()=>setPanel('Challenge')}>Next Challenge　→</button></div><footer>MAGNETISM　|　SOLENOID</footer></main>{panel&&<MasteryChallengeDialog title={panel} close={()=>setPanel(null)}><p>Maximize the central field without exceeding 2.0 A.</p></MasteryChallengeDialog>}</div>}
