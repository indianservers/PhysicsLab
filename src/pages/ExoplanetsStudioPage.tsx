import{useState}from'react';import{Link}from'react-router-dom';import{MasteryChallengeDialog}from'../components/MasteryChallengeDialog';import{EXO_DEFAULTS,exoSettings,exoSolution,type ExoInput}from'../lib/exoplanetsStudio';import'../friction-studio.css';
function C({label,value,min,max,onChange,unit}:{label:string;value:number;min:number;max:number;onChange:(v:number)=>void;unit:string}){return <label className="frs-control"><span>{label}<input aria-label={label+' value'} type="number" value={value} onChange={e=>onChange(Number(e.target.value))}/><em>{unit}</em></span><input aria-label={label} type="range" min={min} max={max} step=".1" value={value} onChange={e=>onChange(Number(e.target.value))}/><small><i>{min}</i><i>{max}</i></small></label>}
export default function ExoplanetsStudioPage(){const[input,setInput]=useState(EXO_DEFAULTS),[panel,setPanel]=useState<string|null>(null);const s=exoSolution(input);const update=(p:Partial<ExoInput>)=>setInput(exoSettings({...input,...p}));return <div className="frs-page tension-page" data-ui-theme="dark"><aside className="frs-side"><Link className="frs-brand" to="/concept-studio">◌ ASTROPHYSICS</Link><nav><Link to="/concept-studio">⌂ Home</Link><Link to="/astrophysics/cosmology">Cosmology</Link><Link to="/astrophysics/exoplanets">Exoplanets</Link></nav></aside><main id="content" className="frs-main"><header className="frs-head"><h1>ASTROPHYSICS　›　<span>EXOPLANETS</span></h1><p>Discover distant worlds with transit photometry and radial velocity.</p></header><div className="frs-modes">{['Observe','Predict','Experiment','Explain'].map(m=><button key={m} onClick={()=>m!=='Observe'&&setPanel(m)}>{m}</button>)}</div><div className="frs-layout"><section className="frs-stage"><svg viewBox="0 0 900 380"><rect width="900" height="380" fill="#07152b"/><circle cx="620" cy="190" r="110" fill="#ffdb89"/><circle cx="620" cy="190" r="18" fill="#142b4c"/><path d="M80 190 H500" stroke="#70bfff" strokeWidth="4"/><text x="250" y="340" fill="#9ed5ff">δ={(s.depth*100).toFixed(2)}% · M={s.mass.toFixed(1)} M⊕ · T={s.temp.toFixed(0)} K</text></svg></section><aside className="frs-right"><section className="frs-panel"><h2>PLANET PARAMETERS</h2><C label="Planet Radius" value={input.radius} min={.5} max={4} onChange={radius=>update({radius})} unit="R⊕"/><C label="Orbital Period" value={input.period} min={1} max={200} onChange={period=>update({period})} unit="days"/><C label="Inclination" value={input.inclination} min={80} max={90} onChange={inclination=>update({inclination})} unit="°"/></section><section className="frs-panel frs-readings"><h2>KEY READINGS</h2><p>Transit Depth δ<b>{s.depth.toFixed(4)} ({(s.depth*100).toFixed(2)}%)</b></p><p>Estimated Mass<b>{s.mass.toFixed(1)} M⊕</b></p><p>Equilibrium Temperature<b>{s.temp.toFixed(0)} K</b></p><p>Habitable-Zone Status<b>{s.habitable?'Within HZ':'Outside HZ'}</b></p></section><section className="frs-panel frs-equation"><h2>TRANSIT DEPTH</h2><b>δ ≈ (Rₚ/R★)²</b><p>A planet blocks a small fraction of starlight during transit.</p></section></aside></div><div className="frs-actions"><button onClick={()=>setInput(EXO_DEFAULTS)}>⟳ Reset</button><button className="frs-next" onClick={()=>setPanel('Challenge')}>Next Challenge　→</button></div><footer>ASTROPHYSICS　|　EXOPLANETS</footer></main>{panel&&<MasteryChallengeDialog title={panel} close={()=>setPanel(null)}><p>Adjust planet radius and period to assess detectability and habitability.</p></MasteryChallengeDialog>}</div>}
