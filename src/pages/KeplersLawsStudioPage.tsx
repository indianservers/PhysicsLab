import{useState}from'react';import{Link}from'react-router-dom';import{MasteryChallengeDialog}from'../components/MasteryChallengeDialog';import{KEPLER_DEFAULTS,keplerSettings,keplerSolution,type KeplerInput}from'../lib/keplerStudio';import'../friction-studio.css';function C({label,value,min,max,unit,onChange}:{label:string;value:number;min:number;max:number;unit:string;onChange:(v:number)=>void}){return <label className="frs-control"><span>{label}<input aria-label={label+' value'} type="number" value={value.toFixed(2)} onChange={e=>onChange(Number(e.target.value))}/><em>{unit}</em></span><input aria-label={label} type="range" min={min} max={max} step={(max-min)/100} value={value} onChange={e=>onChange(Number(e.target.value))}/><small><i>{min}</i><i>{max}</i></small></label>}export default function KeplersLawsStudioPage(){const[input,setInput]=useState(KEPLER_DEFAULTS),[panel,setPanel]=useState<string|null>(null);const s=keplerSolution(input);const update=(p:Partial<KeplerInput>)=>setInput(keplerSettings({...input,...p}));return <div className="frs-page tension-page" data-ui-theme="dark"><aside className="frs-side"><Link className="frs-brand" to="/concept-studio">⚛ PHYSICS STUDIO</Link><nav><Link to="/concept-studio">⌂ Home</Link><Link to="/motion/orbits">Orbits</Link><Link to="/motion/escape-velocity">Escape Velocity</Link><Link to="/motion/keplers-laws">Kepler’s Laws</Link></nav></aside><main id="content" className="frs-main"><header className="frs-head"><h1>GRAVITATION　›　<span>KEPLER’S LAWS</span></h1><p>Explore planetary motion, equal-area sectors, and the relation T² ∝ a³.</p></header><div className="frs-modes">{['Observe','Predict','Experiment','Explain'].map(m=><button key={m} onClick={()=>m!=='Observe'&&setPanel(m)}>{m}</button>)}</div><div className="frs-layout"><section className="frs-stage"><img src="/assets/friction-studio/field-earth.png" alt="Planet orbiting a star"/><svg viewBox="0 0 900 380"><ellipse cx="450" cy="200" rx="340" ry="125" fill="none" stroke="#76bfff" strokeWidth="3"/><circle cx="450" cy="200" r="36" fill="#ffb52e"/><circle cx="790" cy="200" r="8" fill="#77d7ff"/><path d="M450 200L790 200L680 115Z" fill="#7b57ff" opacity=".25" stroke="#b89cff"/><text x="530" y="90" fill="#fff">Equal areas in equal times</text></svg></section><aside className="frs-right"><section className="frs-panel"><h2>ORBITAL PARAMETERS</h2><C label="Semi-major Axis a" value={input.a} min={.5} max={3} unit="AU" onChange={a=>update({a})}/><C label="Eccentricity e" value={input.e} min={0} max={.9} unit="" onChange={e=>update({e})}/><C label="Time Step Δt" value={input.dt} min={.1} max={50} unit="days" onChange={dt=>update({dt})}/></section><section className="frs-panel frs-readings"><h2>LIVE READINGS</h2><p>Orbital Period T<b>{s.T.toFixed(1)} days</b></p><p>Radius r<b>{s.r.toFixed(3)} AU</b></p><p>Orbital Speed v<b>{s.speed.toFixed(1)} km/s</b></p><p>Swept Area A<b>{s.area.toExponential(3)} km²</b></p></section></aside><section className="frs-equation frs-panel"><h2>KEPLER’S THIRD LAW</h2><b>T² / a³ = constant ≈ 365.25² days²/AU³</b><p>Orbital period grows as the three-halves power of semi-major axis.</p></section></div><div className="frs-actions"><button onClick={()=>setInput(KEPLER_DEFAULTS)}>⟳ Reset</button><button className="frs-next" onClick={()=>setPanel('Challenge')}>Next Challenge　→</button></div><footer>GRAVITATION　|　KEPLER’S LAWS</footer></main>{panel&&<MasteryChallengeDialog title={panel} close={()=>setPanel(null)}><p>Compare the period of two orbits with different semi-major axes.</p></MasteryChallengeDialog>}</div>}
