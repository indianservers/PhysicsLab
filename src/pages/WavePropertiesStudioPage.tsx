import{useState}from'react';import{Link}from'react-router-dom';import{MasteryChallengeDialog}from'../components/MasteryChallengeDialog';import{WAVE_DEFAULTS,waveSettings,waveSolution,type WaveInput}from'../lib/wavePropertiesStudio';import'../friction-studio.css';function C({label,value,min,max,unit,onChange}:{label:string;value:number;min:number;max:number;unit:string;onChange:(v:number)=>void}){return <label className="frs-control"><span>{label}<input aria-label={label+' value'} type="number" value={value.toFixed(2)} onChange={e=>onChange(Number(e.target.value))}/><em>{unit}</em></span><input aria-label={label} type="range" min={min} max={max} step={(max-min)/100} value={value} onChange={e=>onChange(Number(e.target.value))}/><small><i>{min}</i><i>{max}</i></small></label>}export default function WavePropertiesStudioPage(){const[input,setInput]=useState(WAVE_DEFAULTS),[panel,setPanel]=useState<string|null>(null);const s=waveSolution(input);const update=(p:Partial<WaveInput>)=>setInput(waveSettings({...input,...p}));return <div className="frs-page tension-page" data-ui-theme="dark"><aside className="frs-side"><Link className="frs-brand" to="/concept-studio">〰 WAVES & SOUND</Link><nav><Link to="/concept-studio">⌂ Home</Link><Link to="/motion/wave-properties">Wave Properties</Link><Link to="/motion/superposition">Superposition</Link><Link to="/motion/interference">Interference</Link><Link to="/motion/standing-waves">Standing Waves</Link><Link to="/motion/sound-spectrum">Sound Spectrum</Link><Link to="/motion/doppler-effect">Doppler Effect</Link></nav></aside><main id="content" className="frs-main"><header className="frs-head"><h1>WAVES & SOUND　›　<span>WAVE PROPERTIES</span></h1><p>Explore how amplitude, frequency, and speed shape a traveling wave.</p></header><div className="frs-modes">{['Observe','Predict','Experiment','Explain'].map(m=><button key={m} onClick={()=>m!=='Observe'&&setPanel(m)}>{m}</button>)}</div><div className="frs-layout"><section className="frs-stage"><img src="/assets/friction-studio/wave-machine.png" alt="Wave machine with transverse wave"/><svg viewBox="0 0 900 380"><path d="M60 210 C140 70 220 350 300 210 S460 70 540 210 S700 350 840 210" fill="none" stroke="#21d5ff" strokeWidth="4"/><text x="560" y="95" fill="#8de8ff">Wave traveling right →</text></svg></section><aside className="frs-right"><section className="frs-panel"><h2>CONTROLS</h2><C label="Amplitude A" value={input.amplitude} min={.5} max={5} unit="cm" onChange={amplitude=>update({amplitude})}/><C label="Frequency f" value={input.frequency} min={.5} max={5} unit="Hz" onChange={frequency=>update({frequency})}/><C label="Wave Speed v" value={input.speed} min={.2} max={1.5} unit="m/s" onChange={speed=>update({speed})}/><C label="Time t" value={input.time} min={0} max={10} unit="s" onChange={time=>update({time})}/></section><section className="frs-panel frs-readings"><h2>LIVE READINGS</h2><p>Wavelength λ<b>{s.lambda.toFixed(2)} m</b></p><p>Period T<b>{s.T.toFixed(3)} s</b></p><p>Frequency f<b>{input.frequency.toFixed(2)} Hz</b></p><p>Wave Speed v<b>{input.speed.toFixed(2)} m/s</b></p><p>Displacement y<b>{s.x.toFixed(2)} cm</b></p></section></aside><section className="frs-equation frs-panel"><h2>WAVE EQUATION</h2><b>v = f λ</b><p>Wave speed equals frequency times wavelength.</p></section></div><div className="frs-actions"><button onClick={()=>setInput(WAVE_DEFAULTS)}>⟳ Reset</button><button className="frs-next" onClick={()=>setPanel('Challenge')}>Next Challenge　→</button></div><footer>WAVES & SOUND　|　WAVE PROPERTIES</footer></main>{panel&&<MasteryChallengeDialog title={panel} close={()=>setPanel(null)}><p>What happens to wavelength when frequency increases at fixed speed?</p></MasteryChallengeDialog>}</div>}
