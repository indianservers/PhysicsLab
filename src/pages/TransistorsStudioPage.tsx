import{useState}from'react';import{Link}from'react-router-dom';import{MasteryChallengeDialog}from'../components/MasteryChallengeDialog';import{TRANS_DEFAULTS,transSettings,transSolution,type TransistorInput}from'../lib/transistorStudio';import'../friction-studio.css';function C({label,value,min,max,unit,onChange}:{label:string;value:number;min:number;max:number;unit:string;onChange:(v:number)=>void}){return <label className="frs-control"><span>{label}<input aria-label={label+' value'} type="number" value={value.toFixed(1)} onChange={e=>onChange(Number(e.target.value))}/><em>{unit}</em></span><input aria-label={label} type="range" min={min} max={max} step=".1" value={value} onChange={e=>onChange(Number(e.target.value))}/><small><i>{min}</i><i>{max}</i></small></label>}export default function TransistorsStudioPage(){const[input,setInput]=useState(TRANS_DEFAULTS),[panel,setPanel]=useState<string|null>(null);const s=transSolution(input);const update=(p:Partial<TransistorInput>)=>setInput(transSettings({...input,...p}));return <div className="frs-page tension-page" data-ui-theme="dark"><aside className="frs-side"><Link className="frs-brand" to="/concept-studio">▣ ELECTRONICS</Link><nav><Link to="/concept-studio">⌂ Home</Link><Link to="/electronics/semiconductors">Semiconductors</Link><Link to="/electronics/diodes">Diodes</Link><Link to="/electronics/transistors">Transistors</Link><Link to="/electronics/sensors">Sensors</Link></nav></aside><main id="content" className="frs-main"><header className="frs-head"><h1>ELECTRONICS　›　<span>BJT TRANSISTOR CHARACTERISTICS</span></h1><p>Explore how base current controls collector current in an NPN transistor.</p></header><div className="frs-modes">{['Observe','Predict','Experiment','Explain'].map(m=><button key={m} onClick={()=>m!=='Observe'&&setPanel(m)}>{m}</button>)}</div><div className="frs-layout"><section className="frs-stage"><svg viewBox="0 0 900 380"><rect width="900" height="380" fill="#162d40"/><path d="M180 90 H720 M180 290 H720 M220 90 V290" stroke="#4caeff" strokeWidth="7" fill="none"/><rect x="420" y="145" width="70" height="90" fill="#343d49"/><line x1="455" y1="90" x2="455" y2="145" stroke="#ff555f" strokeWidth="6"/><line x1="455" y1="235" x2="455" y2="290" stroke="#4caeff" strokeWidth="6"/><line x1="380" y1="190" x2="420" y2="190" stroke="#4caeff" strokeWidth="6"/><text x="405" y="130" fill="white">NPN</text><text x="545" y="120" fill="#ff6570">VCE={input.vce.toFixed(1)} V</text><text x="545" y="150" fill="#9ed5ff">IC={s.ic.toFixed(2)} mA</text></svg></section><aside className="frs-right"><section className="frs-panel"><h2>CONTROLS</h2><C label="Base Current I_B" value={input.ib} min={0} max={100} unit="µA" onChange={ib=>update({ib})}/><C label="Collector Voltage V_CE" value={input.vce} min={0} max={10} unit="V" onChange={vce=>update({vce})}/><C label="Load Resistor R_C" value={input.rc} min={100} max={10000} unit="Ω" onChange={rc=>update({rc})}/></section><section className="frs-panel frs-readings"><h2>LIVE READINGS</h2><p>Collector current I<sub>C</sub><b>{s.ic.toFixed(2)} mA</b></p><p>Current gain β<b>{s.beta.toFixed(1)}</b></p><p>Operating region<b>{s.region}</b></p><p>Collector power<b>{s.power.toFixed(3)} W</b></p></section></aside><section className="frs-equation frs-panel"><h2>KEY RELATION</h2><b>I<sub>C</sub> = βI<sub>B</sub></b><p>In the active region, collector current follows base current until limited by saturation.</p></section></div><div className="frs-actions"><button onClick={()=>setInput(TRANS_DEFAULTS)}>⟳ Reset</button><button className="frs-next" onClick={()=>setPanel('Challenge')}>Next Challenge　→</button></div><footer>ELECTRONICS　|　TRANSISTORS</footer></main>{panel&&<MasteryChallengeDialog title={panel} close={()=>setPanel(null)}><p>Map the output characteristics by changing base current.</p></MasteryChallengeDialog>}</div>}
