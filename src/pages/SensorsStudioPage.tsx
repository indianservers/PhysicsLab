import{useState}from'react';import{Link}from'react-router-dom';import{MasteryChallengeDialog}from'../components/MasteryChallengeDialog';import{SENSOR_DEFAULTS,sensorSettings,sensorSolution,type SensorInput}from'../lib/sensorStudio';import'../friction-studio.css';function C({label,value,min,max,unit,onChange}:{label:string;value:number;min:number;max:number;unit:string;onChange:(v:number)=>void}){return <label className="frs-control"><span>{label}<input aria-label={label+' value'} type="number" value={value.toFixed(1)} onChange={e=>onChange(Number(e.target.value))}/><em>{unit}</em></span><input aria-label={label} type="range" min={min} max={max} step=".1" value={value} onChange={e=>onChange(Number(e.target.value))}/><small><i>{min}</i><i>{max}</i></small></label>}export default function SensorsStudioPage(){const[input,setInput]=useState(SENSOR_DEFAULTS),[panel,setPanel]=useState<string|null>(null);const s=sensorSolution(input);const update=(p:Partial<SensorInput>)=>setInput(sensorSettings({...input,...p}));return <div className="frs-page tension-page" data-ui-theme="dark"><aside className="frs-side"><Link className="frs-brand" to="/concept-studio">▣ ELECTRONICS</Link><nav><Link to="/concept-studio">⌂ Home</Link><Link to="/electronics/logic-gates">Logic Gates</Link><Link to="/electronics/sensors">Sensors</Link><Link to="/electronics/semiconductors">Semiconductors</Link></nav></aside><main id="content" className="frs-main"><header className="frs-head"><h1>ELECTRONICS　›　<span>SENSORS</span></h1><p>Investigate how environmental changes affect sensor output and build a calibration curve.</p></header><div className="frs-modes">{['Observe','Predict','Experiment','Explain'].map(m=><button key={m} onClick={()=>m!=='Observe'&&setPanel(m)}>{m}</button>)}</div><div className="frs-layout"><section className="frs-stage"><svg viewBox="0 0 900 400"><rect width="900" height="400" fill="#1d2f3b"/><circle cx="250" cy="130" r="80" fill="#222"/><path d="M180 170 Q250 110 320 170" stroke="#fff" strokeWidth="16" fill="none"/><rect x="440" y="120" width="140" height="180" rx="18" fill="#8798a5"/><circle cx="510" cy="200" r="45" fill="#34baf2" opacity=".7"/><text x="390" y="350" fill="white">{input.type>0?'LDR':'Thermistor'} · stimulus {input.stimulus.toFixed(0)}%</text><text x="595" y="200" fill="#9ed5ff">R = {(s.resistance/1000).toFixed(2)} kΩ</text></svg></section><aside className="frs-right"><section className="frs-panel"><h2>CONTROLS</h2><label>Sensor Type <select aria-label="Sensor Type" value={input.type} onChange={e=>update({type:Number(e.target.value)})}><option value="1">LDR (Light Dependent Resistor)</option><option value="-1">Thermistor</option></select></label><C label="Stimulus Level" value={input.stimulus} min={0} max={100} unit="%" onChange={stimulus=>update({stimulus})}/><C label="Sampling Rate" value={input.rate} min={1} max={50} unit="Hz" onChange={rate=>update({rate})}/></section><section className="frs-panel frs-readings"><h2>LIVE READINGS</h2><p>Resistance<b>{(s.resistance/1000).toFixed(2)} kΩ</b></p><p>Output voltage<b>{s.voltage.toFixed(2)} V</b></p><p>Temperature<b>{s.temp.toFixed(1)} °C</b></p></section></aside><section className="frs-equation frs-panel"><h2>CALIBRATION MODEL</h2><b>R = a I<sup>b</sup></b><p>Sensor resistance responds predictably to the applied environmental stimulus.</p></section></div><div className="frs-actions"><button onClick={()=>setInput(SENSOR_DEFAULTS)}>⟳ Reset</button><button className="frs-next" onClick={()=>setPanel('Challenge')}>Next Challenge　→</button></div><footer>ELECTRONICS　|　SENSORS</footer></main>{panel&&<MasteryChallengeDialog title={panel} close={()=>setPanel(null)}><p>Change the stimulus and compare sensor resistance.</p></MasteryChallengeDialog>}</div>}
