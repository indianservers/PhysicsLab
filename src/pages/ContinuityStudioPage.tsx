import{useState}from'react';import{Link}from'react-router-dom';import{MasteryChallengeDialog}from'../components/MasteryChallengeDialog';import{CONTINUITY_DEFAULTS,continuitySettings,continuitySolution,type ContinuityInput}from'../lib/continuityStudio';import'../friction-studio.css';
function C({label,value,min,max,onChange,unit}:{label:string;value:number;min:number;max:number;onChange:(v:number)=>void;unit:string}){return <label className="frs-control"><span>{label}<input aria-label={label+' value'} type="number" value={value} onChange={e=>onChange(Number(e.target.value))}/><em>{unit}</em></span><input aria-label={label} type="range" min={min} max={max} step=".00001" value={value} onChange={e=>onChange(Number(e.target.value))}/><small><i>{min}</i><i>{max}</i></small></label>}
export default function ContinuityStudioPage(){const[input,setInput]=useState(CONTINUITY_DEFAULTS),[panel,setPanel]=useState<string|null>(null);const s=continuitySolution(input);const update=(p:Partial<ContinuityInput>)=>setInput(continuitySettings({...input,...p}));return <div className="frs-page tension-page" data-ui-theme="dark"><aside className="frs-side"><Link className="frs-brand" to="/concept-studio">◈ FLUID MECHANICS</Link><nav><Link to="/concept-studio">⌂ Home</Link><Link to="/fluid-mechanics/buoyancy">Buoyancy</Link><Link to="/fluid-mechanics/continuity">Continuity</Link></nav></aside><main id="content" className="frs-main"><header className="frs-head"><h1>FLUID MECHANICS　›　<span>CONTINUITY</span></h1><p>Explore how flow speed changes with cross-sectional area in steady incompressible flow.</p></header><div className="frs-modes">{['Observe','Predict','Experiment','Explain'].map(m=><button key={m} onClick={()=>m!=='Observe'&&setPanel(m)}>{m}</button>)}</div><div className="frs-layout"><section className="frs-stage"><svg viewBox="0 0 900 380"><rect width="900" height="380" fill="#10283a"/><path d="M80 120 H300 C390 120 420 250 510 250 H820" fill="none" stroke="#6fb9df" strokeWidth="85" opacity=".65"/><path d="M100 120 H300 C390 120 420 250 510 250 H800" fill="none" stroke="#55bfff" strokeWidth="8" strokeDasharray="18 14"/><text x="160" y="70" fill="#66e5ff">A₁={input.a1.toExponential(2)} m² · v₁={s.v1.toFixed(2)} m/s</text><text x="540" y="320" fill="#db7aff">A₂={input.a2.toExponential(2)} m² · v₂={s.v2.toFixed(2)} m/s</text></svg></section><aside className="frs-right"><section className="frs-panel"><h2>CONTROLS</h2><C label="Inlet Area A₁" value={input.a1} min={.001} max={.01} unit="m²" onChange={a1=>update({a1})}/><C label="Outlet Area A₂" value={input.a2} min={.0005} max={.005} unit="m²" onChange={a2=>update({a2})}/><C label="Flow Rate Q" value={input.q} min={.0005} max={.01} unit="m³/s" onChange={q=>update({q})}/></section><section className="frs-panel frs-readings"><h2>LIVE READINGS</h2><p>Inlet Velocity v₁<b>{s.v1.toFixed(2)} m/s</b></p><p>Outlet Velocity v₂<b>{s.v2.toFixed(2)} m/s</b></p><p>Volumetric Flow Q<b>{input.q.toExponential(2)} m³/s</b></p><p>Mass Flow Rate ṁ<b>{s.mass.toFixed(2)} kg/s</b></p></section><section className="frs-panel frs-equation"><h2>GOVERNING EQUATION</h2><b>A₁v₁ = A₂v₂</b><p>For incompressible steady flow, narrowing the tube increases speed.</p></section></aside></div><div className="frs-actions"><button onClick={()=>setInput(CONTINUITY_DEFAULTS)}>⟳ Reset</button><button className="frs-next" onClick={()=>setPanel('Challenge')}>Next Challenge　→</button></div><footer>FLUID MECHANICS　|　CONTINUITY</footer></main>{panel&&<MasteryChallengeDialog title={panel} close={()=>setPanel(null)}><p>Change the outlet area and observe the velocity response.</p></MasteryChallengeDialog>}</div>}
