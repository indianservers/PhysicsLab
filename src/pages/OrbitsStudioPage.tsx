import{useState}from'react';import{Link}from'react-router-dom';import{MasteryChallengeDialog}from'../components/MasteryChallengeDialog';import{ORBIT_DEFAULTS,orbitSettings,orbitSolution,type OrbitInput}from'../lib/orbitStudio';import'../friction-studio.css';function C({label,value,min,max,unit,onChange}:{label:string;value:number;min:number;max:number;unit:string;onChange:(v:number)=>void}){return <label className="frs-control"><span>{label}<input aria-label={label+' value'} type="number" value={value.toFixed(2)} onChange={e=>onChange(Number(e.target.value))}/><em>{unit}</em></span><input aria-label={label} type="range" min={min} max={max} step={(max-min)/100} value={value} onChange={e=>onChange(Number(e.target.value))}/><small><i>{min}</i><i>{max}</i></small></label>}export default function OrbitsStudioPage(){const[input,setInput]=useState(ORBIT_DEFAULTS),[panel,setPanel]=useState<string|null>(null);const s=orbitSolution(input);const update=(p:Partial<OrbitInput>)=>setInput(orbitSettings({...input,...p}));return <div className="frs-page tension-page" data-ui-theme="dark"><aside className="frs-side"><Link className="frs-brand" to="/concept-studio">⚛ PHYSICS STUDIO</Link><nav><Link to="/concept-studio">⌂ Home</Link><Link to="/motion/universal-gravity">Universal Gravitation</Link><Link to="/motion/gravitational-field">Gravitational Field</Link><Link to="/motion/gravitational-potential">Gravitational Potential</Link><Link to="/motion/orbits">Orbits</Link><Link to="/motion/escape-velocity">Escape Velocity</Link><Link to="/motion/keplers-laws">Kepler’s Laws</Link></nav></aside><main id="content" className="frs-main"><header className="frs-head"><h1>GRAVITATION　›　<span>ORBITS</span></h1><p>Explore real orbits. Change conditions and see the physics.</p></header><div className="frs-modes">{['Observe','Predict','Experiment','Explain'].map(m=><button key={m} onClick={()=>m!=='Observe'&&setPanel(m)}>{m}</button>)}</div><div className="frs-layout"><section className="frs-stage"><img src="/assets/friction-studio/field-earth.png" alt="Earth with orbital path"/><svg viewBox="0 0 900 380"><ellipse cx="450" cy="205" rx="330" ry="120" fill="none" stroke="#2ac7ff" strokeWidth="3"/><circle cx="450" cy="205" r="90" fill="#2667ae" opacity=".8"/><circle cx="745" cy="205" r="8" fill="#54ffac"/><circle cx="250" cy="205" r="8" fill="#ff6b7a"/><text x="690" y="190" fill="#54ffac">Periapsis</text><text x="200" y="190" fill="#ff7b8d">Apoapsis</text><text x="510" y="105" fill="#9de5ff">v = {input.speed.toFixed(2)} km/s</text></svg></section><aside className="frs-right"><section className="frs-panel"><h2>CONTROLS</h2><C label="Launch Altitude" value={input.altitude} min={100} max={10000} unit="km" onChange={altitude=>update({altitude})}/><C label="Tangential Speed" value={input.speed} min={3} max={12} unit="km/s" onChange={speed=>update({speed})}/><C label="Central Mass" value={input.mass/1e24} min={.1} max={50} unit="×10²⁴ kg" onChange={mass=>update({mass:mass*1e24})}/></section><section className="frs-panel frs-readings"><h2>LIVE READINGS</h2><p>Orbital Period<b>{s.period.toFixed(2)} h</b></p><p>Eccentricity<b>{s.e.toFixed(3)}</b></p><p>Current Radius<b>{s.radius.toFixed(0)} km</b></p><p>Escape speed at radius<b>{(s.vc*Math.sqrt(2)).toFixed(2)} km/s</b></p></section></aside><section className="frs-equation frs-panel"><h2>ORBITAL EQUATION</h2><b>r = a(1−e²)/(1+e cos θ)</b><p>Changing launch speed changes the orbit’s eccentricity and period.</p></section></div><div className="frs-actions"><button onClick={()=>setInput(ORBIT_DEFAULTS)}>⟳ Reset</button><button onClick={()=>setPanel('Challenge')} className="frs-next">Next Challenge　→</button></div><footer>GRAVITATION　|　ORBITS</footer></main>{panel&&<MasteryChallengeDialog title={panel} close={()=>setPanel(null)}><p>Find the speed that gives a circular orbit at this altitude.</p></MasteryChallengeDialog>}</div>}
