import{useState}from'react';import{Link}from'react-router-dom';import{MasteryChallengeDialog}from'../components/MasteryChallengeDialog';import{LORENTZ_DEFAULTS,lorentzSettings,lorentzSolution,type LorentzInput}from'../lib/lorentzStudio';import'../friction-studio.css';function C({label,value,min,max,unit,onChange}:{label:string;value:number;min:number;max:number;unit:string;onChange:(v:number)=>void}){return <label className="frs-control"><span>{label}<input aria-label={label+' value'} type="number" value={value.toExponential(2)} onChange={e=>onChange(Number(e.target.value))}/><em>{unit}</em></span><input aria-label={label} type="range" min={min} max={max} step={(max-min)/100} value={value} onChange={e=>onChange(Number(e.target.value))}/><small><i>{min}</i><i>{max}</i></small></label>}export default function LorentzForceStudioPage(){const[input,setInput]=useState(LORENTZ_DEFAULTS),[panel,setPanel]=useState<string|null>(null);const s=lorentzSolution(input);const update=(p:Partial<LorentzInput>)=>setInput(lorentzSettings({...input,...p}));return <div className="frs-page tension-page" data-ui-theme="dark"><aside className="frs-side"><Link className="frs-brand" to="/concept-studio">◉ MAGNETISM</Link><nav><Link to="/concept-studio">⌂ Home</Link><Link to="/magnetism/field-lines">Field Lines</Link><Link to="/magnetism/lorentz-force">Lorentz Force</Link><Link to="/magnetism/current-wire">Current Wire</Link></nav></aside><main id="content" className="frs-main"><header className="frs-head"><h1>MAGNETISM　›　<span>LORENTZ FORCE</span></h1><p>Observe how a charged particle moves in a uniform magnetic field.</p></header><div className="frs-modes">{['Observe','Predict','Experiment','Explain'].map(m=><button key={m} onClick={()=>m!=='Observe'&&setPanel(m)}>{m}</button>)}</div><div className="frs-layout"><section className="frs-stage"><svg viewBox="0 0 900 380"><rect width="900" height="380" fill="#0a2438"/>{Array.from({length:8},(_,i)=><line key={i} x1={160+i*85} y1="40" x2={160+i*85} y2="340" stroke="#1587d5" opacity=".7" strokeWidth="3"/>)}<path d="M120 190 C300 190 500 190 620 260 A130 130 0 1 1 620 120" fill="none" stroke="#79c8ff" strokeWidth="5" strokeDasharray="8 6"/><circle cx="120" cy="190" r="10" fill="#fff"/><line x1="120" y1="190" x2="190" y2="190" stroke="#ff4e58" strokeWidth="6"/><line x1="120" y1="190" x2="120" y2="125" stroke="#35e37c" strokeWidth="6"/><text x="200" y="185" fill="#ff5b65">v</text><text x="130" y="120" fill="#45e781">F</text><text x="650" y="70" fill="white">r = {s.r.toFixed(3)} m</text></svg></section><aside className="frs-right"><section className="frs-panel"><h2>CONTROLS</h2><label>Charge Sign <button onClick={()=>update({sign:input.sign*-1})}>{input.sign>0?'Positive (q > 0)':'Negative (q < 0)'}</button></label><C label="Particle Speed v" value={input.speed} min={1e6} max={1e7} unit="m/s" onChange={speed=>update({speed})}/><C label="Magnetic Field B" value={input.B} min={.05} max={.5} unit="T" onChange={B=>update({B})}/></section><section className="frs-panel frs-readings"><h2>LIVE READINGS</h2><p>Radius of curvature r<b>{s.r.toFixed(3)} m</b></p><p>Magnetic force |F|<b>{s.F.toExponential(2)} N</b></p><p>Path circumference<b>{(2*Math.PI*s.r).toFixed(3)} m</b></p><p>Revolution period<b>{s.T.toExponential(2)} s</b></p><p>Frequency<b>{s.freq.toExponential(2)} Hz</b></p></section></aside><section className="frs-equation frs-panel"><h2>EQUATION</h2><b>𝐅 = q𝐯 × 𝐁　　r = mv/|q|B　　|F| = |q|vB</b><p>The magnetic force is perpendicular to both velocity and field.</p></section></div><div className="frs-actions"><button onClick={()=>setInput(LORENTZ_DEFAULTS)}>⟳ Reset</button><button className="frs-next" onClick={()=>setPanel('Challenge')}>Next Challenge　→</button></div><footer>MAGNETISM　|　LORENTZ FORCE</footer></main>{panel&&<MasteryChallengeDialog title={panel} close={()=>setPanel(null)}><p>Change speed or field strength and predict the new orbit radius.</p></MasteryChallengeDialog>}</div>}
