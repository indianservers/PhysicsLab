import{useState}from'react';import{Link}from'react-router-dom';import{MasteryChallengeDialog}from'../components/MasteryChallengeDialog';import{POTENTIAL_DEFAULTS,potentialSettings,potentialSolution,type PotentialInput}from'../lib/potentialStudio';import'../friction-studio.css';
function C({label,value,min,max,unit,onChange}:{label:string;value:number;min:number;max:number;unit:string;onChange:(v:number)=>void}){return <label className="frs-control"><span>{label}<input aria-label={label+' value'} type="number" value={value.toFixed(2)} onChange={e=>onChange(Number(e.target.value))}/><em>{unit}</em></span><input aria-label={label} type="range" value={value} min={min} max={max} step=".01" onChange={e=>onChange(Number(e.target.value))}/><small><i>{min}</i><i>{max}</i></small></label>}
export default function PotentialEnergyStudioPage(){const[input,setInput]=useState(POTENTIAL_DEFAULTS),[panel,setPanel]=useState<string|null>(null),[zoom,setZoom]=useState(false);const s=potentialSolution(input);const update=(p:Partial<PotentialInput>)=>setInput(potentialSettings({...input,...p}));const reset=()=>{setInput(POTENTIAL_DEFAULTS);setPanel(null);setZoom(false)};return <div className="frs-page tension-page" data-ui-theme="dark"><aside className="frs-side"><Link className="frs-brand" to="/concept-studio">⚛ PHYSICS STUDIO</Link><nav><Link to="/concept-studio">⌂ Home</Link><button>⚙ Mechanics⌃</button>{[['Kinematics','/concept-studio/motion-kinematics'],['Forces','/motion/friction'],['Work, Energy & Power','/motion/work'],['Momentum','/mechanics/momentum-collisions'],['Rotational Motion','/mechanics/torque-levers'],['Gravitation','/concept-studio/gravitation'],['Waves','/concept-studio/waves-sound'],['Thermodynamics','/concept-studio/thermodynamics'],['Electricity','/concept-studio/electricity'],['Optics','/concept-studio/waves-sound'],['Modern Physics','/concept-studio/modern-physics']].map(([n,to])=><Link key={n} to={to}>{n}</Link>)}</nav></aside><main id="content" className="frs-main"><header className="frs-head"><h1>WORK, ENERGY &amp; POWER　›　<span>POTENTIAL ENERGY</span></h1><p>Lift a mass and store gravitational potential energy.</p></header><div className="frs-modes">{['Observe','Predict','Experiment','Explain'].map(m=><button key={m} aria-pressed={m==='Observe'} onClick={()=>m!=='Observe'&&setPanel(m)}>{m}</button>)}</div><div className="frs-layout"><section className="frs-stage" style={{transform:zoom?'scale(1.1)':'none'}}><img src="/assets/friction-studio/crane.png" alt="Tower crane lifting a concrete block"/><svg viewBox="0 0 900 380"><line x1="600" y1="100" x2="600" y2="300" stroke="#72d9ff" strokeDasharray="8 5"/><text x="620" y="180" fill="white" fontSize="19">h = {input.height.toFixed(1)} m</text><text x="80" y="100" fill="#7ccaff" fontSize="19">g = {input.gravity.toFixed(2)} m/s²</text></svg></section><aside className="frs-right"><section className="frs-panel"><h2>CONTROLS</h2><C label="Mass (kg)" value={input.mass} min={50} max={1000} unit="kg" onChange={mass=>update({mass})}/><C label="Height (m)" value={input.height} min={0} max={30} unit="m" onChange={height=>update({height})}/><C label="Gravity, g" value={input.gravity} min={1.62} max={24.79} unit="m/s²" onChange={gravity=>update({gravity})}/></section><section className="frs-panel frs-readings"><h2>LIVE READINGS</h2><p>Mass, m<b>{input.mass.toFixed(0)} kg</b></p><p>Height, h<b>{input.height.toFixed(1)} m</b></p><p>Gravitational field, g<b>{input.gravity.toFixed(2)} m/s²</b></p><p>Potential energy, U<sub>g</sub><b>{s.energy.toFixed(0)} J</b></p></section></aside><section className="frs-equation frs-panel"><h2>EQUATION</h2><b>ΔU<sub>g</sub> = mgh</b><p>Potential energy increases with mass, height and gravitational field strength.</p></section></div><div className="frs-actions"><button aria-label="Reset" onClick={reset}>⟳ Reset</button><button aria-label={zoom?'Reset Zoom':'Zoom'} onClick={()=>setZoom(v=>!v)}>⌕ {zoom?'Reset Zoom':'Zoom'}</button><button className="frs-next" onClick={()=>setPanel('Challenge')}>Next Challenge　→</button></div><footer>WORK, ENERGY &amp; POWER　|　POTENTIAL ENERGY</footer></main>{panel&&<MasteryChallengeDialog title={panel} close={()=>setPanel(null)}><p>{panel==='Predict'?'Which change stores more potential energy: doubling height or doubling mass?':panel==='Explain'?'Gravitational potential energy is U = mgh relative to a chosen reference level.':'Change the mass, height and gravitational field, then compare energy.'}</p></MasteryChallengeDialog>}</div>}
