import{useState}from'react';import{Link}from'react-router-dom';import{MasteryChallengeDialog}from'../components/MasteryChallengeDialog';import{CAP_DEFAULTS,capSettings,capSolution,type CapacitorInput}from'../lib/capacitorStudio';import'../friction-studio.css';function C({label,value,min,max,unit,onChange}:{label:string;value:number;min:number;max:number;unit:string;onChange:(v:number)=>void}){return <label className="frs-control"><span>{label}<input aria-label={label+' value'} type="number" value={value.toFixed(2)} onChange={e=>onChange(Number(e.target.value))}/><em>{unit}</em></span><input aria-label={label} type="range" min={min} max={max} step=".01" value={value} onChange={e=>onChange(Number(e.target.value))}/><small><i>{min}</i><i>{max}</i></small></label>}export default function CapacitorsStudioPage(){const[input,setInput]=useState(CAP_DEFAULTS),[panel,setPanel]=useState<string|null>(null);const s=capSolution(input);const update=(p:Partial<CapacitorInput>)=>setInput(capSettings({...input,...p}));return <div className="frs-page tension-page" data-ui-theme="dark"><aside className="frs-side"><Link className="frs-brand" to="/concept-studio">⚡ ELECTRICITY</Link><nav><Link to="/concept-studio">⌂ Home</Link><Link to="/electricity/current">Electric Current</Link><Link to="/electricity/circuits">Circuits</Link><Link to="/electricity/capacitors">Capacitors</Link></nav></aside><main id="content" className="frs-main"><header className="frs-head"><h1>ELECTRICITY　›　<span>CAPACITORS</span></h1><p>Explore how geometry and materials affect capacitance, charge, and energy.</p></header><div className="frs-modes">{['Observe','Predict','Experiment','Explain'].map(m=><button key={m} onClick={()=>m!=='Observe'&&setPanel(m)}>{m}</button>)}</div><div className="frs-layout"><section className="frs-stage"><svg viewBox="0 0 900 430"><rect x="270" y="100" width="80" height="250" fill="#ef5b66" opacity=".8"/><rect x="550" y="100" width="80" height="250" fill="#397fe5" opacity=".8"/><rect x="350" y="125" width="200" height="200" fill="#78c7ff" opacity=".2" stroke="white"/><text x="410" y="225" fill="white">Dielectric εᵣ={input.er.toFixed(1)}</text>{Array.from({length:9},(_,i)=><line key={i} x1={350+i*25} y1="135" x2={350+i*25} y2="315" stroke="#7ed2ff" opacity=".5"/>)}<text x="350" y="70" fill="white">C = {(s.C*1e6).toFixed(2)} µF</text></svg></section><aside className="frs-right"><section className="frs-panel"><h2>CONTROLS</h2><C label="Plate Area A" value={input.area} min={1} max={100} unit="cm²" onChange={area=>update({area})}/><C label="Separation d" value={input.distance} min={.5} max={10} unit="mm" onChange={distance=>update({distance})}/><C label="Voltage V" value={input.voltage} min={0} max={12} unit="V" onChange={voltage=>update({voltage})}/><label>Dielectric Material <select aria-label="Dielectric Material" value={input.er} onChange={e=>update({er:Number(e.target.value)})}><option value="1">Air (1.0)</option><option value="2.5">Plastic (2.5)</option><option value="3.9">Glass (3.9)</option><option value="7">Ceramic (7.0)</option></select></label></section><section className="frs-panel frs-readings"><h2>LIVE READINGS</h2><p>Capacitance C<b>{(s.C*1e6).toFixed(2)} µF</b></p><p>Stored Charge Q<b>{(s.charge*1e6).toFixed(1)} µC</b></p><p>Stored Energy U<b>{(s.energy*1e6).toFixed(1)} µJ</b></p></section></aside><section className="frs-equation frs-panel"><h2>KEY EQUATION</h2><b>C = εᵣ ε₀ A/d　　U = ½CV²</b><p>Increasing area or dielectric constant raises capacitance; increasing separation lowers it.</p></section></div><div className="frs-actions"><button onClick={()=>setInput(CAP_DEFAULTS)}>⟳ Reset</button><button className="frs-next" onClick={()=>setPanel('Challenge')}>Next Challenge　→</button></div><footer>ELECTRICITY　|　CAPACITORS</footer></main>{panel&&<MasteryChallengeDialog title={panel} close={()=>setPanel(null)}><p>Change the plate spacing and dielectric to maximize stored energy.</p></MasteryChallengeDialog>}</div>}
