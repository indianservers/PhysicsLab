import{useState}from'react';import{Link}from'react-router-dom';import{MasteryChallengeDialog}from'../components/MasteryChallengeDialog';import{ENERGY_DEFAULTS,energySettings,energySolution,type EnergyExchangeInput}from'../lib/energyExchangeStudio';import'../friction-studio.css';function C({label,value,min,max,unit,onChange}:{label:string;value:number;min:number;max:number;unit:string;onChange:(v:number)=>void}){return <label className="frs-control"><span>{label}<input aria-label={label+' value'} type="number" value={value.toFixed(3)} onChange={e=>onChange(Number(e.target.value))}/><em>{unit}</em></span><input aria-label={label} type="range" min={min} max={max} step={(max-min)/100} value={value} onChange={e=>onChange(Number(e.target.value))}/><small><i>{min}</i><i>{max}</i></small></label>}export default function EnergyExchangeStudioPage(){const[input,setInput]=useState(ENERGY_DEFAULTS),[panel,setPanel]=useState<string|null>(null);const s=energySolution(input);const update=(p:Partial<EnergyExchangeInput>)=>setInput(energySettings({...input,...p}));return <div className="frs-page tension-page" data-ui-theme="dark"><aside className="frs-side"><Link className="frs-brand" to="/concept-studio">⚛ PHYSICS STUDIO</Link><nav><Link to="/concept-studio">⌂ Home</Link><Link to="/motion/spring-shm">Spring SHM</Link><Link to="/motion/pendulum">Pendulum</Link><Link to="/motion/energy-exchange">Energy Exchange</Link><Link to="/motion/damping">Damping</Link><Link to="/motion/resonance">Resonance</Link></nav></aside><main id="content" className="frs-main"><header className="frs-head"><h1>OSCILLATIONS　›　<span>ENERGY EXCHANGE</span></h1><p>Track kinetic and elastic potential energy as a mass oscillates.</p></header><div className="frs-modes">{['Observe','Predict','Experiment','Explain'].map(m=><button key={m} onClick={()=>m!=='Observe'&&setPanel(m)}>{m}</button>)}</div><div className="frs-layout"><section className="frs-stage"><img src="/assets/friction-studio/spring.png" alt="Mass spring oscillator"/><svg viewBox="0 0 900 380"><line x1="180" y1="210" x2="700" y2="210" stroke="#9ecff8" strokeWidth="3"/><rect x={430+s.x*180} y="180" width="76" height="55" rx="8" fill="#1464be"/><path d={`M180 205 q40 ${s.x*80} 80 0 q40 ${-s.x*80} 80 0 q40 ${s.x*80} 80 0`} fill="none" stroke="#e3e9ef" strokeWidth="4"/><text x="520" y="150" fill="#9de5ff">x = {s.x.toFixed(3)} m</text></svg></section><aside className="frs-right"><section className="frs-panel"><h2>CONTROLS</h2><C label="Amplitude A" value={input.amplitude} min={.05} max={.3} unit="m" onChange={amplitude=>update({amplitude})}/><C label="Mass m" value={input.mass} min={.1} max={2} unit="kg" onChange={mass=>update({mass})}/><C label="Spring Constant k" value={input.k} min={10} max={200} unit="N/m" onChange={k=>update({k})}/><C label="Time t" value={input.time} min={0} max={10} unit="s" onChange={time=>update({time})}/></section><section className="frs-panel frs-readings"><h2>ENERGY (J)</h2><p>Kinetic Energy<b>{s.ke.toFixed(3)}</b></p><p>Elastic Potential Energy<b>{s.pe.toFixed(3)}</b></p><p>Total Energy<b>{s.total.toFixed(3)}</b></p><p>Phase φ<b>{s.phase.toFixed(2)} rad</b></p><p>Period T<b>{s.T.toFixed(3)} s</b></p></section></aside><section className="frs-equation frs-panel"><h2>EQUATION</h2><b>E = ½mv² + ½kx²</b><p>Total mechanical energy remains constant while kinetic and elastic potential energy exchange.</p></section></div><div className="frs-actions"><button onClick={()=>setInput(ENERGY_DEFAULTS)}>⟳ Reset</button><button className="frs-next" onClick={()=>setPanel('Challenge')}>Next Challenge　→</button></div><footer>OSCILLATIONS　|　ENERGY EXCHANGE</footer></main>{panel&&<MasteryChallengeDialog title={panel} close={()=>setPanel(null)}><p>At which phase is kinetic energy largest?</p></MasteryChallengeDialog>}</div>}
