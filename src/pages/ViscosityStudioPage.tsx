import{useState}from'react';import{Link}from'react-router-dom';import{MasteryChallengeDialog}from'../components/MasteryChallengeDialog';import{VISCOSITY_DEFAULTS,viscositySettings,viscositySolution,type ViscosityInput}from'../lib/viscosityStudio';import'../friction-studio.css';
function C({label,value,min,max,onChange,unit}:{label:string;value:number;min:number;max:number;onChange:(v:number)=>void;unit:string}){return <label className="frs-control"><span>{label}<input aria-label={label+' value'} type="number" value={value} onChange={e=>onChange(Number(e.target.value))}/><em>{unit}</em></span><input aria-label={label} type="range" min={min} max={max} step=".1" value={value} onChange={e=>onChange(Number(e.target.value))}/><small><i>{min}</i><i>{max}</i></small></label>}
export default function ViscosityStudioPage(){const[input,setInput]=useState(VISCOSITY_DEFAULTS),[panel,setPanel]=useState<string|null>(null);const s=viscositySolution(input);const update=(p:Partial<ViscosityInput>)=>setInput(viscositySettings({...input,...p}));return <div className="frs-page tension-page" data-ui-theme="dark"><aside className="frs-side"><Link className="frs-brand" to="/concept-studio">◈ FLUID MECHANICS</Link><nav><Link to="/concept-studio">⌂ Home</Link><Link to="/fluid-mechanics/bernoulli">Bernoulli</Link><Link to="/fluid-mechanics/viscosity">Viscosity</Link></nav></aside><main id="content" className="frs-main"><header className="frs-head"><h1>FLUID MECHANICS　›　<span>VISCOSITY</span></h1><p>A falling sphere viscometer reveals resistance in a viscous fluid.</p></header><div className="frs-modes">{['Observe','Predict','Experiment','Explain'].map(m=><button key={m} onClick={()=>m!=='Observe'&&setPanel(m)}>{m}</button>)}</div><div className="frs-layout"><section className="frs-stage"><svg viewBox="0 0 900 380"><rect width="900" height="380" fill="#173044"/><rect x="330" y="35" width="240" height="310" rx="24" fill="#8a6d24" opacity=".8" stroke="#d8efff" strokeWidth="6"/><line x1="450" y1="40" x2="450" y2="335" stroke="#d9fbff" strokeDasharray="10 8" opacity=".7"/>{[80,130,180,230,280].map((y,i)=><circle key={i} cx="450" cy={y+i*8} r={input.radius*1.7} fill="#aeb9c2" stroke="#fff" opacity={i===4?1:.35}/>) }<path d="M450 65 V110" stroke="#7de5ff" strokeWidth="5" markerEnd="url(#a)"/><text x="350" y="360" fill="#9ed5ff">vₜ={s.vt.toFixed(4)} m/s</text><defs><marker id="a" markerWidth="8" markerHeight="8" refX="4" refY="4" orient="auto"><path d="M0 0L8 4L0 8z" fill="#7de5ff"/></marker></defs></svg></section><aside className="frs-right"><section className="frs-panel"><h2>CONTROLS</h2><label>Fluid <select aria-label="Fluid" value={input.fluid} onChange={e=>update({fluid:e.target.value})}><option value="silicone">Silicone Oil</option><option value="glycerin">Glycerin</option></select></label><C label="Sphere Radius" value={input.radius} min={1} max={20} onChange={radius=>update({radius})} unit="mm"/><C label="Temperature" value={input.temp} min={0} max={100} onChange={temp=>update({temp})} unit="°C"/></section><section className="frs-panel frs-readings"><h2>REAL-TIME READINGS</h2><p>Dynamic Viscosity μ<b>{s.mu.toFixed(3)} Pa·s</b></p><p>Terminal Speed vₜ<b>{s.vt.toFixed(4)} m/s</b></p><p>Drag Force Fd<b>{s.drag.toExponential(2)} N</b></p><p>Reynolds Number Re<b>{s.re.toFixed(3)}</b></p></section><section className="frs-panel frs-equation"><h2>STOKES’ LAW</h2><b>F<sub>d</sub> = 6πμrv</b><p>Laminar drag grows with viscosity, radius, and speed.</p></section></aside></div><div className="frs-actions"><button onClick={()=>setInput(VISCOSITY_DEFAULTS)}>⟳ Reset</button><button className="frs-next" onClick={()=>setPanel('Challenge')}>Next Challenge　→</button></div><footer>FLUID MECHANICS　|　VISCOSITY</footer></main>{panel&&<MasteryChallengeDialog title={panel} close={()=>setPanel(null)}><p>Change temperature and sphere radius to compare terminal speeds.</p></MasteryChallengeDialog>}</div>}
