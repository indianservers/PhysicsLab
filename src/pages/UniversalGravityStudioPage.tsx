import{useState}from'react';import{Link}from'react-router-dom';import{MasteryChallengeDialog}from'../components/MasteryChallengeDialog';import{GRAVITY_DEFAULTS,gravitySettings,gravitySolution,type GravityInput}from'../lib/gravityStudio';import'../friction-studio.css';function C({label,value,min,max,unit,onChange}:{label:string;value:number;min:number;max:number;unit:string;onChange:(v:number)=>void}){return <label className="frs-control"><span>{label}<input aria-label={label+' value'} type="number" value={value.toFixed(2)} onChange={e=>onChange(Number(e.target.value))}/><em>{unit}</em></span><input aria-label={label} type="range" value={value} min={min} max={max} step=".01" onChange={e=>onChange(Number(e.target.value))}/><small><i>{min}</i><i>{max}</i></small></label>}export default function UniversalGravityStudioPage(){const[input,setInput]=useState(GRAVITY_DEFAULTS),[panel,setPanel]=useState<string|null>(null),[zoom,setZoom]=useState(false);const s=gravitySolution(input);const update=(p:Partial<GravityInput>)=>setInput(gravitySettings({...input,...p}));const reset=()=>{setInput(GRAVITY_DEFAULTS);setPanel(null);setZoom(false)};return <div className="frs-page tension-page" data-ui-theme="dark"><aside className="frs-side"><Link className="frs-brand" to="/concept-studio">⚛ PHYSICS STUDIO</Link><nav><Link to="/concept-studio">⌂ Home</Link><button>⚙ Mechanics⌃</button>{[['Kinematics','/concept-studio/motion-kinematics'],['Work, Energy & Power','/motion/work'],['Gravitation','/motion/universal-gravity'],['Waves','/concept-studio/waves-sound'],['Thermodynamics','/concept-studio/thermodynamics'],['Electricity','/concept-studio/electricity'],['Magnetism','/concept-studio/electricity'],['Optics','/concept-studio/waves-sound'],['Modern Physics','/concept-studio/modern-physics']].map(([n,to])=><Link key={n} to={to}>{n}</Link>)}</nav></aside><main id="content" className="frs-main"><header className="frs-head"><h1>GRAVITATION　›　<span>UNIVERSAL GRAVITATION</span></h1><p>Explore how mass and distance determine gravitational force.</p></header><div className="frs-modes">{['Observe','Predict','Experiment','Explain'].map(m=><button key={m} aria-pressed={m==='Observe'} onClick={()=>m!=='Observe'&&setPanel(m)}>{m}</button>)}</div><div className="frs-layout"><section className="frs-stage" style={{transform:zoom?'scale(1.1)':'none'}}><img src="/assets/friction-studio/torsion.png" alt="Torsion balance with two metal spheres"/><svg viewBox="0 0 900 380"><line x1="250" y1="210" x2="390" y2="210" stroke="#ff5261" strokeWidth="5" markerEnd="url(#a)"/><line x1="650" y1="210" x2="510" y2="210" stroke="#ff5261" strokeWidth="5" markerEnd="url(#a)"/><text x="360" y="180" fill="white" fontSize="18">r = {input.r.toFixed(2)} m</text><text x="80" y="80" fill="#70caff" fontSize="18">F = {s.force.toExponential(3)} N</text><defs><marker id="a" markerWidth="10" markerHeight="10" refX="9" refY="4" orient="auto"><path d="M0 0 10 4 0 8Z" fill="#ff5261"/></marker></defs></svg></section><aside className="frs-right"><section className="frs-panel"><h2>CONTROLS</h2><C label="Mass 1 (m1)" value={input.m1} min={.1} max={20} unit="kg" onChange={m1=>update({m1})}/><C label="Mass 2 (m2)" value={input.m2} min={.1} max={20} unit="kg" onChange={m2=>update({m2})}/><C label="Separation (r)" value={input.r} min={.05} max={1} unit="m" onChange={r=>update({r})}/></section><section className="frs-panel frs-readings"><h2>LIVE READINGS</h2><p>Force on m1<b>{s.force.toExponential(3)} N</b></p><p>Force on m2<b>{s.force.toExponential(3)} N</b></p><p>Torque<b>{s.torque.toExponential(3)} N·m</b></p><p>Force ratio<b>{s.ratio.toFixed(3)}</b></p></section></aside><section className="frs-equation frs-panel"><h2>EQUATION</h2><b>F = G m₁m₂ / r²</b><p>Gravitational attraction increases with mass and decreases with the square of separation.</p></section></div><div className="frs-actions"><button aria-label="Reset" onClick={reset}>⟳ Reset</button><button aria-label={zoom?'Reset Zoom':'Zoom'} onClick={()=>setZoom(v=>!v)}>⌕ {zoom?'Reset Zoom':'Zoom'}</button><button className="frs-next" onClick={()=>setPanel('Challenge')}>Next Challenge　→</button></div><footer>GRAVITATION　|　UNIVERSAL GRAVITATION</footer></main>{panel&&<MasteryChallengeDialog title={panel} close={()=>setPanel(null)}><p>{panel==='Predict'?'What happens to force if separation doubles?':panel==='Explain'?'Newton’s law of universal gravitation follows an inverse-square relationship with distance.':'Change both masses and separation to compare the attraction.'}</p></MasteryChallengeDialog>}</div>}
