import{useState}from'react';import{Link}from'react-router-dom';import{MasteryChallengeDialog}from'../components/MasteryChallengeDialog';import{GAS_DEFAULTS,gasSettings,gasSolution,type GasInput}from'../lib/gasLawsStudio';import'../friction-studio.css';function C({label,value,min,max,unit,onChange}:{label:string;value:number;min:number;max:number;unit:string;onChange:(v:number)=>void}){return <label className="frs-control"><span>{label}<input aria-label={label+' value'} type="number" value={value.toFixed(2)} onChange={e=>onChange(Number(e.target.value))}/><em>{unit}</em></span><input aria-label={label} type="range" min={min} max={max} step=".01" value={value} onChange={e=>onChange(Number(e.target.value))}/><small><i>{min}</i><i>{max}</i></small></label>}export default function GasLawsStudioPage(){const[input,setInput]=useState(GAS_DEFAULTS),[panel,setPanel]=useState<string|null>(null);const s=gasSolution(input);const update=(p:Partial<GasInput>)=>setInput(gasSettings({...input,...p}));return <div className="frs-page tension-page" data-ui-theme="dark"><aside className="frs-side"><Link className="frs-brand" to="/concept-studio">♨ THERMODYNAMICS</Link><nav><Link to="/concept-studio">⌂ Home</Link><Link to="/thermodynamics/temperature">Temperature</Link><Link to="/thermodynamics/heat-transfer">Heat Transfer</Link><Link to="/thermodynamics/gas-laws">Gas Laws</Link><Link to="/thermodynamics/first-law">First Law</Link></nav></aside><main id="content" className="frs-main"><header className="frs-head"><h1>THERMODYNAMICS　›　<span>GAS LAWS</span></h1><p>Explore pressure, volume, and temperature in an ideal gas.</p></header><div className="frs-modes">{['Observe','Predict','Experiment','Explain'].map(m=><button key={m} onClick={()=>m!=='Observe'&&setPanel(m)}>{m}</button>)}</div><div className="frs-layout"><section className="frs-stage"><svg viewBox="0 0 900 380"><rect width="900" height="380" fill="#152c40"/><rect x="300" y="90" width="260" height="240" rx="25" fill="#30475a" stroke="#a8c4da" strokeWidth="5"/><rect x="330" y={150+(input.load/20)*110} width="200" height="18" fill="#d4e4ed"/><line x1="430" y1="40" x2="430" y2={150+(input.load/20)*110} stroke="#9fb6c7" strokeWidth="12"/>{Array.from({length:30},(_,i)=><circle key={i} cx={330+(i*37)%190} cy={185+(i*31)%120} r="5" fill="#6fa8ff"/>)}<text x="360" y="70" fill="white">P = {s.P.toFixed(1)} kPa</text><text x="360" y="355" fill="#9ed5ff">V = {s.V.toFixed(3)} L</text></svg></section><aside className="frs-right"><section className="frs-panel"><h2>CONTROLS</h2><C label="Piston Load" value={input.load} min={0} max={20} unit="kg" onChange={load=>update({load})}/><C label="Gas Amount n" value={input.n} min={.01} max={.1} unit="mol" onChange={n=>update({n})}/><C label="Temperature T" value={input.T} min={200} max={600} unit="K" onChange={T=>update({T})}/></section><section className="frs-panel frs-readings"><h2>LIVE READINGS</h2><p>Pressure P<b>{s.P.toFixed(1)} kPa</b></p><p>Volume V<b>{s.V.toFixed(3)} L</b></p><p>Temperature T<b>{input.T} K</b></p><p>Amount of gas n<b>{input.n.toFixed(3)} mol</b></p></section></aside><section className="frs-equation frs-panel"><h2>IDEAL GAS LAW</h2><b>PV = nRT</b><p>Pressure, volume, temperature, and amount are linked by the ideal gas law.</p></section></div><div className="frs-actions"><button onClick={()=>setInput(GAS_DEFAULTS)}>⟳ Reset</button><button className="frs-next" onClick={()=>setPanel('Challenge')}>Next Challenge　→</button></div><footer>THERMODYNAMICS　|　GAS LAWS</footer></main>{panel&&<MasteryChallengeDialog title={panel} close={()=>setPanel(null)}><p>Change the piston load and predict the volume change.</p></MasteryChallengeDialog>}</div>}
