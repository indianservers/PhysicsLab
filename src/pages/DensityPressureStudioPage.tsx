import{useState}from'react';import{Link}from'react-router-dom';import{MasteryChallengeDialog}from'../components/MasteryChallengeDialog';import{DENSITY_DEFAULTS,densitySettings,densitySolution,FLUIDS,type DensityInput}from'../lib/densityPressureStudio';import'../friction-studio.css';
export default function DensityPressureStudioPage(){const[input,setInput]=useState(DENSITY_DEFAULTS),[panel,setPanel]=useState<string|null>(null);const s=densitySolution(input);const update=(p:Partial<DensityInput>)=>setInput(densitySettings({...input,...p}));return <div className="frs-page tension-page" data-ui-theme="dark"><aside className="frs-side"><Link className="frs-brand" to="/concept-studio">◈ FLUID MECHANICS</Link><nav><Link to="/concept-studio">⌂ Home</Link><Link to="/fluid-mechanics/density-pressure">Density & Pressure</Link><Link to="/fluid-mechanics/buoyancy">Buoyancy</Link></nav></aside><main id="content" className="frs-main"><header className="frs-head"><h1>FLUID MECHANICS　›　<span>DENSITY & PRESSURE</span></h1><p>Explore how density, depth and gravity determine pressure in a fluid.</p></header><div className="frs-modes">{['Observe','Predict','Experiment','Explain'].map(m=><button key={m} onClick={()=>m!=='Observe'&&setPanel(m)}>{m}</button>)}</div><div className="frs-layout"><section className="frs-stage"><svg viewBox="0 0 900 380"><rect width="900" height="380" fill="#14344a"/><rect x="240" y="35" width="420" height="300" rx="18" fill="#255b7a" stroke="#9ddcff" strokeWidth="6"/><rect x="246" y="170" width="408" height="159" fill={input.fluid==='oil'?'#a56a22':input.fluid==='glycerin'?'#4e2c8d':'#23709d'} opacity=".8"/><line x1="450" y1="60" x2="450" y2={80+input.depth*230} stroke="#fff" strokeWidth="5"/><circle cx="450" cy={80+input.depth*230} r="10" fill="#8ef"/><text x="275" y="145" fill="white">{FLUIDS[input.fluid as keyof typeof FLUIDS].name} · ρ={s.rho} kg/m³</text><text x="360" y="360" fill="#9ed5ff">P = {s.pressure.toFixed(2)} kPa</text></svg></section><aside className="frs-right"><section className="frs-panel"><h2>CONTROLS</h2><label>Fluid Type <select aria-label="Fluid Type" value={input.fluid} onChange={e=>update({fluid:e.target.value})}>{Object.entries(FLUIDS).map(([k,v])=><option key={k} value={k}>{v.name}</option>)}</select></label><label className="frs-control"><span>Probe Depth<input aria-label="Probe Depth value" type="number" value={input.depth} onChange={e=>update({depth:Number(e.target.value)})}/><em>m</em></span><input aria-label="Probe Depth" type="range" min="0" max="1" step=".01" value={input.depth} onChange={e=>update({depth:Number(e.target.value)})}/><small><i>0</i><i>1</i></small></label><label className="frs-control"><span>Gravity g<input aria-label="Gravity g value" type="number" value={input.g} onChange={e=>update({g:Number(e.target.value)})}/><em>m/s²</em></span><input aria-label="Gravity g" type="range" min="1.62" max="24.79" step=".01" value={input.g} onChange={e=>update({g:Number(e.target.value)})}/><small><i>1.62</i><i>24.79</i></small></label></section><section className="frs-panel frs-readings"><h2>LIVE READINGS</h2><p>Fluid at probe<b>{FLUIDS[input.fluid as keyof typeof FLUIDS].name}</b></p><p>Density ρ<b>{s.rho} kg/m³</b></p><p>Depth h<b>{input.depth.toFixed(2)} m</b></p><p>Gauge pressure P<b>{s.pressure.toFixed(2)} kPa</b></p></section><section className="frs-panel frs-equation"><h2>FORMULA</h2><b>P = ρgh</b><p>Gauge pressure increases linearly with depth.</p></section></aside></div><div className="frs-actions"><button onClick={()=>setInput(DENSITY_DEFAULTS)}>⟳ Reset</button><button className="frs-next" onClick={()=>setPanel('Challenge')}>Next Challenge　→</button></div><footer>FLUID MECHANICS　|　DENSITY & PRESSURE</footer></main>{panel&&<MasteryChallengeDialog title={panel} close={()=>setPanel(null)}><p>Compare pressure in fluids with different densities.</p></MasteryChallengeDialog>}</div>}
