import{useState}from'react';import{Link}from'react-router-dom';import{MasteryChallengeDialog}from'../components/MasteryChallengeDialog';import{PENDULUM_DEFAULTS,pendulumSettings,pendulumSolution,type PendulumInput}from'../lib/pendulumStudio';import'../friction-studio.css';function C({label,value,min,max,unit,onChange}:{label:string;value:number;min:number;max:number;unit:string;onChange:(v:number)=>void}){return <label className="frs-control"><span>{label}<input aria-label={label+' value'} type="number" value={value.toFixed(2)} onChange={e=>onChange(Number(e.target.value))}/><em>{unit}</em></span><input aria-label={label} type="range" min={min} max={max} step={(max-min)/100} value={value} onChange={e=>onChange(Number(e.target.value))}/><small><i>{min}</i><i>{max}</i></small></label>}export default function PendulumStudioPage(){const[input,setInput]=useState(PENDULUM_DEFAULTS),[panel,setPanel]=useState<string|null>(null);const s=pendulumSolution(input);const update=(p:Partial<PendulumInput>)=>setInput(pendulumSettings({...input,...p}));return <div className="frs-page tension-page" data-ui-theme="dark"><aside className="frs-side"><Link className="frs-brand" to="/concept-studio">⚛ PHYSICS STUDIO</Link><nav><Link to="/concept-studio">⌂ Home</Link><Link to="/motion/spring-shm">Spring SHM</Link><Link to="/motion/pendulum">Pendulum</Link><Link to="/motion/energy-exchange">Energy Exchange</Link><Link to="/motion/damping">Damping</Link><Link to="/motion/resonance">Resonance</Link></nav></aside><main id="content" className="frs-main"><header className="frs-head"><h1>OSCILLATIONS　›　<span>PENDULUM</span></h1><p>Measure period and explore dependence on length and gravity.</p></header><div className="frs-modes">{['Observe','Predict','Experiment','Explain'].map(m=><button key={m} onClick={()=>m!=='Observe'&&setPanel(m)}>{m}</button>)}</div><div className="frs-layout"><section className="frs-stage"><img src="/assets/friction-studio/spring.png" alt="Pendulum laboratory apparatus"/><svg viewBox="0 0 900 380"><line x1="450" y1="35" x2="450" y2="260" stroke="#d8efff" strokeWidth="4"/><line x1="450" y1="35" x2="530" y2="250" stroke="#69cbff" strokeWidth="3"/><circle cx="530" cy="250" r="24" fill="#b88546"/><path d="M390 95 A80 80 0 0 1 510 95" fill="none" stroke="#8be7ff"/><text x="540" y="230" fill="white">θ₀ = {input.angle.toFixed(1)}°</text></svg></section><aside className="frs-right"><section className="frs-panel"><h2>CONTROLS</h2><C label="Length L" value={input.length} min={.2} max={2} unit="m" onChange={length=>update({length})}/><C label="Release Angle θ₀" value={input.angle} min={1} max={60} unit="°" onChange={angle=>update({angle})}/><C label="Gravity g" value={input.gravity} min={1.62} max={24.79} unit="m/s²" onChange={gravity=>update({gravity})}/></section><section className="frs-panel frs-readings"><h2>LIVE READINGS</h2><p>Period T<b>{s.T.toFixed(3)} s</b></p><p>Frequency<b>{s.freq.toFixed(3)} Hz</b></p><p>Cycles per 5 s<b>{Math.floor(5/s.T)}</b></p><p>Energy per mass<b>{s.energy.toFixed(3)} J/kg</b></p></section></aside><section className="frs-equation frs-panel"><h2>THEORY (SMALL ANGLE)</h2><b>T = 2π √(L/g)</b><p>For small angles, the period depends on length and gravity, not mass.</p></section></div><div className="frs-actions"><button onClick={()=>setInput(PENDULUM_DEFAULTS)}>⟳ Reset</button><button className="frs-next" onClick={()=>setPanel('Challenge')}>Next Challenge　→</button></div><footer>OSCILLATIONS　|　PENDULUM</footer></main>{panel&&<MasteryChallengeDialog title={panel} close={()=>setPanel(null)}><p>Predict the period when the pendulum length doubles.</p></MasteryChallengeDialog>}</div>}
