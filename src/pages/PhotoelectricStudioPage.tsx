import{useState}from'react';import{Link}from'react-router-dom';import{MasteryChallengeDialog}from'../components/MasteryChallengeDialog';import{PHOTO_DEFAULTS,photoSettings,photoSolution,METALS,type PhotoInput}from'../lib/photoelectricStudio';import'../friction-studio.css';
function C({label,value,min,max,onChange,unit}:{label:string;value:number;min:number;max:number;onChange:(v:number)=>void;unit:string}){return <label className="frs-control"><span>{label}<input aria-label={label+' value'} type="number" value={value} onChange={e=>onChange(Number(e.target.value))}/><em>{unit}</em></span><input aria-label={label} type="range" min={min} max={max} step=".01" value={value} onChange={e=>onChange(Number(e.target.value))}/><small><i>{min}</i><i>{max}</i></small></label>}
export default function PhotoelectricStudioPage(){const[input,setInput]=useState(PHOTO_DEFAULTS),[panel,setPanel]=useState<string|null>(null);const s=photoSolution(input);const update=(p:Partial<PhotoInput>)=>setInput(photoSettings({...input,...p}));return <div className="frs-page tension-page" data-ui-theme="dark"><aside className="frs-side"><Link className="frs-brand" to="/concept-studio">◌ MODERN PHYSICS</Link><nav><Link to="/concept-studio">⌂ Home</Link><Link to="/modern-physics/quantum-ideas">Quantum Ideas</Link><Link to="/modern-physics/photoelectric-effect">Photoelectric Effect</Link></nav></aside><main id="content" className="frs-main"><header className="frs-head"><h1>MODERN PHYSICS　›　<span>PHOTOELECTRIC EFFECT</span></h1><p>Light ejects electrons when its frequency exceeds a threshold.</p></header><div className="frs-modes">{['Observe','Predict','Experiment','Explain'].map(m=><button key={m} onClick={()=>m!=='Observe'&&setPanel(m)}>{m}</button>)}</div><div className="frs-layout"><section className="frs-stage"><svg viewBox="0 0 900 380"><rect width="900" height="380" fill="#0b1b35"/><rect x="170" y="90" width="560" height="190" rx="35" fill="#142a4d" stroke="#b7d7f3" strokeWidth="6"/><rect x="270" y="130" width="18" height="110" fill="#dce7ef"/><rect x="610" y="130" width="18" height="110" fill="#c99f58"/><path d="M50 185 H260" stroke="#805dff" strokeWidth="18"/><path d="M300 150 Q450 90 600 150" stroke="#57bfff" strokeWidth="4" fill="none" opacity={s.k>0?1:.15}/>{Array.from({length:8},(_,i)=><circle key={i} cx={330+i*35} cy={150+(i%3)*22} r="5" fill="#8ad4ff" opacity={s.k>0?.85:0.1}/>) }<text x="350" y="330" fill="#9ed5ff">Kmax={s.k.toFixed(2)} eV · I={s.current.toFixed(2)} μA</text></svg></section><aside className="frs-right"><section className="frs-panel"><h2>SIMULATION CONTROLS</h2><C label="Light Frequency f" value={input.freq} min={4} max={12} onChange={freq=>update({freq})} unit="×10¹⁴ Hz"/><C label="Light Intensity" value={input.intensity} min={0} max={1} onChange={intensity=>update({intensity})} unit=""/><label>Metal (cathode) <select aria-label="Metal" value={input.metal} onChange={e=>update({metal:e.target.value})}>{Object.entries(METALS).map(([k,v])=><option key={k} value={k}>{v.name}</option>)}</select></label></section><section className="frs-panel frs-readings"><h2>LIVE READINGS</h2><p>Threshold Frequency f₀<b>{s.threshold.toFixed(1)} ×10¹⁴ Hz</b></p><p>Maximum Kinetic Energy<b>{s.k.toFixed(2)} eV</b></p><p>Stopping Potential Vₛ<b>{s.stop.toFixed(2)} V</b></p><p>Photocurrent I<b>{s.current.toFixed(2)} μA</b></p></section><section className="frs-panel frs-equation"><h2>PHOTOELECTRIC EQUATION</h2><b>K<sub>max</sub> = hf − ϕ</b><p>Frequency controls electron energy; intensity controls the number emitted.</p></section></aside></div><div className="frs-actions"><button onClick={()=>setInput(PHOTO_DEFAULTS)}>⟳ Reset</button><button className="frs-next" onClick={()=>setPanel('Challenge')}>Next Challenge　→</button></div><footer>MODERN PHYSICS　|　PHOTOELECTRIC EFFECT</footer></main>{panel&&<MasteryChallengeDialog title={panel} close={()=>setPanel(null)}><p>Raise frequency above the threshold to eject electrons.</p></MasteryChallengeDialog>}</div>}
