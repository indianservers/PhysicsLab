import{useState}from'react';import{Link}from'react-router-dom';import{MasteryChallengeDialog}from'../components/MasteryChallengeDialog';import{SPECTRA_DEFAULTS,spectraSettings,spectraSolution,type SpectraInput}from'../lib/atomicSpectraStudio';import'../friction-studio.css';
export default function AtomicSpectraStudioPage(){const[input,setInput]=useState(SPECTRA_DEFAULTS),[panel,setPanel]=useState<string|null>(null);const s=spectraSolution(input);const update=(p:Partial<SpectraInput>)=>setInput(spectraSettings({...input,...p}));return <div className="frs-page tension-page" data-ui-theme="dark"><aside className="frs-side"><Link className="frs-brand" to="/concept-studio">◌ MODERN PHYSICS</Link><nav><Link to="/concept-studio">⌂ Home</Link><Link to="/modern-physics/matter-waves">Matter Waves</Link><Link to="/modern-physics/atomic-spectra">Atomic Spectra</Link></nav></aside><main id="content" className="frs-main"><header className="frs-head"><h1>MODERN PHYSICS　›　<span>ATOMIC SPECTRA & MODELS</span></h1><p>Hydrogen emission lines reveal quantized energy levels.</p></header><div className="frs-modes">{['Observe','Predict','Experiment','Explain'].map(m=><button key={m} onClick={()=>m!=='Observe'&&setPanel(m)}>{m}</button>)}</div><div className="frs-layout"><section className="frs-stage"><svg viewBox="0 0 900 380"><rect width="900" height="380" fill="#08182c"/><circle cx="360" cy="190" r="38" fill="#da72ff" opacity=".85"/>{[70,120,170].map(r=><circle key={r} cx="360" cy="190" r={r} fill="none" stroke="#4a91ff" strokeWidth="3"/>)}<circle cx="360" cy={190-170} r="9" fill="#bdeaff"/><path d="M360 20 Q460 95 430 170" stroke="#80deff" strokeWidth="4" fill="none" markerEnd="url(#arr)"/><rect x="610" y="70" width="210" height="240" fill="#020409" stroke="#91b8db" strokeWidth="4"/>{[410,434,486,656].map((x,i)=><line key={i} x1={x} y1="85" x2={x} y2="295" stroke={i===3?'#ff4d42':i===2?'#4edbff':'#695bff'} strokeWidth="5"/>)}<text x="250" y="350" fill="#9ed5ff">n={input.ni} → {input.nf} · λ={s.lambda.toFixed(1)} nm · E={s.energy.toFixed(2)} eV</text><defs><marker id="arr" markerWidth="8" markerHeight="8" refX="4" refY="4" orient="auto"><path d="M0 0L8 4L0 8z" fill="#80deff"/></marker></defs></svg></section><aside className="frs-right"><section className="frs-panel"><h2>ATOM & TRANSITION</h2><label>Element <select aria-label="Element" value={input.element} onChange={e=>update({element:e.target.value})}><option value="hydrogen">Hydrogen (H)</option><option value="helium">Helium (He)</option></select></label><label>Initial Level nᵢ <select aria-label="Initial Level" value={input.ni} onChange={e=>update({ni:Number(e.target.value)})}>{[2,3,4,5,6].map(n=><option key={n}>{n}</option>)}</select></label><label>Transition nᶠ <select aria-label="Transition Level" value={input.nf} onChange={e=>update({nf:Number(e.target.value)})}>{[1,2,3,4,5].map(n=><option key={n}>{n}</option>)}</select></label></section><section className="frs-panel frs-readings"><h2>LIVE READINGS</h2><p>Wavelength λ<b>{s.lambda.toFixed(1)} nm</b></p><p>Photon Energy E<b>{s.energy.toFixed(2)} eV</b></p><p>Spectral Series<b>{s.series}</b></p><p>Transition<b>n={input.ni} → n={input.nf}</b></p></section><section className="frs-panel frs-equation"><h2>EQUATION</h2><b>E = hc/λ</b><p>Rydberg transitions produce discrete spectral lines.</p></section></aside></div><div className="frs-actions"><button onClick={()=>setInput(SPECTRA_DEFAULTS)}>⟳ Reset</button><button className="frs-next" onClick={()=>setPanel('Challenge')}>Next Challenge　→</button></div><footer>MODERN PHYSICS　|　ATOMIC SPECTRA</footer></main>{panel&&<MasteryChallengeDialog title={panel} close={()=>setPanel(null)}><p>Choose a transition and identify the spectral series.</p></MasteryChallengeDialog>}</div>}
