import{useState}from'react';import{Link}from'react-router-dom';import{MasteryChallengeDialog}from'../components/MasteryChallengeDialog';import{POTENTIAL_DEFAULTS,potentialSettings,potentialSolution,type PotentialInput}from'../lib/potentialGravityStudio';import'../friction-studio.css';function C({label,value,min,max,unit,onChange}:{label:string;value:number;min:number;max:number;unit:string;onChange:(v:number)=>void}){return <label className="frs-control"><span>{label}<input aria-label={label+' value'} type="number" value={value.toExponential(2)} onChange={e=>onChange(Number(e.target.value))}/><em>{unit}</em></span><input aria-label={label} type="range" min={min} max={max} step={(max-min)/100} value={value} onChange={e=>onChange(Number(e.target.value))}/><small><i>{min.toExponential(0)}</i><i>{max.toExponential(0)}</i></small></label>}export default function GravitationalPotentialStudioPage(){const[input,setInput]=useState(POTENTIAL_DEFAULTS),[panel,setPanel]=useState<string|null>(null);const s=potentialSolution(input);const update=(p:Partial<PotentialInput>)=>setInput(potentialSettings({...input,...p}));return <div className="frs-page tension-page" data-ui-theme="dark"><aside className="frs-side"><Link className="frs-brand" to="/concept-studio">⚛ PHYSICS STUDIO</Link><nav><Link to="/concept-studio">⌂ Home</Link><Link to="/motion/universal-gravity">Universal Gravitation</Link><Link to="/motion/gravitational-field">Gravitational Field</Link><Link to="/motion/gravitational-potential">Gravitational Potential</Link><Link to="/motion/orbits">Orbits</Link><Link to="/motion/escape-velocity">Escape Velocity</Link></nav></aside><main id="content" className="frs-main"><header className="frs-head"><h1>GRAVITATION　›　<span>GRAVITATIONAL POTENTIAL</span></h1><p>Explore the gravitational potential field of a planet–moon system.</p></header><div className="frs-modes">{['Observe','Predict','Experiment','Explain'].map(m=><button key={m} onClick={()=>m!=='Observe'&&setPanel(m)}>{m}</button>)}</div><div className="frs-layout"><section className="frs-stage"><img src="/assets/friction-studio/field-earth.png" alt="Planet and moon potential surface"/><svg viewBox="0 0 900 380"><ellipse cx="410" cy="250" rx="260" ry="70" fill="none" stroke="#22c9ff" strokeWidth="3" opacity=".8"/><ellipse cx="410" cy="250" rx="180" ry="48" fill="none" stroke="#a45cff" strokeWidth="3"/><circle cx="410" cy="220" r="72" fill="#245eaa" opacity=".8"/><circle cx="720" cy="235" r="28" fill="#aaa"/><circle cx="590" cy="130" r="7" fill="#fff"/><text x="600" y="125" fill="white">Probe</text><text x="480" y="90" fill="#9de5ff">V = {s.V.toExponential(3)} J/kg</text></svg></section><aside className="frs-right"><section className="frs-panel"><h2>CONTROLS</h2><C label="Planet Mass" value={input.planet} min={1e23} max={1e26} unit="kg" onChange={planet=>update({planet})}/><C label="Moon Mass" value={input.moon} min={1e21} max={1e24} unit="kg" onChange={moon=>update({moon})}/><C label="Probe Radius" value={input.radius} min={1e6} max={3e8} unit="m" onChange={radius=>update({radius})}/><C label="Orbital Angle" value={input.angle} min={0} max={180} unit="°" onChange={angle=>update({angle})}/></section><section className="frs-panel frs-readings"><h2>LIVE READINGS</h2><p>Gravitational potential V<b>{s.V.toExponential(3)} J/kg</b></p><p>Potential energy U (m=1000kg)<b>{s.energy.toExponential(3)} J</b></p><p>Potential gradient |∇V|<b>{s.gradient.toFixed(2)} m/s²</b></p></section></aside><section className="frs-equation frs-panel"><h2>KEY EQUATION</h2><b>V(r) = −G Σ Mᵢ / |r−rᵢ|</b><p>Potential is the work per unit mass required to bring a probe from infinity.</p></section></div><div className="frs-actions"><button onClick={()=>setInput(POTENTIAL_DEFAULTS)}>⟳ Reset</button><button className="frs-next" onClick={()=>setPanel('Challenge')}>Next Challenge　→</button></div><footer>GRAVITATION　|　GRAVITATIONAL POTENTIAL</footer></main>{panel&&<MasteryChallengeDialog title={panel} close={()=>setPanel(null)}><p>Compare potential as the probe moves farther from the planet.</p></MasteryChallengeDialog>}</div>}
