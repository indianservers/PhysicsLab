import{useState}from'react';import{Link}from'react-router-dom';import{MasteryChallengeDialog}from'../components/MasteryChallengeDialog';import{NUCLEAR_DEFAULTS,nuclearSettings,nuclearSolution,type NuclearInput}from'../lib/nuclearStructureStudio';import'../friction-studio.css';
function C({label,value,min,max,onChange}:{label:string;value:number;min:number;max:number;onChange:(v:number)=>void}){return <label className="frs-control"><span>{label}<input aria-label={label+' value'} type="number" value={value} onChange={e=>onChange(Number(e.target.value))}/></span><input aria-label={label} type="range" min={min} max={max} step="1" value={value} onChange={e=>onChange(Number(e.target.value))}/><small><i>{min}</i><i>{max}</i></small></label>}
export default function NuclearStructureStudioPage(){const[input,setInput]=useState(NUCLEAR_DEFAULTS),[panel,setPanel]=useState<string|null>(null);const s=nuclearSolution(input);const update=(p:Partial<NuclearInput>)=>setInput(nuclearSettings({...input,...p}));const balls=Array.from({length:Math.min(60,s.A)},(_,i)=>{const a=i*2.399;const r=70+((i*37)%110);return <circle key={i} cx={360+Math.cos(a)*r} cy={190+Math.sin(a)*r} r="16" fill={i%2?'#536fe0':'#d95d6c'} stroke="#b8d7ff" strokeWidth="1"/>});return <div className="frs-page tension-page" data-ui-theme="dark"><aside className="frs-side"><Link className="frs-brand" to="/concept-studio">◌ MODERN PHYSICS</Link><nav><Link to="/concept-studio">⌂ Home</Link><Link to="/modern-physics/atomic-spectra">Atomic Spectra</Link><Link to="/modern-physics/nuclear-structure">Nuclear Structure</Link></nav></aside><main id="content" className="frs-main"><header className="frs-head"><h1>MODERN PHYSICS　›　<span>NUCLEAR STRUCTURE</span></h1><p>Explore nuclear composition, stability, and binding energy.</p></header><div className="frs-modes">{['Observe','Predict','Experiment','Explain'].map(m=><button key={m} onClick={()=>m!=='Observe'&&setPanel(m)}>{m}</button>)}</div><div className="frs-layout"><section className="frs-stage"><svg viewBox="0 0 900 380"><rect width="900" height="380" fill="#081a33"/><circle cx="360" cy="190" r="145" fill="#102c54" stroke="#69a9ff" strokeWidth="4"/>{balls}<text x="300" y="350" fill="#9ed5ff">{s.A} nucleons · Z={input.z} · N={input.n}</text></svg></section><aside className="frs-right"><section className="frs-panel"><h2>CONTROLS</h2><C label="Proton Number Z" value={input.z} min={1} max={118} onChange={z=>update({z})}/><C label="Neutron Number N" value={input.n} min={0} max={200} onChange={n=>update({n})}/><label>Nuclear Model <select aria-label="Nuclear Model"><option>Semi-empirical mass formula</option><option>Liquid drop model</option></select></label></section><section className="frs-panel frs-readings"><h2>NUCLEAR PROPERTIES</h2><p>Mass Number A<b>{s.A}</b></p><p>Binding Energy Eᵦ<b>{s.be.toFixed(1)} MeV</b></p><p>Binding / Nucleon<b>{s.per.toFixed(2)} MeV</b></p><p>Mass Defect Δm<b>{s.massDefect.toFixed(3)} u</b></p><p>Stability<b>{s.stable?'Stable':'Unstable'}</b></p></section><section className="frs-panel frs-equation"><h2>MASS DEFECT & BINDING</h2><b>Δm = Zmₚ + Nmₙ − M(A,Z)</b><p>Eᵦ = Δmc²; the semi-empirical formula estimates nuclear cohesion.</p></section></aside></div><div className="frs-actions"><button onClick={()=>setInput(NUCLEAR_DEFAULTS)}>⟳ Reset</button><button className="frs-next" onClick={()=>setPanel('Challenge')}>Next Challenge　→</button></div><footer>MODERN PHYSICS　|　NUCLEAR STRUCTURE</footer></main>{panel&&<MasteryChallengeDialog title={panel} close={()=>setPanel(null)}><p>Adjust neutron number and explore nuclear stability.</p></MasteryChallengeDialog>}</div>}
