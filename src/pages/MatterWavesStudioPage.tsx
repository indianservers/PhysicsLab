import{useState}from'react';import{Link}from'react-router-dom';import{MasteryChallengeDialog}from'../components/MasteryChallengeDialog';import{MATTER_DEFAULTS,matterSettings,matterSolution,type MatterInput}from'../lib/matterWavesStudio';import'../friction-studio.css';
function C({label,value,min,max,onChange,unit}:{label:string;value:number;min:number;max:number;onChange:(v:number)=>void;unit:string}){return <label className="frs-control"><span>{label}<input aria-label={label+' value'} type="number" value={value} onChange={e=>onChange(Number(e.target.value))}/><em>{unit}</em></span><input aria-label={label} type="range" min={min} max={max} step=".01" value={value} onChange={e=>onChange(Number(e.target.value))}/><small><i>{min}</i><i>{max}</i></small></label>}
export default function MatterWavesStudioPage(){const[input,setInput]=useState(MATTER_DEFAULTS),[panel,setPanel]=useState<string|null>(null);const s=matterSolution(input);const update=(p:Partial<MatterInput>)=>setInput(matterSettings({...input,...p}));return <div className="frs-page tension-page" data-ui-theme="dark"><aside className="frs-side"><Link className="frs-brand" to="/concept-studio">◌ MODERN PHYSICS</Link><nav><Link to="/concept-studio">⌂ Home</Link><Link to="/modern-physics/photoelectric-effect">Photoelectric Effect</Link><Link to="/modern-physics/matter-waves">Matter Waves</Link></nav></aside><main id="content" className="frs-main"><header className="frs-head"><h1>MODERN PHYSICS　›　<span>MATTER WAVES</span></h1><p>Electron diffraction from a crystal reveals wave–particle duality.</p></header><div className="frs-modes">{['Observe','Predict','Experiment','Explain'].map(m=><button key={m} onClick={()=>m!=='Observe'&&setPanel(m)}>{m}</button>)}</div><div className="frs-layout"><section className="frs-stage"><svg viewBox="0 0 900 380"><rect width="900" height="380" fill="#0a1c32"/><rect x="160" y="120" width="80" height="140" fill="#738394"/><rect x="400" y="80" width="80" height="220" fill="#9bb3cb" opacity=".8"/><circle cx="700" cy="190" r="110" fill="#122d55" stroke="#8ec8ff" strokeWidth="6"/>{[25,50,75].map((r,i)=><circle key={i} cx="700" cy="190" r={r} fill="none" stroke="#7ebdff" strokeWidth="5" opacity=".8"/>)}<path d="M60 190 H400 M480 190 H590" stroke="#63c8ff" strokeWidth="5"/><text x="240" y="345" fill="#9ed5ff">λ={s.lambda.toFixed(5)} nm · θ₁={s.angle.toFixed(1)}°</text></svg></section><aside className="frs-right"><section className="frs-panel"><h2>CONTROLS</h2><C label="Particle Energy" value={input.energy} min={20} max={300} onChange={energy=>update({energy})} unit="keV"/><C label="Crystal Spacing" value={input.spacing} min={.1} max={.4} onChange={spacing=>update({spacing})} unit="nm"/><C label="Beam Width" value={input.width} min={.1} max={2} onChange={width=>update({width})} unit="mm"/></section><section className="frs-panel frs-readings"><h2>LIVE READINGS</h2><p>de Broglie Wavelength λ<b>{s.lambda.toFixed(5)} nm</b></p><p>First Order Angle θ₁<b>{s.angle.toFixed(1)}°</b></p><p>Ring Radius R₁<b>{s.radius.toFixed(2)} cm</b></p></section><section className="frs-panel frs-equation"><h2>KEY EQUATION</h2><b>λ = h / √(2m<sub>e</sub>E)</b><p>Bragg diffraction links crystal spacing to the electron wavelength.</p></section></aside></div><div className="frs-actions"><button onClick={()=>setInput(MATTER_DEFAULTS)}>⟳ Reset</button><button className="frs-next" onClick={()=>setPanel('Challenge')}>Next Challenge　→</button></div><footer>MODERN PHYSICS　|　MATTER WAVES</footer></main>{panel&&<MasteryChallengeDialog title={panel} close={()=>setPanel(null)}><p>Increase particle energy and observe the wavelength shrink.</p></MasteryChallengeDialog>}</div>}
