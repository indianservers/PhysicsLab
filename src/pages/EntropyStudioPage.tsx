import{useState}from'react';import{Link}from'react-router-dom';import{MasteryChallengeDialog}from'../components/MasteryChallengeDialog';import{ENTROPY_DEFAULTS,entropySettings,entropySolution,type EntropyInput}from'../lib/entropyStudio';import'../friction-studio.css';
function C({label,value,min,max,step,onChange,unit}:{label:string;value:number;min:number;max:number;step:number;onChange:(v:number)=>void;unit:string}){return <label className="frs-control"><span>{label}<input aria-label={label+' value'} type="number" value={value} onChange={e=>onChange(Number(e.target.value))}/><em>{unit}</em></span><input aria-label={label} type="range" min={min} max={max} step={step} value={value} onChange={e=>onChange(Number(e.target.value))}/><small><i>{min}</i><i>{max}</i></small></label>}
export default function EntropyStudioPage(){const[input,setInput]=useState(ENTROPY_DEFAULTS),[panel,setPanel]=useState<string|null>(null);const s=entropySolution(input);const update=(p:Partial<EntropyInput>)=>setInput(entropySettings({...input,...p}));return <div className="frs-page tension-page" data-ui-theme="dark"><aside className="frs-side"><Link className="frs-brand" to="/concept-studio">♨ THERMODYNAMICS</Link><nav><Link to="/concept-studio">⌂ Home</Link><Link to="/thermodynamics/first-law">First Law</Link><Link to="/thermodynamics/entropy">Entropy</Link><Link to="/thermodynamics/heat-engines">Heat Engines</Link></nav></aside><main id="content" className="frs-main"><header className="frs-head"><h1>THERMODYNAMICS　›　<span>ENTROPY — TWO-CHAMBER GAS MIXING</span></h1><p>Remove the divider and watch the system evolve toward higher entropy.</p></header><div className="frs-modes">{['Observe','Predict','Experiment','Explain'].map(m=><button key={m} onClick={()=>m!=='Observe'&&setPanel(m)}>{m}</button>)}</div><div className="frs-layout"><section className="frs-stage"><svg viewBox="0 0 900 380"><rect width="900" height="380" fill="#10253a"/><rect x="85" y="70" width="730" height="250" rx="20" fill="#16354d" stroke="#8eb9df" strokeWidth="5"/><rect x="445" y="70" width="8" height="250" fill={input.mixed?'#28506b':'#d4e5f2'}/>{Array.from({length:34},(_,i)=>{const left=!input.mixed&&i<17||input.mixed&&i%2===0;const x=left?110+(i*37)%300:490+(i*41)%290;const y=105+(i*53)%190;return <circle key={i} cx={x} cy={y} r="6" fill={i%2?'#67b7ff':'#e36eff'}/>})}<text x="130" y="55" fill="white">T₁={s.t1} K · N₁={s.N1}</text><text x="590" y="55" fill="white">T₂={s.t2} K · N₂={s.N2}</text><button/><foreignObject x="360" y="330" width="180" height="40"><button className="frs-next" onClick={()=>update({mixed:!input.mixed})}>{input.mixed?'Reset Divider':'Remove Divider'}</button></foreignObject></svg></section><aside className="frs-right"><section className="frs-panel"><h2>CONTROLS</h2><C label="Particle Count N" value={input.count} min={100} max={2000} step={1} unit="particles" onChange={count=>update({count})}/><C label="Initial Partition" value={input.partition} min={0} max={1} step={.01} unit="fraction" onChange={partition=>update({partition})}/><C label="Temperature Difference" value={input.deltaT} min={0} max={1000} step={1} unit="K" onChange={deltaT=>update({deltaT})}/></section><section className="frs-panel frs-readings"><h2>LIVE READINGS</h2><p>N₁<b>{s.N1}</b></p><p>N₂<b>{s.N2}</b></p><p>T₁<b>{s.t1} K</b></p><p>T₂<b>{s.t2} K</b></p><p>ΔS<b>{s.S.toFixed(1)} J/K</b></p></section><section className="frs-panel frs-equation"><h2>FORMULAS</h2><b>ΔS = 2Nk<sub>B</sub> ln 2</b><p>Mixing increases the number of accessible microstates.</p></section></aside></div><div className="frs-actions"><button onClick={()=>setInput(ENTROPY_DEFAULTS)}>⟳ Reset</button><button className="frs-next" onClick={()=>setPanel('Challenge')}>Next Challenge　→</button></div><footer>THERMODYNAMICS　|　ENTROPY</footer></main>{panel&&<MasteryChallengeDialog title={panel} close={()=>setPanel(null)}><p>Predict how entropy changes when the divider is removed.</p></MasteryChallengeDialog>}</div>}
