import{useState}from'react';import{Link}from'react-router-dom';import{MasteryChallengeDialog}from'../components/MasteryChallengeDialog';import{COSMO_DEFAULTS,cosmoSettings,cosmoSolution,type CosmoInput}from'../lib/cosmologyStudio';import'../friction-studio.css';
function C({label,value,min,max,step,onChange,unit}:{label:string;value:number;min:number;max:number;step:number;onChange:(v:number)=>void;unit:string}){return <label className="frs-control"><span>{label}<input aria-label={label+' value'} type="number" value={value} onChange={e=>onChange(Number(e.target.value))}/><em>{unit}</em></span><input aria-label={label} type="range" min={min} max={max} step={step} value={value} onChange={e=>onChange(Number(e.target.value))}/><small><i>{min}</i><i>{max}</i></small></label>}
export default function CosmologyStudioPage(){const[input,setInput]=useState(COSMO_DEFAULTS),[panel,setPanel]=useState<string|null>(null);const s=cosmoSolution(input);const update=(p:Partial<CosmoInput>)=>setInput(cosmoSettings({...input,...p}));return <div className="frs-page tension-page" data-ui-theme="dark"><aside className="frs-side"><Link className="frs-brand" to="/concept-studio">◌ ASTROPHYSICS</Link><nav><Link to="/concept-studio">⌂ Home</Link><Link to="/astrophysics/galaxies">Galaxies</Link><Link to="/astrophysics/cosmology">Cosmology</Link></nav></aside><main id="content" className="frs-main"><header className="frs-head"><h1>ASTROPHYSICS　›　<span>THE EXPANDING UNIVERSE</span></h1><p>From the early universe to today, explore how space and galaxies evolve.</p></header><div className="frs-modes">{['Observe','Predict','Experiment','Explain'].map(m=><button key={m} onClick={()=>m!=='Observe'&&setPanel(m)}>{m}</button>)}</div><div className="frs-layout"><section className="frs-stage"><svg viewBox="0 0 900 380"><rect width="900" height="380" fill="#050f25"/><path d="M120 190 Q480 100 820 80" stroke="#8ec8ff" strokeWidth="4" fill="none"/><path d="M120 190 Q480 280 820 300" stroke="#8ec8ff" strokeWidth="4" fill="none"/><circle cx="120" cy="190" r="12" fill="#fff"/><circle cx="760" cy="115" r="10" fill="#ffb96b"/><circle cx="650" cy="145" r="7" fill="#bcd8ff"/><circle cx="560" cy="170" r="6" fill="#ffc4e0"/><text x="100" y="245" fill="white">Big Bang</text><text x="610" y="350" fill="#9ed5ff">z={s.z.toFixed(2)} · lookback={s.look.toFixed(1)} Gyr · v={s.velocity.toFixed(0)} km/s</text></svg></section><aside className="frs-right"><section className="frs-panel"><h2>SIMULATION CONTROLS</h2><C label="Epoch" value={input.epoch} min={1e6} max={13.8e9} step={1e6} onChange={epoch=>update({epoch})} unit="years"/><C label="Expansion Rate H₀" value={input.h0} min={50} max={100} step={1} onChange={h0=>update({h0})} unit="km/s/Mpc"/><C label="Matter Density Ωm" value={input.omega} min={0} max={1} step={.01} onChange={omega=>update({omega})} unit=""/></section><section className="frs-panel frs-readings"><h2>LIVE READINGS</h2><p>Redshift z<b>{s.z.toFixed(2)}</b></p><p>Lookback Time<b>{s.look.toFixed(1)} billion years</b></p><p>Present Distance<b>{s.dist.toFixed(1)} billion ly</b></p><p>Recession Velocity<b>{s.velocity.toFixed(0)} km/s</b></p></section><section className="frs-panel frs-equation"><h2>HUBBLE’S LAW</h2><b>v = H₀d</b><p>As space expands, distant galaxies recede faster.</p></section></aside></div><div className="frs-actions"><button onClick={()=>setInput(COSMO_DEFAULTS)}>⟳ Reset</button><button className="frs-next" onClick={()=>setPanel('Challenge')}>Next Challenge　→</button></div><footer>ASTROPHYSICS　|　COSMOLOGY</footer></main>{panel&&<MasteryChallengeDialog title={panel} close={()=>setPanel(null)}><p>Move the epoch toward the early universe and observe redshift grow.</p></MasteryChallengeDialog>}</div>}
