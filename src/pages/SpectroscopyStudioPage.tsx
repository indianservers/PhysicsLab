import{useState}from'react';import{Link}from'react-router-dom';import{MasteryChallengeDialog}from'../components/MasteryChallengeDialog';import{SPECTRO_DEFAULTS,spectroSettings,spectroSolution,STARS,type SpectroInput}from'../lib/spectroscopyStudio';import'../friction-studio.css';
export default function SpectroscopyStudioPage(){const[input,setInput]=useState(SPECTRO_DEFAULTS),[panel,setPanel]=useState<string|null>(null);const s=spectroSolution(input);const update=(p:Partial<SpectroInput>)=>setInput(spectroSettings({...input,...p}));return <div className="frs-page tension-page" data-ui-theme="dark"><aside className="frs-side"><Link className="frs-brand" to="/concept-studio">◌ ASTROPHYSICS</Link><nav><Link to="/concept-studio">⌂ Home</Link><Link to="/astrophysics/stellar-life">Stellar Life</Link><Link to="/astrophysics/spectroscopy">Spectroscopy</Link></nav></aside><main id="content" className="frs-main"><header className="frs-head"><h1>ASTROPHYSICS　›　<span>STELLAR SPECTROSCOPY</span></h1><p>Analyze starlight, identify elements, and measure the cosmos.</p></header><div className="frs-modes">{['Observe','Predict','Experiment','Explain'].map(m=><button key={m} onClick={()=>m!=='Observe'&&setPanel(m)}>{m}</button>)}</div><div className="frs-layout"><section className="frs-stage"><svg viewBox="0 0 900 380"><rect width="900" height="380" fill="#07172d"/><circle cx="250" cy="190" r="80" fill="#bfe7ff" opacity=".85"/><path d="M330 190 H570" stroke="#85cfff" strokeWidth="7"/><rect x="600" y="80" width="220" height="220" fill="#02060d" stroke="#89b8dc" strokeWidth="5"/>{[630,660,690,720,760].map((x,i)=><line key={x} x1={x} y1="100" x2={x} y2="280" stroke={i%2?'#6a61ff':'#46c9ff'} strokeWidth="5"/>)}<text x="300" y="350" fill="#9ed5ff">{s.name} · T={s.temp} K · vᵣ={s.velocity.toFixed(1)} km/s</text></svg></section><aside className="frs-right"><section className="frs-panel"><h2>CONTROLS</h2><label>Target Star <select aria-label="Target Star" value={input.star} onChange={e=>update({star:e.target.value})}>{Object.entries(STARS).map(([k,v])=><option key={k} value={k}>{v.name}</option>)}</select></label><label className="frs-control"><span>Spectral Range<input aria-label="Spectral Range value" type="number" value={input.range} onChange={e=>update({range:Number(e.target.value)})}/><em>nm</em></span><input aria-label="Spectral Range" type="range" min="350" max="750" step="1" value={input.range} onChange={e=>update({range:Number(e.target.value)})}/><small><i>350</i><i>750</i></small></label><label className="frs-control"><span>Redshift z<input aria-label="Redshift z value" type="number" value={input.redshift} onChange={e=>update({redshift:Number(e.target.value)})}/><em></em></span><input aria-label="Redshift z" type="range" min="-.01" max=".01" step=".00001" value={input.redshift} onChange={e=>update({redshift:Number(e.target.value)})}/><small><i>−.01</i><i>+.01</i></small></label></section><section className="frs-panel frs-readings"><h2>DERIVED PROPERTIES</h2><p>Effective Temperature<b>{s.temp.toLocaleString()} K</b></p><p>Spectral Type<b>{s.type}</b></p><p>Radial Velocity<b>{s.velocity>=0?'+':''}{s.velocity.toFixed(1)} km/s</b></p><p>Observed Range<b>{input.range} nm</b></p></section><section className="frs-panel frs-equation"><h2>DOPPLER SHIFT</h2><b>z = (λobs − λrest)/λrest ≈ vᵣ/c</b><p>Redshift reveals radial motion along the line of sight.</p></section></aside></div><div className="frs-actions"><button onClick={()=>setInput(SPECTRO_DEFAULTS)}>⟳ Reset</button><button className="frs-next" onClick={()=>setPanel('Challenge')}>Next Challenge　→</button></div><footer>ASTROPHYSICS　|　STELLAR SPECTROSCOPY</footer></main>{panel&&<MasteryChallengeDialog title={panel} close={()=>setPanel(null)}><p>Adjust redshift and infer the star’s radial velocity.</p></MasteryChallengeDialog>}</div>}
