import{useState}from'react';import{Link}from'react-router-dom';import{MasteryChallengeDialog}from'../components/MasteryChallengeDialog';import{RESONANCE_DEFAULTS,resonanceSettings,resonanceSolution,type ResonanceInput}from'../lib/resonanceStudio';import'../friction-studio.css';function C({label,value,min,max,unit,onChange}:{label:string;value:number;min:number;max:number;unit:string;onChange:(v:number)=>void}){return <label className="frs-control"><span>{label}<input aria-label={label+' value'} type="number" value={value.toFixed(2)} onChange={e=>onChange(Number(e.target.value))}/><em>{unit}</em></span><input aria-label={label} type="range" min={min} max={max} step={(max-min)/100} value={value} onChange={e=>onChange(Number(e.target.value))}/><small><i>{min}</i><i>{max}</i></small></label>}export default function ResonanceStudioPage(){const[input,setInput]=useState(RESONANCE_DEFAULTS),[panel,setPanel]=useState<string|null>(null);const s=resonanceSolution(input);const update=(p:Partial<ResonanceInput>)=>setInput(resonanceSettings({...input,...p}));return <div className="frs-page tension-page" data-ui-theme="dark"><aside className="frs-side"><Link className="frs-brand" to="/concept-studio">⚛ PHYSICS STUDIO</Link><nav><Link to="/concept-studio">⌂ Home</Link><Link to="/motion/spring-shm">Spring SHM</Link><Link to="/motion/pendulum">Pendulum</Link><Link to="/motion/energy-exchange">Energy Exchange</Link><Link to="/motion/damping">Damping</Link><Link to="/motion/resonance">Resonance</Link></nav></aside><main id="content" className="frs-main"><header className="frs-head"><h1>OSCILLATIONS　›　<span>RESONANCE</span></h1><p>Explore how steady-state amplitude and phase depend on drive frequency.</p></header><div className="frs-modes">{['Observe','Predict','Experiment','Explain'].map(m=><button key={m} onClick={()=>m!=='Observe'&&setPanel(m)}>{m}</button>)}</div><div className="frs-layout"><section className="frs-stage"><img src="/assets/friction-studio/spring.png" alt="Driven oscillator on shake table"/><svg viewBox="0 0 900 380"><rect x="370" y="170" width="140" height="80" rx="8" fill="#286dc0"/><path d="M80 220 Q180 110 280 220 T480 220 T680 220 T820 220" fill="none" stroke="#2cdcff" strokeWidth="3"/><line x1="460" y1="95" x2="460" y2="320" stroke="#9ccfff" opacity=".5"/><text x="520" y="115" fill="#8ce9ff">A = {(s.amp*100).toFixed(2)} cm</text></svg></section><aside className="frs-right"><section className="frs-panel"><h2>CONTROLS</h2><C label="Drive Frequency f" value={input.frequency} min={.1} max={5} unit="Hz" onChange={frequency=>update({frequency})}/><C label="Drive Amplitude F₀" value={input.drive} min={0} max={5} unit="N" onChange={drive=>update({drive})}/><C label="Damping b" value={input.b} min={0} max={2} unit="N·s/m" onChange={b=>update({b})}/></section><section className="frs-panel frs-readings"><h2>LIVE READINGS</h2><p>Amplitude A<b>{(s.amp*100).toFixed(2)} cm</b></p><p>Phase φ<b>{s.phase.toFixed(1)}°</b></p><p>Resonant frequency fᵣ<b>{s.fr.toFixed(2)} Hz</b></p><p>Quality factor Q<b>{s.Q.toFixed(2)}</b></p></section></aside><section className="frs-equation frs-panel"><h2>GOVERNING EQUATION</h2><b>mẍ + bẋ + kx = F₀ cos(ωt)</b><p>Amplitude peaks near the natural frequency and falls as damping increases.</p></section></div><div className="frs-actions"><button onClick={()=>setInput(RESONANCE_DEFAULTS)}>⟳ Reset</button><button className="frs-next" onClick={()=>setPanel('Challenge')}>Next Challenge　→</button></div><footer>OSCILLATIONS　|　RESONANCE</footer></main>{panel&&<MasteryChallengeDialog title={panel} close={()=>setPanel(null)}><p>Find the drive frequency that produces maximum amplitude.</p></MasteryChallengeDialog>}</div>}
