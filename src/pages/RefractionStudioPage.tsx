import{useState}from'react';import{Link}from'react-router-dom';import{MasteryChallengeDialog}from'../components/MasteryChallengeDialog';import{REFRACTION_DEFAULTS,refractionSettings,refractionSolution,type RefractionInput}from'../lib/refractionStudio';import'../friction-studio.css';function C({label,value,min,max,unit,onChange}:{label:string;value:number;min:number;max:number;unit:string;onChange:(v:number)=>void}){return <label className="frs-control"><span>{label}<input aria-label={label+' value'} type="number" value={value.toFixed(3)} onChange={e=>onChange(Number(e.target.value))}/><em>{unit}</em></span><input aria-label={label} type="range" min={min} max={max} step=".001" value={value} onChange={e=>onChange(Number(e.target.value))}/><small><i>{min}</i><i>{max}</i></small></label>}export default function RefractionStudioPage(){const[input,setInput]=useState(REFRACTION_DEFAULTS),[panel,setPanel]=useState<string|null>(null);const s=refractionSolution(input);const update=(p:Partial<RefractionInput>)=>setInput(refractionSettings({...input,...p}));return <div className="frs-page tension-page" data-ui-theme="dark"><aside className="frs-side"><Link className="frs-brand" to="/concept-studio">◈ OPTICS</Link><nav><Link to="/concept-studio">⌂ Home</Link><Link to="/optics/reflection">Reflection</Link><Link to="/optics/refraction">Refraction</Link><Link to="/optics/lenses">Lenses</Link><Link to="/optics/mirrors">Mirrors</Link><Link to="/optics/interference">Interference</Link><Link to="/optics/diffraction">Diffraction</Link></nav></aside><main id="content" className="frs-main"><header className="frs-head"><h1>OPTICS　›　<span>REFRACTION</span></h1><p>Explore how light bends when it passes between different media.</p></header><div className="frs-modes">{['Observe','Predict','Experiment','Explain'].map(m=><button key={m} onClick={()=>m!=='Observe'&&setPanel(m)}>{m}</button>)}</div><div className="frs-layout"><section className="frs-stage"><img src="/assets/friction-studio/reflection.png" alt="Laser through water and acrylic"/><svg viewBox="0 0 900 380"><polygon points="420,80 560,80 620,280 360,280" fill="#2c6fa6" opacity=".45"/><line x1="180" y1="110" x2="450" y2="180" stroke="#ff3d56" strokeWidth="4"/><line x1="450" y1="180" x2="620" y2="260" stroke="#ff3d56" strokeWidth="4"/><line x1="450" y1="80" x2="450" y2="300" stroke="white" strokeDasharray="8 6"/><text x="200" y="90" fill="#9de5ff">θ₁ = {input.angle.toFixed(1)}°</text><text x="500" y="330" fill="#d8b0ff">θ₂ = {s.refracted.toFixed(1)}°</text></svg></section><aside className="frs-right"><section className="frs-panel"><h2>CONTROLS</h2><C label="Medium A refractive index n₁" value={input.n1} min={1} max={2.5} unit="" onChange={n1=>update({n1})}/><C label="Medium B refractive index n₂" value={input.n2} min={1} max={2.5} unit="" onChange={n2=>update({n2})}/><C label="Incident Angle θ₁" value={input.angle} min={0} max={90} unit="°" onChange={angle=>update({angle})}/></section><section className="frs-panel frs-readings"><h2>LIVE READINGS</h2><p>Incident angle θ₁<b>{input.angle.toFixed(1)}°</b></p><p>Refracted angle θ₂<b>{s.refracted.toFixed(1)}°</b></p><p>Refractive index n₁<b>{input.n1.toFixed(3)}</b></p><p>Refractive index n₂<b>{input.n2.toFixed(3)}</b></p><p>Speed in A / B<b>{(s.speed1/1e8).toFixed(2)} / {(s.speed2/1e8).toFixed(2)} ×10⁸ m/s</b></p></section></aside><section className="frs-equation frs-panel"><h2>KEY RELATIONSHIP</h2><b>n₁ sin θ₁ = n₂ sin θ₂</b><p>Snell’s law predicts the bend in a ray at a boundary.</p></section></div><div className="frs-actions"><button onClick={()=>setInput(REFRACTION_DEFAULTS)}>⟳ Reset</button><button className="frs-next" onClick={()=>setPanel('Challenge')}>Next Challenge　→</button></div><footer>OPTICS　|　REFRACTION</footer></main>{panel&&<MasteryChallengeDialog title={panel} close={()=>setPanel(null)}><p>Change the second medium and observe the refracted angle.</p></MasteryChallengeDialog>}</div>}
