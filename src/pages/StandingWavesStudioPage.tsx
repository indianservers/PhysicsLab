import{useState}from'react';import{Link}from'react-router-dom';import{MasteryChallengeDialog}from'../components/MasteryChallengeDialog';import{STANDING_DEFAULTS,standingSettings,standingSolution,type StandingInput}from'../lib/standingStudio';import'../friction-studio.css';function C({label,value,min,max,unit,onChange,step}:{label:string;value:number;min:number;max:number;unit:string;onChange:(v:number)=>void;step?:number}){return <label className="frs-control"><span>{label}<input aria-label={label+' value'} type="number" value={value.toFixed(2)} onChange={e=>onChange(Number(e.target.value))}/><em>{unit}</em></span><input aria-label={label} type="range" min={min} max={max} step={step??(max-min)/100} value={value} onChange={e=>onChange(Number(e.target.value))}/><small><i>{min}</i><i>{max}</i></small></label>}export default function StandingWavesStudioPage(){const[input,setInput]=useState(STANDING_DEFAULTS),[panel,setPanel]=useState<string|null>(null);const s=standingSolution(input);const update=(p:Partial<StandingInput>)=>setInput(standingSettings({...input,...p}));return <div className="frs-page tension-page" data-ui-theme="dark"><aside className="frs-side"><Link className="frs-brand" to="/concept-studio">〰 WAVES & SOUND</Link><nav><Link to="/concept-studio">⌂ Home</Link><Link to="/motion/wave-properties">Wave Properties</Link><Link to="/motion/superposition">Superposition</Link><Link to="/motion/interference">Interference</Link><Link to="/motion/standing-waves">Standing Waves</Link><Link to="/motion/sound-spectrum">Sound Spectrum</Link><Link to="/motion/doppler-effect">Doppler Effect</Link></nav></aside><main id="content" className="frs-main"><header className="frs-head"><h1>WAVES & SOUND　›　<span>STANDING WAVES</span></h1><p>Explore nodes, antinodes, and harmonics on a driven string.</p></header><div className="frs-modes">{['Observe','Predict','Experiment','Explain'].map(m=><button key={m} onClick={()=>m!=='Observe'&&setPanel(m)}>{m}</button>)}</div><div className="frs-layout"><section className="frs-stage"><img src="/assets/friction-studio/wave-machine.png" alt="Standing wave string apparatus"/><svg viewBox="0 0 900 380"><path d="M80 210 Q150 60 220 210 T360 210 T500 210 T640 210 T780 210" fill="none" stroke="#54cfff" strokeWidth="4"/><path d="M80 210 Q150 360 220 210 T360 210 T500 210 T640 210 T780 210" fill="none" stroke="#a96dff" strokeDasharray="8 6"/><text x="620" y="90" fill="#9de5ff">n = {input.harmonic} harmonic</text></svg></section><aside className="frs-right"><section className="frs-panel"><h2>CONTROLS</h2><C label="String Tension T" value={input.tension} min={5} max={50} unit="N" onChange={tension=>update({tension})}/><C label="String Length L" value={input.length} min={.2} max={2} unit="m" onChange={length=>update({length})}/><C label="Harmonic n" value={input.harmonic} min={1} max={5} unit="" step={1} onChange={harmonic=>update({harmonic})}/><C label="Time t" value={input.time} min={0} max={10} unit="s" onChange={time=>update({time})}/></section><section className="frs-panel frs-readings"><h2>LIVE READINGS</h2><p>Frequency fₙ<b>{s.freq.toFixed(1)} Hz</b></p><p>Nodes (including ends)<b>{s.nodes}</b></p><p>Wavelength λ<b>{s.lambda.toFixed(3)} m</b></p><p>Wave Speed v<b>{s.velocity.toFixed(1)} m/s</b></p></section></aside><section className="frs-equation frs-panel"><h2>STANDING WAVE ANALYSIS</h2><b>fₙ = n v / 2L = n / 2L √(T/μ)</b><p>Nodes remain fixed while antinodes oscillate between them.</p></section></div><div className="frs-actions"><button onClick={()=>setInput(STANDING_DEFAULTS)}>⟳ Reset</button><button className="frs-next" onClick={()=>setPanel('Challenge')}>Next Challenge　→</button></div><footer>WAVES & SOUND　|　STANDING WAVES</footer></main>{panel&&<MasteryChallengeDialog title={panel} close={()=>setPanel(null)}><p>How does the harmonic number change frequency?</p></MasteryChallengeDialog>}</div>}
