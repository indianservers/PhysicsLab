import{useState}from'react';import{Link}from'react-router-dom';import{MasteryChallengeDialog}from'../components/MasteryChallengeDialog';import{AMP_DEFAULTS,ampSettings,ampSolution,type AmplifierInput}from'../lib/amplifierStudio';import'../friction-studio.css';function C({label,value,min,max,unit,onChange}:{label:string;value:number;min:number;max:number;unit:string;onChange:(v:number)=>void}){return <label className="frs-control"><span>{label}<input aria-label={label+' value'} type="number" value={value.toFixed(2)} onChange={e=>onChange(Number(e.target.value))}/><em>{unit}</em></span><input aria-label={label} type="range" min={min} max={max} step=".01" value={value} onChange={e=>onChange(Number(e.target.value))}/><small><i>{min}</i><i>{max}</i></small></label>}export default function AmplifiersStudioPage(){const[input,setInput]=useState(AMP_DEFAULTS),[panel,setPanel]=useState<string|null>(null);const s=ampSolution(input);const update=(p:Partial<AmplifierInput>)=>setInput(ampSettings({...input,...p}));return <div className="frs-page tension-page" data-ui-theme="dark"><aside className="frs-side"><Link className="frs-brand" to="/concept-studio">▣ ELECTRONICS</Link><nav><Link to="/concept-studio">⌂ Home</Link><Link to="/electronics/transistors">Transistors</Link><Link to="/electronics/amplifiers">Amplifiers</Link><Link to="/electronics/logic-gates">Logic Gates</Link></nav></aside><main id="content" className="frs-main"><header className="frs-head"><h1>ELECTRONICS　›　<span>COMMON-EMITTER AMPLIFIER</span></h1><p>Explore voltage gain, clipping, and bias in a transistor amplifier.</p></header><div className="frs-modes">{['Observe','Predict','Experiment','Explain'].map(m=><button key={m} onClick={()=>m!=='Observe'&&setPanel(m)}>{m}</button>)}</div><div className="frs-layout"><section className="frs-stage"><svg viewBox="0 0 900 380"><rect width="900" height="380" fill="#142b3e"/><polyline points="80,220 140,180 200,220 260,180 320,220 380,180" fill="none" stroke="#25d5f6" strokeWidth="4"/><polyline points="520,180 580,100 640,260 700,100 760,260 820,180" fill="none" stroke="#bd6cff" strokeWidth="5"/><line x1="450" y1="60" x2="450" y2="320" stroke="#909dad" strokeWidth="10"/><rect x="400" y="150" width="100" height="70" fill="#3a4653"/><text x="335" y="120" fill="#25d5f6">CH1 Input {input.vin.toFixed(2)} Vpp</text><text x="535" y="80" fill="#bd6cff">CH2 Output {s.out.toFixed(2)} Vpp</text></svg></section><aside className="frs-right"><section className="frs-panel"><h2>CONTROLS</h2><C label="Input Amplitude (Vin)" value={input.vin} min={.05} max={2} unit="Vpp" onChange={vin=>update({vin})}/><C label="Gain" value={input.gain} min={-50} max={-2} unit="×" onChange={gain=>update({gain})}/><C label="Bias (Vb)" value={input.bias} min={.6} max={2} unit="V" onChange={bias=>update({bias})}/></section><section className="frs-panel frs-readings"><h2>LIVE READINGS</h2><p>Vin (pp)<b>{input.vin.toFixed(2)} V</b></p><p>Vout (pp)<b>{s.out.toFixed(2)} V</b></p><p>Gain (Vout/Vin)<b>{s.gain.toFixed(1)}</b></p><p>Collector voltage Vc<b>{s.vc.toFixed(1)} V</b></p><p>Collector current Ic<b>{s.ic.toFixed(2)} mA</b></p></section></aside><section className="frs-equation frs-panel"><h2>KEY RELATION</h2><b>Aᵥ = Vout/Vin ≈ −R<sub>C</sub>/r<sub>e</sub></b><p>Bias sets the operating point; gain scales the input waveform until clipping.</p></section></div><div className="frs-actions"><button onClick={()=>setInput(AMP_DEFAULTS)}>⟳ Reset</button><button className="frs-next" onClick={()=>setPanel('Challenge')}>Next Challenge　→</button></div><footer>ELECTRONICS　|　AMPLIFIERS</footer></main>{panel&&<MasteryChallengeDialog title={panel} close={()=>setPanel(null)}><p>Increase input amplitude and observe output clipping.</p></MasteryChallengeDialog>}</div>}
