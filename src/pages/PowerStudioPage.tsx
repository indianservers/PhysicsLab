import{useState}from'react';import{Link}from'react-router-dom';import{MasteryChallengeDialog}from'../components/MasteryChallengeDialog';import{POWER_DEFAULTS,powerSettings,powerSolution,type PowerInput}from'../lib/powerStudio';import'../friction-studio.css';function C({label,value,min,max,unit,onChange}:{label:string;value:number;min:number;max:number;unit:string;onChange:(v:number)=>void}){return <label className="frs-control"><span>{label}<input aria-label={label+' value'} type="number" value={value.toFixed(2)} onChange={e=>onChange(Number(e.target.value))}/><em>{unit}</em></span><input aria-label={label} type="range" value={value} min={min} max={max} step=".01" onChange={e=>onChange(Number(e.target.value))}/><small><i>{min}</i><i>{max}</i></small></label>}export default function PowerStudioPage(){const[input,setInput]=useState(POWER_DEFAULTS),[panel,setPanel]=useState<string|null>(null),[zoom,setZoom]=useState(false);const s=powerSolution(input);const update=(p:Partial<PowerInput>)=>setInput(powerSettings({...input,...p}));const reset=()=>{setInput(POWER_DEFAULTS);setPanel(null);setZoom(false)};return <div className="frs-page tension-page" data-ui-theme="dark"><aside className="frs-side"><Link className="frs-brand" to="/concept-studio">⚛ PHYSICS STUDIO</Link><nav><Link to="/concept-studio">⌂ Home</Link><button>⚙ Mechanics⌃</button>{[['Work','/motion/work'],['Kinetic Energy','/motion/kinetic-energy'],['Potential Energy','/motion/potential-energy'],['Conservation','/motion/conservation-energy'],['Power','/motion/power'],['Waves','/concept-studio/waves-sound'],['Thermodynamics','/concept-studio/thermodynamics'],['Electricity','/concept-studio/electricity'],['Optics','/concept-studio/waves-sound'],['Modern Physics','/concept-studio/modern-physics']].map(([n,to])=><Link key={n} to={to}>{n}</Link>)}</nav></aside><main id="content" className="frs-main"><header className="frs-head"><h1>WORK, ENERGY &amp; POWER　›　<span>POWER</span></h1><p>Rate at which work is done.</p></header><div className="frs-modes">{['Observe','Predict','Experiment','Explain'].map(m=><button key={m} aria-pressed={m==='Observe'} onClick={()=>m!=='Observe'&&setPanel(m)}>{m}</button>)}</div><div className="frs-layout"><section className="frs-stage" style={{transform:zoom?'scale(1.1)':'none'}}><img src="/assets/friction-studio/lift-rig.png" alt="Motorized rig lifting a stack of weights"/><svg viewBox="0 0 900 380"><line x1="500" y1="90" x2="500" y2="260" stroke="#69dcff" strokeDasharray="8 5"/><text x="520" y="180" fill="white" fontSize="18">P = {s.power.toFixed(1)} W</text><text x="80" y="90" fill="#70caff" fontSize="18">W = {s.work.toFixed(1)} J</text></svg></section><aside className="frs-right"><section className="frs-panel"><h2>CONTROLS</h2><C label="Load (mass)" value={input.mass} min={1} max={10} unit="kg" onChange={mass=>update({mass})}/><C label="Lift Speed" value={input.speed} min={.1} max={1} unit="m/s" onChange={speed=>update({speed})}/><C label="Motor Torque" value={input.torque} min={.5} max={4} unit="N·m" onChange={torque=>update({torque})}/><C label="Elapsed Time" value={input.time} min={.01} max={10} unit="s" onChange={time=>update({time})}/></section><section className="frs-panel frs-readings"><h2>LIVE READINGS</h2><p>Work Done, W<b>{s.work.toFixed(1)} J</b></p><p>Elapsed Time, t<b>{input.time.toFixed(2)} s</b></p><p>Power, P<b>{s.power.toFixed(1)} W</b></p><p>Lift Speed<b>{input.speed.toFixed(2)} m/s</b></p></section></aside><section className="frs-equation frs-panel"><h2>KEY EQUATION</h2><b>P = W / t</b><p>Power measures how quickly work is performed. Increasing speed or load changes the required rate.</p></section></div><div className="frs-actions"><button aria-label="Reset" onClick={reset}>⟳ Reset</button><button aria-label={zoom?'Reset Zoom':'Zoom'} onClick={()=>setZoom(v=>!v)}>⌕ {zoom?'Reset Zoom':'Zoom'}</button><button className="frs-next" onClick={()=>setPanel('Challenge')}>Next Challenge　→</button></div><footer>WORK, ENERGY &amp; POWER　|　POWER</footer></main>{panel&&<MasteryChallengeDialog title={panel} close={()=>setPanel(null)}><p>{panel==='Predict'?'How does power change when the same work is done in half the time?':panel==='Explain'?'Power is work divided by elapsed time.':'Adjust mass, lift speed and time, then compare power.'}</p></MasteryChallengeDialog>}</div>}
