import{useState}from'react';import{Link}from'react-router-dom';import{MasteryChallengeDialog}from'../components/MasteryChallengeDialog';import{WIRE_DEFAULTS,wireSettings,wireSolution,type WireInput}from'../lib/currentWireStudio';import'../friction-studio.css';function C({label,value,min,max,unit,onChange}:{label:string;value:number;min:number;max:number;unit:string;onChange:(v:number)=>void}){return <label className="frs-control"><span>{label}<input aria-label={label+' value'} type="number" value={value.toFixed(1)} onChange={e=>onChange(Number(e.target.value))}/><em>{unit}</em></span><input aria-label={label} type="range" min={min} max={max} step=".1" value={value} onChange={e=>onChange(Number(e.target.value))}/><small><i>{min}</i><i>{max}</i></small></label>}export default function CurrentWireStudioPage(){const[input,setInput]=useState(WIRE_DEFAULTS),[panel,setPanel]=useState<string|null>(null);const s=wireSolution(input);const update=(p:Partial<WireInput>)=>setInput(wireSettings({...input,...p}));return <div className="frs-page tension-page" data-ui-theme="dark"><aside className="frs-side"><Link className="frs-brand" to="/concept-studio">◉ MAGNETISM</Link><nav><Link to="/concept-studio">⌂ Home</Link><Link to="/magnetism/field-lines">Field Lines</Link><Link to="/magnetism/lorentz-force">Lorentz Force</Link><Link to="/magnetism/current-wire">Current Wire</Link><Link to="/magnetism/solenoids">Solenoids</Link></nav></aside><main id="content" className="frs-main"><header className="frs-head"><h1>MAGNETISM　›　<span>CURRENT-CARRYING WIRE</span></h1><p>Explore the magnetic field around a straight current-carrying conductor.</p></header><div className="frs-modes">{['Observe','Predict','Experiment','Explain'].map(m=><button key={m} onClick={()=>m!=='Observe'&&setPanel(m)}>{m}</button>)}</div><div className="frs-layout"><section className="frs-stage"><svg viewBox="0 0 900 380"><rect width="900" height="380" fill="#142b3a"/><line x1="450" y1="20" x2="450" y2="360" stroke="#d78c54" strokeWidth="16"/><line x1="450" y1="40" x2="450" y2="10" stroke="#e7f7ff" strokeWidth="5"/><text x="465" y="25" fill="white">I = {input.current.toFixed(1)} A</text>{[45,80,115,150].map((r,i)=><circle key={i} cx="450" cy="200" r={r} fill="none" stroke="#65baff" strokeWidth="3" strokeDasharray="8 6"/>)}<text x="610" y="70" fill="white">B = {(s.B*1e6).toFixed(2)} µT</text></svg></section><aside className="frs-right"><section className="frs-panel"><h2>CONTROLS</h2><C label="Current I" value={input.current} min={0} max={10} unit="A" onChange={current=>update({current})}/><label>Wire Direction <select aria-label="Wire Direction" value={input.direction} onChange={e=>update({direction:Number(e.target.value)})}><option value="1">Upward (+z)</option><option value="-1">Downward (−z)</option></select></label><C label="Probe Radius r" value={input.radius} min={1} max={20} unit="cm" onChange={radius=>update({radius})}/></section><section className="frs-panel frs-readings"><h2>LIVE READINGS</h2><p>Magnetic field strength B<b>{(s.B*1e6).toFixed(2)} µT</b></p><p>Probe radius r<b>{(input.radius/100).toFixed(3)} m</b></p><p>Current I<b>{input.current.toFixed(1)} A</b></p></section></aside><section className="frs-equation frs-panel"><h2>RELEVANT EQUATION</h2><b>B = μ₀I/(2πr)</b><p>Field strength grows with current and falls inversely with distance from the wire.</p></section></div><div className="frs-actions"><button onClick={()=>setInput(WIRE_DEFAULTS)}>⟳ Reset</button><button className="frs-next" onClick={()=>setPanel('Challenge')}>Next Challenge　→</button></div><footer>MAGNETISM　|　CURRENT-CARRYING WIRE</footer></main>{panel&&<MasteryChallengeDialog title={panel} close={()=>setPanel(null)}><p>Double the current and observe the field strength.</p></MasteryChallengeDialog>}</div>}
