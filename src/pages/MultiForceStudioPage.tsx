import{useState}from'react';import{Link}from'react-router-dom';import{MasteryChallengeDialog}from'../components/MasteryChallengeDialog';import{MULTI_DEFAULTS,multiSettings,multiSolution,type MultiForceInput}from'../lib/multiForceStudio';import'../friction-studio.css';
function C({label,value,min,max,unit,onChange}:{label:string;value:number;min:number;max:number;unit:string;onChange:(v:number)=>void}){return <label className="frs-control"><span>{label}<input aria-label={label+' value'} type="number" value={value.toFixed(0)} onChange={e=>onChange(Number(e.target.value))}/><em>{unit}</em></span><input aria-label={label} type="range" value={value} min={min} max={max} step="1" onChange={e=>onChange(Number(e.target.value))}/><small><i>{min}</i><i>{max}</i></small></label>}
export default function MultiForceStudioPage(){const[input,setInput]=useState(MULTI_DEFAULTS),[panel,setPanel]=useState<string|null>(null),[zoom,setZoom]=useState(false);const s=multiSolution(input);const update=(p:Partial<MultiForceInput>)=>setInput(multiSettings({...input,...p}));const reset=()=>{setInput(MULTI_DEFAULTS);setPanel(null);setZoom(false)};return <div className="frs-page tension-page"><aside className="frs-side"><Link className="frs-brand" to="/concept-studio">⚛ PHYSICS STUDIO</Link><nav><Link to="/concept-studio">⌂ Home</Link><button>⚙ Mechanics⌃</button>{[['Motion','/concept-studio/motion-kinematics'],["Force & Newton's Laws",'/motion/multi-force-challenge'],['Energy','/concept-studio/work-energy-power'],['Momentum','/mechanics/momentum-collisions'],['Circular Motion','/motion/circular-motion'],['Gravitation','/concept-studio/gravitation'],['Waves','/concept-studio/waves-sound'],['Thermodynamics','/concept-studio/thermodynamics'],['Electricity & Magnetism','/concept-studio/electricity'],['Modern Physics','/concept-studio/modern-physics']].map(([n,to])=><Link key={n} to={to}>{n}</Link>)}</nav></aside><main id="content" className="frs-main"><header className="frs-head"><h1>FORCE &amp; NEWTON’S LAWS　›　<span>MULTI-FORCE CHALLENGE</span></h1><p>Rescue the sled — balance multiple forces to reach the target acceleration.</p></header><div className="frs-modes">{['Observe','Predict','Experiment','Explain'].map(m=><button key={m} aria-pressed={m==='Observe'} onClick={()=>m!=='Observe'&&setPanel(m)}>{m}</button>)}</div><div className="frs-layout"><section className="frs-stage" style={{transform:zoom?'scale(1.1)':'none'}}><img src="/assets/friction-studio/sled.png" alt="Rescue sled on snowy slope with winch"/><svg viewBox="0 0 900 380"><line x1="300" y1="240" x2="650" y2="150" stroke="#35a6ff" strokeWidth="6" markerEnd="url(#a)"/><line x1="300" y1="240" x2="100" y2="240" stroke="#ff5b78" strokeWidth="6" markerEnd="url(#a)"/><line x1="300" y1="240" x2="500" y2="280" stroke="#ffc02e" strokeWidth="6" markerEnd="url(#a)"/><text x="600" y="145" fill="#35a6ff" fontSize="18">T = {input.tension} N</text><text x="80" y="230" fill="#ff5b78" fontSize="18">Fwind = {input.wind} N</text><text x="500" y="300" fill="#ffc02e" fontSize="18">Ffriction = {s.friction.toFixed(0)} N</text><defs><marker id="a" markerWidth="10" markerHeight="10" refX="9" refY="4" orient="auto"><path d="M0 0 10 4 0 8Z" fill="currentColor"/></marker></defs></svg></section><aside className="frs-right"><section className="frs-panel"><h2>CONTROLS</h2><C label="Winch Tension (T)" value={input.tension} min={0} max={2000} unit="N" onChange={tension=>update({tension})}/><C label="Slope Angle (θ)" value={input.angle} min={0} max={40} unit="°" onChange={angle=>update({angle})}/><C label="Wind Force (Fwind)" value={input.wind} min={0} max={800} unit="N" onChange={wind=>update({wind})}/></section><section className="frs-panel frs-readings"><h2>REAL-TIME READINGS</h2><p>Tension (T)<b>{input.tension} N</b></p><p>Gravity component<b>{s.gravity.toFixed(0)} N</b></p><p>Friction (f)<b>{s.friction.toFixed(0)} N</b></p><p>Wind (down slope)<b>{input.wind} N</b></p><p>Net force (ΣF)<b>{s.net.toFixed(0)} N</b></p><p>Acceleration (a)<b>{s.acceleration.toFixed(2)} m/s²</b></p></section></aside><section className="frs-equation frs-panel"><h2>KEY EQUATION</h2><b>ΣF = ma</b><p>a = (T − Fg∥ − f − Fwind) / m</p><p>Fg∥ = mg sin θ　　f = μmg cos θ</p></section></div><div className="frs-actions"><button aria-label="Reset" onClick={reset}>⟳ Reset</button><button aria-label={zoom?'Reset Zoom':'Zoom'} onClick={()=>setZoom(v=>!v)}>⌕ {zoom?'Reset Zoom':'Zoom'}</button><button className="frs-next" onClick={()=>setPanel('Challenge')}>Next Challenge　→</button></div><footer>FORCE &amp; NEWTON’S LAWS　|　MULTI-FORCE CHALLENGE</footer></main>{panel&&<MasteryChallengeDialog title={panel} close={()=>setPanel(null)}><p>{panel==='Challenge'?'Keep acceleration between 0.50 and 1.00 m/s² as wind rises.':panel==='Predict'?'Which force must increase to maintain acceleration as wind increases?':panel==='Explain'?'The net force is tension minus gravity, friction and wind.':'Adjust each force and observe the resultant acceleration.'}</p></MasteryChallengeDialog>}</div>}
