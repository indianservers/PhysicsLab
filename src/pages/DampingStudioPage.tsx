import{useState}from'react';import{Link}from'react-router-dom';import{MasteryChallengeDialog}from'../components/MasteryChallengeDialog';import{DAMPING_DEFAULTS,dampingSettings,dampingSolution,type DampingInput}from'../lib/dampingStudio';import'../friction-studio.css';function C({label,value,min,max,unit,onChange}:{label:string;value:number;min:number;max:number;unit:string;onChange:(v:number)=>void}){return <label className="frs-control"><span>{label}<input aria-label={label+' value'} type="number" value={value.toFixed(3)} onChange={e=>onChange(Number(e.target.value))}/><em>{unit}</em></span><input aria-label={label} type="range" min={min} max={max} step={(max-min)/100} value={value} onChange={e=>onChange(Number(e.target.value))}/><small><i>{min}</i><i>{max}</i></small></label>}export default function DampingStudioPage(){const[input,setInput]=useState(DAMPING_DEFAULTS),[panel,setPanel]=useState<string|null>(null);const s=dampingSolution(input);const update=(p:Partial<DampingInput>)=>setInput(dampingSettings({...input,...p}));return <div className="frs-page tension-page" data-ui-theme="dark"><aside className="frs-side"><Link className="frs-brand" to="/concept-studio">⚛ PHYSICS STUDIO</Link><nav><Link to="/concept-studio">⌂ Home</Link><Link to="/motion/spring-shm">Spring SHM</Link><Link to="/motion/pendulum">Pendulum</Link><Link to="/motion/energy-exchange">Energy Exchange</Link><Link to="/motion/damping">Damping</Link><Link to="/motion/resonance">Resonance</Link></nav></aside><main id="content" className="frs-main"><header className="frs-head"><h1>OSCILLATIONS　›　<span>DAMPING</span></h1><p>Explore how a viscous fluid reduces oscillation amplitude over time.</p></header><div className="frs-modes">{['Observe','Predict','Experiment','Explain'].map(m=><button key={m} onClick={()=>m!=='Observe'&&setPanel(m)}>{m}</button>)}</div><div className="frs-layout"><section className="frs-stage"><img src="/assets/friction-studio/spring.png" alt="Damped spring in fluid chamber"/><svg viewBox="0 0 900 380"><path d="M80 190 C140 80 200 300 260 190 S380 80 440 190 S560 300 620 190 S740 100 820 190" fill="none" stroke="#22d7ff" strokeWidth="3"/><path d="M80 190 C200 90 320 290 440 190 S680 120 820 190" fill="none" stroke="#c27bff" strokeDasharray="9 7"/><text x="570" y="90" fill="#9de5ff">x(t) = {s.x.toFixed(3)} m</text></svg></section><aside className="frs-right"><section className="frs-panel"><h2>CONTROLS</h2><C label="Damping Coefficient b" value={input.b} min={0} max={5} unit="N·s/m" onChange={b=>update({b})}/><C label="Mass m" value={input.m} min={.05} max={2} unit="kg" onChange={m=>update({m})}/><C label="Initial Displacement x₀" value={input.x0} min={0} max={.2} unit="m" onChange={x0=>update({x0})}/><C label="Time t" value={input.time} min={0} max={25} unit="s" onChange={time=>update({time})}/></section><section className="frs-panel frs-readings"><h2>LIVE READINGS</h2><p>Displacement x<b>{s.x.toFixed(3)} m</b></p><p>Velocity v<b>{s.vel.toFixed(3)} m/s</b></p><p>Decay constant δ<b>{s.delta.toFixed(3)} s⁻¹</b></p><p>Q factor<b>{s.Q.toFixed(2)}</b></p><p>Regime<b>{s.regime}</b></p></section></aside><section className="frs-equation frs-panel"><h2>GOVERNING EQUATION</h2><b>x(t) = A₀e⁻ᵟᵗ cos(ωᵈt + φ)</b><p>Damping coefficient controls the exponential decay envelope.</p></section></div><div className="frs-actions"><button onClick={()=>setInput(DAMPING_DEFAULTS)}>⟳ Reset</button><button className="frs-next" onClick={()=>setPanel('Challenge')}>Next Challenge　→</button></div><footer>OSCILLATIONS　|　DAMPING</footer></main>{panel&&<MasteryChallengeDialog title={panel} close={()=>setPanel(null)}><p>Increase damping until the system becomes critically damped.</p></MasteryChallengeDialog>}</div>}
