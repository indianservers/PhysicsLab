import{useState}from'react';import{Link}from'react-router-dom';import{MasteryChallengeDialog}from'../components/MasteryChallengeDialog';import{BUOYANCY_DEFAULTS,buoyancySettings,buoyancySolution,BUOYANCY_FLUIDS,type BuoyancyInput}from'../lib/buoyancyStudio';import'../friction-studio.css';
function C({label,value,min,max,onChange,unit}:{label:string;value:number;min:number;max:number;onChange:(v:number)=>void;unit:string}){return <label className="frs-control"><span>{label}<input aria-label={label+' value'} type="number" value={value} onChange={e=>onChange(Number(e.target.value))}/><em>{unit}</em></span><input aria-label={label} type="range" min={min} max={max} step=".01" value={value} onChange={e=>onChange(Number(e.target.value))}/><small><i>{min}</i><i>{max}</i></small></label>}
export default function BuoyancyStudioPage(){const[input,setInput]=useState(BUOYANCY_DEFAULTS),[panel,setPanel]=useState<string|null>(null);const s=buoyancySolution(input);const update=(p:Partial<BuoyancyInput>)=>setInput(buoyancySettings({...input,...p}));return <div className="frs-page tension-page" data-ui-theme="dark"><aside className="frs-side"><Link className="frs-brand" to="/concept-studio">◈ FLUID MECHANICS</Link><nav><Link to="/concept-studio">⌂ Home</Link><Link to="/fluid-mechanics/density-pressure">Density & Pressure</Link><Link to="/fluid-mechanics/buoyancy">Buoyancy</Link></nav></aside><main id="content" className="frs-main"><header className="frs-head"><h1>FLUID MECHANICS　›　<span>BUOYANCY</span></h1><p>Explore how fluid displacement creates an upward buoyant force.</p></header><div className="frs-modes">{['Observe','Predict','Experiment','Explain'].map(m=><button key={m} onClick={()=>m!=='Observe'&&setPanel(m)}>{m}</button>)}</div><div className="frs-layout"><section className="frs-stage"><svg viewBox="0 0 900 380"><rect width="900" height="380" fill="#102c43"/><rect x="130" y="80" width="640" height="250" rx="18" fill="#1e6385" stroke="#9edcff" strokeWidth="6"/><rect x="136" y="180" width="628" height="144" fill="#247aa2" opacity=".7"/><rect x="390" y={130+input.fraction*120} width="120" height="120" fill="#aeb8c2" stroke="#fff" strokeWidth="4"/><path d="M450 100 V60 M430 80 l20 -20 20 20" stroke="#7de3ff" strokeWidth="8" fill="none"/><text x="250" y="355" fill="#9ed5ff">Fᵦ={s.fb.toFixed(3)} N · Wapp={s.apparent.toFixed(2)} N</text></svg></section><aside className="frs-right"><section className="frs-panel"><h2>CONTROLS</h2><C label="Object Density" value={input.density} min={500} max={5000} unit="kg/m³" onChange={density=>update({density})}/><C label="Submerged Fraction" value={input.fraction} min={0} max={1} unit="fraction" onChange={fraction=>update({fraction})}/><label>Fluid <select aria-label="Fluid" value={input.fluid} onChange={e=>update({fluid:e.target.value})}>{Object.entries(BUOYANCY_FLUIDS).map(([k,v])=><option key={k} value={k}>{v.name} (ρ={v.rho} kg/m³)</option>)}</select></label></section><section className="frs-panel frs-readings"><h2>LIVE READINGS</h2><p>Buoyant Force Fᵦ<b>{s.fb.toFixed(3)} N</b></p><p>Apparent Weight<b>{s.apparent.toFixed(2)} N</b></p><p>True Weight W<b>{s.weight.toFixed(2)} N</b></p><p>Displaced Volume V<b>{(s.V*1e6).toFixed(1)} cm³</b></p><p>Fluid Density ρ<b>{s.rho} kg/m³</b></p></section><section className="frs-panel frs-equation"><h2>KEY RELATION</h2><b>Fᵦ = ρgV<sub>displaced</sub></b><p>Archimedes’ principle links displaced fluid volume to upward force.</p></section></aside></div><div className="frs-actions"><button onClick={()=>setInput(BUOYANCY_DEFAULTS)}>⟳ Reset</button><button className="frs-next" onClick={()=>setPanel('Challenge')}>Next Challenge　→</button></div><footer>FLUID MECHANICS　|　BUOYANCY</footer></main>{panel&&<MasteryChallengeDialog title={panel} close={()=>setPanel(null)}><p>Change the submerged fraction and compare buoyant force.</p></MasteryChallengeDialog>}</div>}
