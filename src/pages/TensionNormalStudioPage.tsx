import{useState}from'react';import{Link}from'react-router-dom';import{MasteryChallengeDialog}from'../components/MasteryChallengeDialog';import{TENSION_DEFAULTS,tensionSettings,tensionSolution,type TensionInput}from'../lib/tensionStudio';import'../friction-studio.css';
function C({label,value,min,max,unit,onChange}:{label:string;value:number;min:number;max:number;unit:string;onChange:(v:number)=>void}){return <label className="frs-control"><span>{label}<input aria-label={label+' value'} type="number" value={value.toFixed(1)} onChange={e=>onChange(Number(e.target.value))}/><em>{unit}</em></span><input aria-label={label} type="range" value={value} min={min} max={max} step="1" onChange={e=>onChange(Number(e.target.value))}/><small><i>{min}</i><i>{max}</i></small></label>}
export default function TensionNormalStudioPage(){const[input,setInput]=useState(TENSION_DEFAULTS),[panel,setPanel]=useState<string|null>(null),[zoom,setZoom]=useState(false);const s=tensionSolution(input);const update=(p:Partial<TensionInput>)=>setInput(tensionSettings({...input,...p}));const reset=()=>{setInput(TENSION_DEFAULTS);setPanel(null);setZoom(false)};return <div className="frs-page tension-page" data-ui-theme="dark"><aside className="frs-side"><Link className="frs-brand" to="/concept-studio">⚛ PHYSICS STUDIO</Link><nav><Link to="/concept-studio">⌂ Home</Link><button>⚙ Mechanics⌃</button>{[['Kinematics','/concept-studio/motion-kinematics'],["Forces & Newton's Laws",'/motion/tension-normal'],['Work & Energy','/concept-studio/work-energy-power'],['Momentum','/mechanics/momentum-collisions'],['Rotational Motion','/mechanics/torque-levers'],['Gravitation','/concept-studio/gravitation'],['Waves','/concept-studio/waves-sound'],['Thermodynamics','/concept-studio/thermodynamics'],['Electricity & Magnetism','/concept-studio/electricity'],['Optics','/concept-studio/waves-sound'],['Modern Physics','/concept-studio/modern-physics']].map(([n,to])=><Link key={n} to={to}>{n}</Link>)}</nav></aside><main id="content" className="frs-main"><header className="frs-head"><h1>FORCE &amp; NEWTON’S LAWS　›　<span>TENSION &amp; NORMAL FORCE</span></h1><p>Adjust the cable angles and load mass. Observe the tensions in the cables and normal force from the scale.</p></header><div className="frs-modes">{['Observe','Predict','Experiment','Explain'].map(m=><button key={m} aria-pressed={m==='Experiment'} onClick={()=>m!=='Observe'&&setPanel(m)}>{m}</button>)}</div><div className="frs-layout"><section className="frs-stage" style={{transform:zoom?'scale(1.1)':'none'}}><img src="/assets/friction-studio/tension-lamp.png" alt="Lamp suspended by two cables above a digital scale"/><svg viewBox="0 0 900 380"><line x1="190" y1="85" x2="450" y2="220" stroke="#48cfff" strokeWidth="4"/><line x1="710" y1="85" x2="450" y2="220" stroke="#ae79ff" strokeWidth="4"/><line x1="450" y1="100" x2="450" y2="220" stroke="white" strokeWidth="3"/><text x="220" y="110" fill="#48cfff" fontSize="20">T_A {s.ta.toFixed(1)} N</text><text x="600" y="110" fill="#ae79ff" fontSize="20">T_B {s.tb.toFixed(1)} N</text><text x="470" y="180" fill="white" fontSize="18">W = {s.weight.toFixed(1)} N</text></svg></section><aside className="frs-right"><section className="frs-panel"><h2>CONTROLS</h2><C label="Cable Angle A θA" value={input.angleA} min={10} max={80} unit="°" onChange={angleA=>update({angleA})}/><C label="Cable Angle B θB" value={input.angleB} min={10} max={80} unit="°" onChange={angleB=>update({angleB})}/><C label="Load Mass m" value={input.mass} min={1} max={20} unit="kg" onChange={mass=>update({mass})}/></section><section className="frs-panel frs-readings"><h2>LIVE READINGS</h2><p>Tension in Cable A<b>{s.ta.toFixed(1)} N</b></p><p>Tension in Cable B<b>{s.tb.toFixed(1)} N</b></p><p>Normal Force (Scale)<b>{s.normal.toFixed(1)} N</b></p><p>Weight mg<b>{s.weight.toFixed(1)} N</b></p></section></aside><section className="frs-equation frs-panel"><h2>Equilibrium (vertical)</h2><b>T<sub>A</sub> sin θ<sub>A</sub> + T<sub>B</sub> sin θ<sub>B</sub> = mg</b><p>At equilibrium the upward cable components and scale normal force balance the load.</p></section></div><div className="frs-actions"><button aria-label="Reset" onClick={reset}>⟳ Reset</button><button onClick={()=>setZoom(v=>!v)} aria-label={zoom?'Reset Zoom':'Zoom'}>⌕ {zoom?'Reset Zoom':'Zoom'}</button><button className="frs-next" onClick={()=>setPanel('Challenge')}>Next Challenge　→</button></div><footer>FORCE &amp; NEWTON’S LAWS　|　TENSION &amp; NORMAL FORCE</footer></main>{panel&&<MasteryChallengeDialog title={panel} close={()=>setPanel(null)}><p>{panel==='Predict'?'How does increasing either cable angle change the tension?':panel==='Explain'?'The horizontal components cancel while vertical components support the weight.':'Adjust angles and mass, then compare cable tensions.'}</p></MasteryChallengeDialog>}</div>}


