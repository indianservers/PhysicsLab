import{useState}from'react';import{Link}from'react-router-dom';import{MasteryChallengeDialog}from'../components/MasteryChallengeDialog';import{SEMI_DEFAULTS,semiSettings,semiSolution,type SemiconductorInput}from'../lib/semiconductorStudio';import'../friction-studio.css';function C({label,value,min,max,unit,onChange}:{label:string;value:number;min:number;max:number;unit:string;onChange:(v:number)=>void}){return <label className="frs-control"><span>{label}<input aria-label={label+' value'} type="number" value={value.toExponential(1)} onChange={e=>onChange(Number(e.target.value))}/><em>{unit}</em></span><input aria-label={label} type="range" min={min} max={max} step="1" value={Math.log10(value)} onChange={e=>onChange(10**Number(e.target.value))}/><small><i>{min}</i><i>{max}</i></small></label>}export default function SemiconductorsStudioPage(){const[input,setInput]=useState(SEMI_DEFAULTS),[panel,setPanel]=useState<string|null>(null);const s=semiSolution(input);const update=(p:Partial<SemiconductorInput>)=>setInput(semiSettings({...input,...p}));return <div className="frs-page tension-page" data-ui-theme="dark"><aside className="frs-side"><Link className="frs-brand" to="/concept-studio">▣ ELECTRONICS</Link><nav><Link to="/concept-studio">⌂ Home</Link><Link to="/electronics/semiconductors">Semiconductors</Link><Link to="/electronics/diodes">Diodes</Link><Link to="/electronics/transistors">Transistors</Link><Link to="/electronics/sensors">Sensors</Link></nav></aside><main id="content" className="frs-main"><header className="frs-head"><h1>ELECTRONICS　›　<span>SEMICONDUCTORS</span></h1><p>Explore silicon crystal, doping, and charge carriers.</p></header><div className="frs-modes">{['Observe','Predict','Experiment','Explain'].map(m=><button key={m} onClick={()=>m!=='Observe'&&setPanel(m)}>{m}</button>)}</div><div className="frs-layout"><section className="frs-stage"><svg viewBox="0 0 900 400"><rect width="900" height="400" fill="#152c40"/>{Array.from({length:7},(_,r)=><g key={r}>{Array.from({length:8},(_,c)=><g key={c}><circle cx={180+c*80+(r%2)*35} cy={80+r*45} r="14" fill="#8998aa" stroke="#c5d3df"/><line x1={180+c*80+(r%2)*35} y1={80+r*45} x2={220+c*80+(r%2)*35} y2={105+r*45} stroke="#b8c8d8" strokeWidth="5"/></g>)}</g>)}{Array.from({length:10},(_,i)=><circle key={'e'+i} cx={240+(i%5)*120} cy={120+Math.floor(i/5)*150} r="7" fill="#27a9ff"/>)}<text x="40" y="360" fill="white">Si crystal · {input.type>0?'n-type':'p-type'} · T={input.temperature} K</text></svg></section><aside className="frs-right"><section className="frs-panel"><h2>CONTROLS</h2><label>Material Type <select aria-label="Material Type" value={input.type} onChange={e=>update({type:Number(e.target.value)})}><option value="1">n-type (Phosphorus)</option><option value="-1">p-type (Boron)</option></select></label><C label="Doping Level" value={input.doping} min={14} max={18} unit="cm⁻³" onChange={doping=>update({doping})}/><C label="Temperature" value={input.temperature} min={100} max={500} unit="K" onChange={temperature=>update({temperature})}/></section><section className="frs-panel frs-readings"><h2>LIVE READINGS</h2><p>Electron density n<b>{s.n.toExponential(2)} cm⁻³</b></p><p>Band gap E<sub>g</sub><b>{s.gap.toFixed(2)} eV</b></p><p>Conductivity σ<b>{s.sigma.toExponential(2)} S/m</b></p></section></aside><section className="frs-equation frs-panel"><h2>KEY RELATION</h2><b>n = N<sub>C</sub> exp(−(E<sub>C</sub>−E<sub>F</sub>)/(k<sub>B</sub>T))</b><p>Doping and temperature control charge-carrier density and conductivity.</p></section></div><div className="frs-actions"><button onClick={()=>setInput(SEMI_DEFAULTS)}>⟳ Reset</button><button className="frs-next" onClick={()=>setPanel('Challenge')}>Next Challenge　→</button></div><footer>ELECTRONICS　|　SEMICONDUCTORS</footer></main>{panel&&<MasteryChallengeDialog title={panel} close={()=>setPanel(null)}><p>Increase doping and compare conductivity.</p></MasteryChallengeDialog>}</div>}

