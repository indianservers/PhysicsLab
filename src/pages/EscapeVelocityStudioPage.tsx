import{useState}from'react';import{Link}from'react-router-dom';import{MasteryChallengeDialog}from'../components/MasteryChallengeDialog';import{ESCAPE_DEFAULTS,escapeSettings,escapeSolution,type EscapeInput}from'../lib/escapeStudio';import'../friction-studio.css';function C({label,value,min,max,unit,onChange}:{label:string;value:number;min:number;max:number;unit:string;onChange:(v:number)=>void}){return <label className="frs-control"><span>{label}<input aria-label={label+' value'} type="number" value={value.toFixed(2)} onChange={e=>onChange(Number(e.target.value))}/><em>{unit}</em></span><input aria-label={label} type="range" min={min} max={max} step={(max-min)/100} value={value} onChange={e=>onChange(Number(e.target.value))}/><small><i>{min}</i><i>{max}</i></small></label>}export default function EscapeVelocityStudioPage(){const[input,setInput]=useState(ESCAPE_DEFAULTS),[panel,setPanel]=useState<string|null>(null);const s=escapeSolution(input);const update=(p:Partial<EscapeInput>)=>setInput(escapeSettings({...input,...p}));return <div className="frs-page tension-page" data-ui-theme="dark"><aside className="frs-side"><Link className="frs-brand" to="/concept-studio">⚛ PHYSICS STUDIO</Link><nav><Link to="/concept-studio">⌂ Home</Link><Link to="/motion/universal-gravity">Universal Gravitation</Link><Link to="/motion/gravitational-field">Gravitational Field</Link><Link to="/motion/gravitational-potential">Gravitational Potential</Link><Link to="/motion/orbits">Orbits</Link><Link to="/motion/escape-velocity">Escape Velocity</Link><Link to="/motion/keplers-laws">Kepler’s Laws</Link></nav></aside><main id="content" className="frs-main"><header className="frs-head"><h1>GRAVITATION　›　<span>ESCAPE VELOCITY</span></h1><p>Explore how launch speed, planet mass, and radius determine escape.</p></header><div className="frs-modes">{['Observe','Predict','Experiment','Explain'].map(m=><button key={m} onClick={()=>m!=='Observe'&&setPanel(m)}>{m}</button>)}</div><div className="frs-layout"><section className="frs-stage"><img src="/assets/friction-studio/escape.png" alt="Spacecraft escaping a planet"/><svg viewBox="0 0 900 380"><path d="M170 330 Q350 40 700 75" fill="none" stroke="#15ddff" strokeWidth="3" strokeDasharray="12 8"/><path d="M170 330 Q370 140 620 230" fill="none" stroke="#b45cff" strokeWidth="3" strokeDasharray="10 8"/><text x="520" y="75" fill="#8cf8ff">Unbound trajectory</text><text x="500" y="220" fill="#d5a0ff">Bound trajectory</text></svg></section><aside className="frs-right"><section className="frs-panel"><h2>CONTROLS</h2><C label="Planet Radius" value={input.radius} min={1000} max={20000} unit="km" onChange={radius=>update({radius})}/><C label="Planet Mass" value={input.mass} min={.1} max={50} unit="×10²⁴ kg" onChange={mass=>update({mass})}/><C label="Launch Speed" value={input.speed} min={0} max={20} unit="km/s" onChange={speed=>update({speed})}/></section><section className="frs-panel frs-readings"><h2>KEY READINGS</h2><p>Escape velocity vₑₛc<b>{s.ve.toFixed(2)} km/s</b></p><p>Residual speed at ∞<b>{s.residual.toFixed(2)} km/s</b></p><p>Total energy per mass<b>{s.energy.toExponential(2)} J/kg</b></p><p>Status<b>{s.status}</b></p></section></aside><section className="frs-equation frs-panel"><h2>EQUATION</h2><b>vₑₛc = √(2GM / R)</b><p>If launch speed exceeds escape velocity, the object is unbound.</p></section></div><div className="frs-actions"><button onClick={()=>setInput(ESCAPE_DEFAULTS)}>⟳ Reset</button><button className="frs-next" onClick={()=>setPanel('Challenge')}>Next Challenge　→</button></div><footer>GRAVITATION　|　ESCAPE VELOCITY</footer></main>{panel&&<MasteryChallengeDialog title={panel} close={()=>setPanel(null)}><p>Adjust speed until the status changes from bound to unbound.</p></MasteryChallengeDialog>}</div>}
