import{useState}from'react';import{Link}from'react-router-dom';import{MasteryChallengeDialog}from'../components/MasteryChallengeDialog';import{DIFF_DEFAULTS,diffractionSettings,diffractionSolution,type DiffractionInput}from'../lib/diffractionStudio';import'../friction-studio.css';function C({label,value,min,max,unit,onChange}:{label:string;value:number;min:number;max:number;unit:string;onChange:(v:number)=>void}){return <label className="frs-control"><span>{label}<input aria-label={label+' value'} type="number" value={value.toFixed(1)} onChange={e=>onChange(Number(e.target.value))}/><em>{unit}</em></span><input aria-label={label} type="range" min={min} max={max} step={(max-min)/100} value={value} onChange={e=>onChange(Number(e.target.value))}/><small><i>{min}</i><i>{max}</i></small></label>}export default function DiffractionStudioPage(){const[input,setInput]=useState(DIFF_DEFAULTS),[panel,setPanel]=useState<string|null>(null);const s=diffractionSolution(input);const update=(p:Partial<DiffractionInput>)=>setInput(diffractionSettings({...input,...p}));return <div className="frs-page tension-page" data-ui-theme="dark"><aside className="frs-side"><Link className="frs-brand" to="/concept-studio">◈ OPTICS</Link><nav><Link to="/concept-studio">⌂ Home</Link><Link to="/optics/reflection">Reflection</Link><Link to="/optics/refraction">Refraction</Link><Link to="/optics/lenses">Lenses</Link><Link to="/optics/mirrors">Mirrors</Link><Link to="/optics/interference">Interference</Link><Link to="/optics/diffraction">Diffraction</Link></nav></aside><main id="content" className="frs-main"><header className="frs-head"><h1>OPTICS　›　<span>DIFFRACTION</span></h1><p>Explore the single-slit diffraction pattern and central maximum.</p></header><div className="frs-modes">{['Observe','Predict','Experiment','Explain'].map(m=><button key={m} onClick={()=>m!=='Observe'&&setPanel(m)}>{m}</button>)}</div><div className="frs-layout"><section className="frs-stage"><img src="/assets/friction-studio/reflection.png" alt="Single slit diffraction apparatus"/><svg viewBox="0 0 900 380"><line x1="120" y1="200" x2="350" y2="200" stroke="#ff3855" strokeWidth="4"/><rect x="350" y="120" width="20" height="160" fill="#222"/><line x1="360" y1="180" x2="360" y2="220" stroke="white" strokeWidth="6"/>{Array.from({length:15},(_,i)=><ellipse key={i} cx={650+i*10} cy="200" rx={4+i%3} ry={50/(i+1)} fill="#ff4b55" opacity={.8-i*.03}/>)}<rect x="650" y="80" width="20" height="240" fill="#b64050" opacity=".4"/><text x="610" y="60" fill="#d6eaff">Screen</text></svg></section><aside className="frs-right"><section className="frs-panel"><h2>CONTROLS</h2><C label="Aperture Width a" value={input.width} min={10} max={500} unit="µm" onChange={width=>update({width})}/><C label="Wavelength λ" value={input.wavelength} min={400} max={700} unit="nm" onChange={wavelength=>update({wavelength})}/><C label="Screen Distance L" value={input.screen} min={.1} max={2} unit="m" onChange={screen=>update({screen})}/><C label="Detector Position y" value={input.position} min={-10} max={10} unit="mm" onChange={position=>update({position})}/></section><section className="frs-panel frs-readings"><h2>LIVE MEASUREMENTS</h2><p>Central maximum width Δy<b>{s.dy.toFixed(2)} mm</b></p><p>Angular spread θ<b>±{s.angle.toFixed(2)}°</b></p><p>Slit width a<b>{input.width.toFixed(0)} µm</b></p><p>Wavelength λ<b>{input.wavelength.toFixed(0)} nm</b></p><p>Intensity at detector<b>{s.I.toFixed(2)}</b></p></section></aside><section className="frs-equation frs-panel"><h2>DIFFRACTION EQUATION</h2><b>a sin θ = mλ　　Δy ≈ 2Lλ/a</b><p>Narrower apertures produce a wider central maximum.</p></section></div><div className="frs-actions"><button onClick={()=>setInput(DIFF_DEFAULTS)}>⟳ Reset</button><button className="frs-next" onClick={()=>setPanel('Challenge')}>Next Challenge　→</button></div><footer>OPTICS　|　DIFFRACTION</footer></main>{panel&&<MasteryChallengeDialog title={panel} close={()=>setPanel(null)}><p>Decrease the slit width and observe the wider pattern.</p></MasteryChallengeDialog>}</div>}
