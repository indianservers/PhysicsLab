import{useState}from'react';import{Link}from'react-router-dom';import{MasteryChallengeDialog}from'../components/MasteryChallengeDialog';import{KINETIC_DEFAULTS,kineticSettings,kineticSolution,type KineticInput}from'../lib/kineticStudio';import'../friction-studio.css';
function C({label,value,min,max,unit,onChange}:{label:string;value:number;min:number;max:number;unit:string;onChange:(v:number)=>void}){return <label className="frs-control"><span>{label}<input aria-label={label+' value'} type="number" value={value.toFixed(2)} onChange={e=>onChange(Number(e.target.value))}/><em>{unit}</em></span><input aria-label={label} type="range" value={value} min={min} max={max} step=".01" onChange={e=>onChange(Number(e.target.value))}/><small><i>{min}</i><i>{max}</i></small></label>}
export default function KineticEnergyStudioPage(){const[input,setInput]=useState(KINETIC_DEFAULTS),[panel,setPanel]=useState<string|null>(null),[zoom,setZoom]=useState(false);const s=kineticSolution(input),s2=kineticSolution({mass:input.mass,speed:input.speed*2});const update=(p:Partial<KineticInput>)=>setInput(kineticSettings({...input,...p}));const reset=()=>{setInput(KINETIC_DEFAULTS);setPanel(null);setZoom(false)};return <div className="frs-page tension-page" data-ui-theme="dark"><aside className="frs-side"><Link className="frs-brand" to="/concept-studio">⚛ PHYSICS STUDIO</Link><nav><Link to="/concept-studio">⌂ Home</Link><button>⚙ Mechanics⌃</button>{[['Motion','/concept-studio/motion-kinematics'],['Forces','/motion/friction'],['Work, Energy & Power','/motion/work'],['Rotational Motion','/mechanics/torque-levers'],['Gravitation','/concept-studio/gravitation'],['Waves','/concept-studio/waves-sound'],['Thermodynamics','/concept-studio/thermodynamics'],['Electricity','/concept-studio/electricity'],['Optics','/concept-studio/waves-sound'],['Modern Physics','/concept-studio/modern-physics']].map(([n,to])=><Link key={n} to={to}>{n}</Link>)}</nav></aside><main id="content" className="frs-main"><header className="frs-head"><h1>WORK, ENERGY &amp; POWER　›　<span>KINETIC ENERGY</span></h1><p>Explore how mass and speed affect kinetic energy.</p></header><div className="frs-modes">{['Observe','Predict','Experiment','Explain'].map(m=><button key={m} aria-pressed={m==='Experiment'} onClick={()=>m!=='Observe'&&setPanel(m)}>{m}</button>)}</div><div className="frs-layout"><section className="frs-stage" style={{transform:zoom?'scale(1.1)':'none'}}><img src="/assets/friction-studio/kinetic-track.png" alt="Blue and purple carts crossing photogates on an air track"/><svg viewBox="0 0 900 380"><text x="280" y="150" fill="#5bcaff" fontSize="20">m = {input.mass.toFixed(2)} kg</text><text x="600" y="150" fill="#b878ff" fontSize="20">v = {input.speed.toFixed(2)} m/s</text></svg></section><aside className="frs-right"><section className="frs-panel"><h2>CONTROLS</h2><C label="Mass (kg)" value={input.mass} min={.1} max={2} unit="kg" onChange={mass=>update({mass})}/><C label="Speed (m/s)" value={input.speed} min={.1} max={4} unit="m/s" onChange={speed=>update({speed})}/></section><section className="frs-panel frs-readings"><h2>ENERGY COMPARISON</h2><p>Cart 1 kinetic energy<b>{s.energy.toFixed(2)} J</b></p><p>Cart 1 momentum<b>{s.momentum.toFixed(2)} kg·m/s</b></p><p>Cart 2 at 2× speed<b>{s2.energy.toFixed(2)} J</b></p><p>Energy ratio<b>{(s2.energy/s.energy).toFixed(1)}×</b></p></section></aside><section className="frs-equation frs-panel"><h2>KEY EQUATION</h2><b>K = ½mv²</b><p>Doubling speed quadruples kinetic energy when mass stays constant.</p></section></div><div className="frs-actions"><button aria-label="Reset" onClick={reset}>⟳ Reset</button><button aria-label={zoom?'Reset Zoom':'Zoom'} onClick={()=>setZoom(v=>!v)}>⌕ {zoom?'Reset Zoom':'Zoom'}</button><button className="frs-next" onClick={()=>setPanel('Challenge')}>Next Challenge　→</button></div><footer>WORK, ENERGY &amp; POWER　|　KINETIC ENERGY</footer></main>{panel&&<MasteryChallengeDialog title={panel} close={()=>setPanel(null)}><p>{panel==='Predict'?'How does kinetic energy change when speed doubles?':panel==='Explain'?'Kinetic energy depends linearly on mass and quadratically on speed.':'Change mass and speed and compare both carts.'}</p></MasteryChallengeDialog>}</div>}
