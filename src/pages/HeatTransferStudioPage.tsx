import{useState}from'react';import{Link}from'react-router-dom';import{MasteryChallengeDialog}from'../components/MasteryChallengeDialog';import{HEAT_DEFAULTS,heatSettings,heatSolution,type HeatInput}from'../lib/heatTransferStudio';import'../friction-studio.css';function C({label,value,min,max,unit,onChange}:{label:string;value:number;min:number;max:number;unit:string;onChange:(v:number)=>void}){return <label className="frs-control"><span>{label}<input aria-label={label+' value'} type="number" value={value.toFixed(0)} onChange={e=>onChange(Number(e.target.value))}/><em>{unit}</em></span><input aria-label={label} type="range" min={min} max={max} step="1" value={value} onChange={e=>onChange(Number(e.target.value))}/><small><i>{min}</i><i>{max}</i></small></label>}export default function HeatTransferStudioPage(){const[input,setInput]=useState(HEAT_DEFAULTS),[panel,setPanel]=useState<string|null>(null);const s=heatSolution(input);const update=(p:Partial<HeatInput>)=>setInput(heatSettings({...input,...p}));return <div className="frs-page tension-page" data-ui-theme="dark"><aside className="frs-side"><Link className="frs-brand" to="/concept-studio">♨ THERMODYNAMICS</Link><nav><Link to="/concept-studio">⌂ Home</Link><Link to="/thermodynamics/temperature">Temperature</Link><Link to="/thermodynamics/heat-transfer">Heat Transfer</Link><Link to="/thermodynamics/gas-laws">Gas Laws</Link></nav></aside><main id="content" className="frs-main"><header className="frs-head"><h1>THERMODYNAMICS　›　<span>HEAT TRANSFER</span></h1><p>Compare conduction through a rod, convection in a fluid, and thermal radiation.</p></header><div className="frs-modes">{['Observe','Predict','Experiment','Explain'].map(m=><button key={m} onClick={()=>m!=='Observe'&&setPanel(m)}>{m}</button>)}</div><div className="frs-layout"><section className="frs-stage"><svg viewBox="0 0 900 380"><rect width="900" height="380" fill="#182d3b"/><rect x="130" y="150" width="150" height="80" fill="#ef534b"/><rect x="620" y="150" width="150" height="80" fill="#307ddc"/><rect x="280" y="175" width="340" height="30" fill="url(#g)"/><defs><linearGradient id="g"><stop stopColor="#ff4a35"/><stop offset="1" stopColor="#2179ed"/></linearGradient></defs><text x="175" y="140" fill="white">Hot {s.hot.toFixed(0)}°C</text><text x="650" y="140" fill="white">Cold 20°C</text><text x="350" y="280" fill="#9ed5ff">Heat flux q = {(s.flux/1000).toFixed(2)} ×10³ W/m²</text></svg></section><aside className="frs-right"><section className="frs-panel"><h2>CONTROLS</h2><label>Material <select aria-label="Material" value={input.material} onChange={e=>update({material:Number(e.target.value)})}><option value="237">Aluminum (k=237 W/mK)</option><option value="401">Copper (k=401 W/mK)</option><option value="1.4">Glass (k=1.4 W/mK)</option></select></label><C label="Temperature Difference ΔT" value={input.delta} min={10} max={200} unit="°C" onChange={delta=>update({delta})}/><C label="Time t" value={input.time} min={0} max={600} unit="s" onChange={time=>update({time})}/></section><section className="frs-panel frs-readings"><h2>LIVE READINGS</h2><p>Hot end<b>{s.hot.toFixed(1)} °C</b></p><p>Cold end<b>{s.cold.toFixed(1)} °C</b></p><p>Heat flux q<b>{s.flux.toExponential(2)} W/m²</b></p><p>Total heat transfer rate Q<b>{s.rate.toFixed(2)} W</b></p></section></aside><section className="frs-equation frs-panel"><h2>THEORY &amp; COMPARISON</h2><b>q = −k dT/dx ≈ k(Tₕ−T꜀)/L</b><p>Conduction through the rod transfers heat from hot to cold.</p></section></div><div className="frs-actions"><button onClick={()=>setInput(HEAT_DEFAULTS)}>⟳ Reset</button><button className="frs-next" onClick={()=>setPanel('Challenge')}>Next Challenge　→</button></div><footer>THERMODYNAMICS　|　HEAT TRANSFER</footer></main>{panel&&<MasteryChallengeDialog title={panel} close={()=>setPanel(null)}><p>Increase the temperature difference and compare heat flux.</p></MasteryChallengeDialog>}</div>}
