import{useState}from'react';import{Link}from'react-router-dom';import{MasteryChallengeDialog}from'../components/MasteryChallengeDialog';import{DIODE_DEFAULTS,diodeSettings,diodeSolution,type DiodeInput}from'../lib/diodeStudio';import'../friction-studio.css';function C({label,value,min,max,unit,onChange}:{label:string;value:number;min:number;max:number;unit:string;onChange:(v:number)=>void}){return <label className="frs-control"><span>{label}<input aria-label={label+' value'} type="number" value={value.toFixed(2)} onChange={e=>onChange(Number(e.target.value))}/><em>{unit}</em></span><input aria-label={label} type="range" min={min} max={max} step=".01" value={value} onChange={e=>onChange(Number(e.target.value))}/><small><i>{min}</i><i>{max}</i></small></label>}export default function DiodesStudioPage(){const[input,setInput]=useState(DIODE_DEFAULTS),[panel,setPanel]=useState<string|null>(null);const s=diodeSolution(input);const update=(p:Partial<DiodeInput>)=>setInput(diodeSettings({...input,...p}));return <div className="frs-page tension-page" data-ui-theme="dark"><aside className="frs-side"><Link className="frs-brand" to="/concept-studio">▣ ELECTRONICS</Link><nav><Link to="/concept-studio">⌂ Home</Link><Link to="/electronics/semiconductors">Semiconductors</Link><Link to="/electronics/diodes">Diodes</Link><Link to="/electronics/transistors">Transistors</Link><Link to="/electronics/sensors">Sensors</Link></nav></aside><main id="content" className="frs-main"><header className="frs-head"><h1>ELECTRONICS　›　<span>DIODE I–V CHARACTERISTICS</span></h1><p>Build and explore a real PN diode circuit. Adjust the bias and observe how the diode conducts.</p></header><div className="frs-modes">{['Observe','Predict','Experiment','Explain'].map(m=><button key={m} onClick={()=>m!=='Observe'&&setPanel(m)}>{m}</button>)}</div><div className="frs-layout"><section className="frs-stage"><svg viewBox="0 0 900 380"><rect width="900" height="380" fill="#182c3d"/><line x1="140" y1="210" x2="350" y2="210" stroke="#36aaff" strokeWidth="6"/><rect x="350" y="170" width="90" height="80" fill="#c19a6b"/><line x1="440" y1="170" x2="440" y2="250" stroke="#222" strokeWidth="10"/><line x1="440" y1="210" x2="700" y2="210" stroke="#36aaff" strokeWidth="6"/><text x="370" y="145" fill="white">PN diode</text><text x="170" y="180" fill="#9ed5ff">V = {input.voltage.toFixed(2)} V</text><text x="480" y="180" fill="#9ed5ff">I = {s.current.toFixed(2)} mA</text></svg></section><aside className="frs-right"><section className="frs-panel"><h2>CONTROLS</h2><C label="Bias Voltage Vₛ" value={input.voltage} min={0} max={5} unit="V" onChange={voltage=>update({voltage})}/><label>Diode Material <select aria-label="Diode Material" value={input.material} onChange={e=>update({material:Number(e.target.value)})}><option value="1">Silicon (Si)</option><option value="-0.1">Germanium (Ge)</option></select></label><C label="Series Resistance R" value={input.resistance} min={10} max={10000} unit="Ω" onChange={resistance=>update({resistance})}/></section><section className="frs-panel frs-readings"><h2>LIVE READINGS</h2><p>Threshold V<sub>D</sub><b>{s.threshold.toFixed(2)} V</b></p><p>Current I<sub>D</sub><b>{s.current.toFixed(2)} mA</b></p><p>Power P<sub>D</sub><b>{s.power.toFixed(2)} mW</b></p></section></aside><section className="frs-equation frs-panel"><h2>DIODE EQUATION</h2><b>I = Iₛ(e<sup>Vᴅ/(nVᵀ)</sup> − 1)</b><p>Forward current rises sharply above the threshold voltage.</p></section></div><div className="frs-actions"><button onClick={()=>setInput(DIODE_DEFAULTS)}>⟳ Reset</button><button className="frs-next" onClick={()=>setPanel('Challenge')}>Next Challenge　→</button></div><footer>ELECTRONICS　|　DIODES</footer></main>{panel&&<MasteryChallengeDialog title={panel} close={()=>setPanel(null)}><p>Raise the bias above threshold and observe the current curve.</p></MasteryChallengeDialog>}</div>}

