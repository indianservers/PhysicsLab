import{useState}from'react';import{Link}from'react-router-dom';import{MasteryChallengeDialog}from'../components/MasteryChallengeDialog';import{INDUC_DEFAULTS,inductionSettings,inductionSolution,type InductionInput}from'../lib/inductionStudio';import'../friction-studio.css';function C({label,value,min,max,unit,onChange}:{label:string;value:number;min:number;max:number;unit:string;onChange:(v:number)=>void}){return <label className="frs-control"><span>{label}<input aria-label={label+' value'} type="number" value={value.toFixed(2)} onChange={e=>onChange(Number(e.target.value))}/><em>{unit}</em></span><input aria-label={label} type="range" min={min} max={max} step=".01" value={value} onChange={e=>onChange(Number(e.target.value))}/><small><i>{min}</i><i>{max}</i></small></label>}export default function InductionStudioPage(){const[input,setInput]=useState(INDUC_DEFAULTS),[panel,setPanel]=useState<string|null>(null);const s=inductionSolution(input);const update=(p:Partial<InductionInput>)=>setInput(inductionSettings({...input,...p}));return <div className="frs-page tension-page" data-ui-theme="dark"><aside className="frs-side"><Link className="frs-brand" to="/concept-studio">◉ MAGNETISM</Link><nav><Link to="/concept-studio">⌂ Home</Link><Link to="/magnetism/solenoids">Solenoids</Link><Link to="/magnetism/induction">Induction</Link><Link to="/magnetism/electromagnets">Electromagnets</Link></nav></aside><main id="content" className="frs-main"><header className="frs-head"><h1>MAGNETISM　›　<span>ELECTROMAGNETIC INDUCTION</span></h1><p>A changing magnetic flux induces an emf in a closed circuit.</p></header><div className="frs-modes">{['Observe','Predict','Experiment','Explain'].map(m=><button key={m} onClick={()=>m!=='Observe'&&setPanel(m)}>{m}</button>)}</div><div className="frs-layout"><section className="frs-stage"><svg viewBox="0 0 900 380"><rect width="900" height="380" fill="#122a3b"/><rect x="500" y="100" width="170" height="190" rx="40" fill="none" stroke="#c47d4d" strokeWidth="16"/>{Array.from({length:8},(_,i)=><ellipse key={i} cx="585" cy="195" rx={70+i*8} ry={90+i*4} fill="none" stroke="#5fbaff" opacity=".7"/>)}<rect x="180" y="160" width="100" height="70" fill="#dd4348"/><rect x="280" y="160" width="100" height="70" fill="#2873d9"/><line x1="380" y1="195" x2="500" y2="195" stroke="white" strokeWidth="6"/><text x="530" y="70" fill="white">ε = {(s.emf*1000).toFixed(0)} mV</text><text x="520" y="330" fill="#9ed5ff">Φ = {(s.flux*1000).toFixed(2)} mWb</text></svg></section><aside className="frs-right"><section className="frs-panel"><h2>CONTROLS</h2><C label="Magnet Speed" value={input.speed} min={0} max={1} unit="m/s" onChange={speed=>update({speed})}/><C label="Coil Turns N" value={input.turns} min={50} max={800} unit="turns" onChange={turns=>update({turns})}/><label>Motion Direction <select aria-label="Motion Direction" value={input.direction} onChange={e=>update({direction:Number(e.target.value)})}><option value="1">Into coil (N first)</option><option value="-1">Out of coil (S first)</option></select></label></section><section className="frs-panel frs-readings"><h2>LIVE READINGS</h2><p>Magnetic Flux Φ<b>{(s.flux*1000).toFixed(2)} mWb</b></p><p>Induced EMF ε<b>{(s.emf*1000).toFixed(0)} mV</b></p><p>Motion direction<b>{input.direction>0?'Into coil':'Out of coil'}</b></p><p>Coil turns N<b>{input.turns}</b></p></section></aside><section className="frs-equation frs-panel"><h2>FARADAY’S LAW</h2><b>ε = −N dΦ/dt</b><p>Lenz’s law sets the induced current direction to oppose the flux change.</p></section></div><div className="frs-actions"><button onClick={()=>setInput(INDUC_DEFAULTS)}>⟳ Reset</button><button className="frs-next" onClick={()=>setPanel('Challenge')}>Next Challenge　→</button></div><footer>MAGNETISM　|　ELECTROMAGNETIC INDUCTION</footer></main>{panel&&<MasteryChallengeDialog title={panel} close={()=>setPanel(null)}><p>Maximize induced emf by changing speed and turns.</p></MasteryChallengeDialog>}</div>}
