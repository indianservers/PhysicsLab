import{useState}from'react';import{Link}from'react-router-dom';import{MasteryChallengeDialog}from'../components/MasteryChallengeDialog';import{SPRING_DEFAULTS,springSettings,springSolution,type SpringInput}from'../lib/springStudio';import'../friction-studio.css';function C({label,value,min,max,unit,onChange}:{label:string;value:number;min:number;max:number;unit:string;onChange:(v:number)=>void}){return <label className="frs-control"><span>{label}<input aria-label={label+' value'} type="number" value={value.toFixed(3)} onChange={e=>onChange(Number(e.target.value))}/><em>{unit}</em></span><input aria-label={label} type="range" min={min} max={max} step={(max-min)/100} value={value} onChange={e=>onChange(Number(e.target.value))}/><small><i>{min}</i><i>{max}</i></small></label>}export default function SpringShmStudioPage(){const[input,setInput]=useState(SPRING_DEFAULTS),[panel,setPanel]=useState<string|null>(null);const s=springSolution(input);const update=(p:Partial<SpringInput>)=>setInput(springSettings({...input,...p}));return <div className="frs-page tension-page" data-ui-theme="dark"><aside className="frs-side"><Link className="frs-brand" to="/concept-studio">⚛ PHYSICS STUDIO</Link><nav><Link to="/concept-studio">⌂ Home</Link><Link to="/motion/spring-shm">Spring SHM</Link><Link to="/motion/pendulum">Pendulum</Link><Link to="/motion/energy-exchange">Energy Exchange</Link><Link to="/motion/damping">Damping</Link><Link to="/motion/resonance">Resonance</Link></nav></aside><main id="content" className="frs-main"><header className="frs-head"><h1>OSCILLATIONS　›　<span>SPRING SHM</span></h1><p>Model a vertical mass–spring system and explore simple harmonic motion.</p></header><div className="frs-modes">{['Observe','Predict','Experiment','Explain'].map(m=><button key={m} onClick={()=>m!=='Observe'&&setPanel(m)}>{m}</button>)}</div><div className="frs-layout"><section className="frs-stage"><img src="/assets/friction-studio/spring.png" alt="Vertical spring mass apparatus"/><svg viewBox="0 0 900 380"><path d={`M430 55 q-30 20 0 40 q30 20 0 40 q-30 20 0 40 q30 20 0 40 q-30 20 0 40`} fill="none" stroke="#77d8ff" strokeWidth="4"/><circle cx="430" cy="290" r="22" fill="#c38b40"/><line x1="520" y1="30" x2="520" y2="340" stroke="#9ccfff" opacity=".5"/><text x="540" y="100" fill="#d5ecff">x(t) = {s.x.toFixed(3)} m</text></svg></section><aside className="frs-right"><section className="frs-panel"><h2>SIMULATION CONTROLS</h2><C label="Spring Constant k" value={input.k} min={10} max={200} unit="N/m" onChange={k=>update({k})}/><C label="Mass m" value={input.m} min={.05} max={1} unit="kg" onChange={m=>update({m})}/><C label="Amplitude A" value={input.amplitude} min={.01} max={.3} unit="m" onChange={amplitude=>update({amplitude})}/><C label="Time t" value={input.time} min={0} max={10} unit="s" onChange={time=>update({time})}/></section><section className="frs-panel frs-readings"><h2>LIVE READINGS</h2><p>Displacement x<b>{s.x.toFixed(3)} m</b></p><p>Velocity v<b>{s.vel.toFixed(3)} m/s</b></p><p>Acceleration a<b>{s.acc.toFixed(2)} m/s²</b></p><p>Period T<b>{s.period.toFixed(3)} s</b></p><p>Angular frequency ω<b>{s.omega.toFixed(2)} rad/s</b></p></section></aside><section className="frs-equation frs-panel"><h2>GOVERNING EQUATION</h2><b>a = −ω²x　　ω = √(k/m)　　T = 2π/ω</b><p>Displacement, velocity, and acceleration remain phase-linked in simple harmonic motion.</p></section></div><div className="frs-actions"><button onClick={()=>setInput(SPRING_DEFAULTS)}>⟳ Reset</button><button className="frs-next" onClick={()=>setPanel('Challenge')}>Next Challenge　→</button></div><footer>OSCILLATIONS　|　SPRING SHM</footer></main>{panel&&<MasteryChallengeDialog title={panel} close={()=>setPanel(null)}><p>How does the period change when the mass doubles?</p></MasteryChallengeDialog>}</div>}
