# Asset provenance — Matter Waves

No external raster assets were used. The electron gun, crystal, diffraction rings, and detector are authored as inline SVG/CSS in `src/pages/MatterWavesStudioPage.tsx`.
