# Curved Mirrors

- Mockup: `10_optics/04_mirrors.png`
- Route: `/optics/mirrors`
- Status: VERIFIED

Implemented concave/convex mirror optical bench with curvature-radius, object-distance, and mirror-type controls, live focal length, image distance, magnification, image nature, ray construction, mirror equation, reset, and challenge dialog. Uses `1/f = 1/d₀ + 1/dᵢ` with `f = ±R/2`.

Validation: production build succeeds; direct route load passes; default focal length 20.0 cm, image distance 60.0 cm, magnification −2.00; controls update; all required responsive viewports have no horizontal overflow; screenshot captured at 1536x1024.
