# Asset provenance — Radioactivity

No external raster assets were used. Radiation sources, shielding blocks, detector, and labels are authored as inline SVG/CSS in `src/pages/RadioactivityStudioPage.tsx`.
