# Asset provenance — Density & Pressure

No external raster assets were used. The vessel, fluid layers, probe, and annotations are authored as inline SVG/CSS in `src/pages/DensityPressureStudioPage.tsx`.
