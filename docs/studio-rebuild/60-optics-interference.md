# Optics Interference

- Mockup: `10_optics/05_interference.png`
- Route: `/optics/interference`
- Status: VERIFIED

Implemented a double-slit laser experiment with wavelength, slit-separation, screen-distance, and detector-position controls; live fringe spacing and intensity, ray visualization, interference equation, reset, and challenge dialog. Uses `Δy = λL/d` and `I/I₀ = cos²(πdy/λL)`.

Validation: production build succeeds; direct route load passes; default fringe spacing 2.17 mm and central intensity 1.00; controls update; all required responsive viewports have no horizontal overflow; screenshot captured at 1536x1024.
