# Asset provenance — Photoelectric Effect

No external raster assets were used. The photocell, electrodes, light beam, and electron paths are authored as inline SVG/CSS in `src/pages/PhotoelectricStudioPage.tsx`.
