# Motion & Kinematics — Position–Time

Status: VERIFIED. Page19/102. Mechanics pages13–18 completed every gate before this studio began.

## Studio inventory and order

04_motion_kinematics contains six unique mockups:01_position_time,02_velocity_time,03_acceleration_time,04_projectile_motion,05_relative_motion,06_circular_motion. Related routes in inventory.json:distance-time-graph,uniform-motion (two different target pages),projectile-motion,parent motion-kinematics (no dedicated relative-motion route),circular-motion. Work only on Position–Time until all its gates pass.

## Reference inspection

01_position_time.png1536×1024. Sidebar238px, brand/small tagline, Home, expanded Mechanics with Motion & Graphs selected, Waves/Thermo/Electromagnetism/Modern/Tools accordion groups, Settings/Help at bottom. Headingx267y20 eyebrow, Position–Time28px y43; breadcrumb upperright. Modesx256y82w1262h45. Left apparatusx256y140w586h459, realistic blue trolley with vertical peg, silver ruler track, dark motion sensor at right, gray lab wall/bench. Right graphx853y140w665h459; x-axis0..11s, y-axis-.5..3m, cyanpositiontrace, currentpointt4, purple tangent, dashed verticalcursor, floating readout and lower-right relation v=dx/dt and Δx=x(t2)-x(t1).

Controls rowy611h132: initialpositionx258w373, x0=.5m range-1..2; velocity-programx643w464, selected accelerate→constant→decelerate; timewindowx1119w398 T10s range2..12. Lower readingsy758h123:position,slope,displacement,selectedpoints. Footer y903 border:reset,pause,currenttime4.00s,timeline,speed−1×+,fullscreen,nextchallenge. Both selectedtimes/points must be interactive, not display-only. Camera is an instructional side elevation; simplest suitable engine is SVG apparatus with real generated materials/sprites and exact dynamic graph.

Reference contradictions: curve begins at0 even though x0=.5. At t4 it claims x1.36 and slope.62 while printed acceleration schedule (+.80 for0–2s,0 for2–6s,−.60 for6–8s) would give x5.30,v1.60 from rest at4s. This schedule also does not stop at8s; terminalv.40. The illustrated track and flattened curve are incompatible with those numbers. Preserve composition and educational intent, correct values and explicitly display the actual program.

## Existing implementation inspection

Opened /experiments/distance-time-graph default view and Simulate tab; screenshots artifacts/studio-rebuild/19-position-time/before-default.png and before-simulate.png. Existing default route selects legacy3D; Simulate is the registered DistanceTimeGraphLab (Split Interactive Motion Studio). Inspected its state, controls, drag endpoints, distance/position modes, segment addition/removal, playback/speed/reduced motion, graph/table and challenge. Preserve all existing route/content/contracts.

Read distanceTimeSimulation.ts: compileJourney/sampleJourney implement piecewise constant signed velocity, constrained duration.5..8s and speed-5..5(position) or0..5(distance); correct continuity. Its default journey is rest1s,1.5m/s2s,4.5m/s1s. This cannot directly represent a continuous acceleration program without changing its existing contract. Also inspected uniform-motionSimulation.ts and lesson metadata; uniform motion x=x0+vt is correct. New page will use a separate exact acceleration-program module, reusing existing shared UI, icons/dialogs and cart assets where appropriate. Proposed dedicated route /motion/position-time, retain original lesson route.

## Implemented physical model

src/lib/positionTime.ts: x0 range -1..2m; T2..12s, default x0=.5,T10. Default starts at rest, a+.12m/s² for0–2s, coasts0 for2–6s, brakes-.12 for6–8s, then remains at rest. The gentler acceleration matches the depicted metre-scale track and preserves a smooth rising-then-flat position curve. Values and program labels are calculated consistently: at4s x1.22m,v.24m/s,Δx.72m; after8s x1.94m,v0. This corrects the contradictory reference, not any existing lesson parameters.

Other programs: uniform+.12m/s, constant acceleration+.02 from rest, forward/return with initial+.24 then-.12acceleration2–6s (turns at4,returns to x0 at8), and rest. Exact integration x+=vΔt+.5aΔt²,v+=aΔt keeps x/v continuous. Samples include all phase boundaries and exact velocity-zero extrema. Selected-point displacement is signed and averagevelocity is null for equal times; no divide-by-zero. Playback time clamps to the chosen observation window, not a false physical stop.

scripts/testPositionTimePhysics.mjs passes three groups: exact default and rest plateau; all programs/x0/window limits, finite-difference tangent slopes, selected-point reversal/equal-time boundaries; exact turnaround/return and negative displacement. physics-results.json records values. UI implemented at /motion/position-time, with a parent studio link. PositionTimePage owns controls/playback, PositionTimeGraph maps exact samples and tangent slopes to responsive SVG, and PositionTimeScene maps the same position to a calibrated ruler. Generated backdrop/sensor and reused blue-cart sprite provide apparatus realism; the cart is explicitly enlarged for clarity. Existing lesson route and parameters are preserved.

Browser test passes playback, speed limits/reset, all numeric/range bounds and drafts, all five programs, reverse/equal selected intervals, keyboard/pointer graph selection, four modes, prediction feedback, experiment checkpoints, tangent/settings/help, all three challenges and fullscreen with portal dialogs. Six refreshed screenshots pass2-* captured after final sensor integration, with zero horizontal overflow. Visually reviewed 1536×1024, 1440×900, 1280×720, 1024×768, 768×1024 and 390×844. Desktop composition closely follows reference; narrower layouts stack graph and controls, retain actual pixel-sized graph text and horizontally scroll navigation. No page errors or failed requests; only pre-existing React Router migration warnings.

Final checks passed: 14 boundary captures (seven motion states at desktop/mobile), 17 sidebar links, all expandable groups, visible parent hero link, direct reload, preserved legacy Simulate lab, graph drag beyond its boundary with pointer capture, and playback restart. Parent link was initially inside a pre-existing hidden container; relocated only this new link to the visible hero. Negative-position boundary captures exposed equation/readout overlap; the equation panel now chooses upper free space for low curves and avoids the readout. Rechecked negative return/rest and maximum positive acceleration states visually. All six default responsive screenshots remain clear. Animation timing over 90 frames: median7ms, p95 13.6ms, max13.9ms. Zero errors or failed requests.

Final production build 84453 exited0 after the graph correction: 794 precache entries. Existing large-chunk advisory remains; no build configuration changes. All page gates VERIFIED. Evidence: browser-prechecks.json, final-checks.json, physics-results.json, pass2-* and extreme-* screenshots, parent-link.png. Asset provenance and exact prompts:19-position-assets.md. No live handles remain. Next page: Velocity–Time, page20, in the same Motion & Kinematics studio.
