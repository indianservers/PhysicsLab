# Asset provenance — Entropy

No external raster assets were used. The two-chamber apparatus and particle field are authored as inline SVG/CSS in `src/pages/EntropyStudioPage.tsx`. The entropy readout uses the educational reference scale for the displayed particle count while preserving the ΔS ∝ N ln 2 relationship.
