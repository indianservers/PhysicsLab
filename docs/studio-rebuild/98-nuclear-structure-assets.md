# Asset provenance — Nuclear Structure

No external raster assets were used. The nucleon visualization, orbit shell, and annotations are authored as inline SVG/CSS in `src/pages/NuclearStructureStudioPage.tsx`.
