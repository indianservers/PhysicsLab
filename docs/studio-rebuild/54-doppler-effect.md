# Doppler Effect

- Mockup: `09_waves_sound/06_doppler_effect.png`
- Route: `/motion/doppler-effect`
- Status: VERIFIED

Implemented moving source and stationary observer visualization with source/observer speed and emitted frequency controls, live approaching/receding frequencies and wavelengths, Doppler equation, reset, and challenge dialog. Uses `f′ = f(v ± vₒ)/(v ∓ vₛ)` with sound speed 343 m/s.

Validation: production build succeeds; direct route load passes; default approaching frequency 876.7 Hz, receding frequency 735.7 Hz; controls update; all required responsive viewports have no horizontal overflow; screenshot captured at 1536x1024.
